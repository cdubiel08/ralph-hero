---
date: 2026-05-12
github_issue: 1203
github_url: https://github.com/cdubiel08/ralph-hero/issues/1203
plan_document: thoughts/shared/plans/2026-05-12-group-GH-1203-1207-memory-tier-productization.md
status: approved
type: review
tags: [plan-review, memory-tier, dream-loop, ralph-knowledge, ralph-engine, embedder]
---

# Plan Critique: GH-1203 — Memory-Tier Productization (Phase 1 of Group Plan)

## Summary

VERDICT: APPROVED

The plan is a well-researched, atomically-decomposed group plan covering 5 phases (GH-1203 through GH-1207). Phase 1 (this issue's scope) is fully specified with 6 dispatchable tasks, every referenced file exists, and the technical approach correctly identifies the root-cause embedder hotspot. The group-plan structure makes phase dependencies explicit and orders work so each phase's verification produces incremental signal. Cross-repo work (Phase 5 touches `ralph-engine`) is flagged clearly. Plan can proceed directly to implementation.

## Dimension Scores

### 1. Completeness — PASS

All five phases have explicit `Overview`, `Tasks`, and `Phase Success Criteria` sections. Phase 1 (the in-scope issue) lists six tasks (1.1-1.6) each with `files`, `tdd`, `complexity`, `depends_on`, and an `acceptance` checklist. Shared Constraints section (9 invariants) is unusually thorough — explicitly fixes the embedding model, tier enum, engine schema posture, hook latency budget, and idempotence requirements before phase content begins. The `Current State Analysis` accurately maps each phase's surface area to verified file paths and line numbers.

### 2. Feasibility — PASS

Every file referenced in Phase 1 exists on disk and the substantive claims about the codebase verify against current source:

- `plugin/ralph-knowledge/src/embedder.ts` (159 lines) — `embed()` at line 23 and `embedDocument()` at line 91 are accurately described. The plan's "per-chunk await" claim is correct (line 130 calls `await embed(embedText)` inside a loop).
- `plugin/ralph-knowledge/src/reindex.ts` (385 lines) — `parsedDocs[]` at line 97 confirmed; `embedDocument` call at line 221 confirmed.
- `plugin/ralph-knowledge/package.json` — verified missing `--max-old-space-size` and `--expose-gc` in current `reindex` script.
- `scripts/dream/ingest.py` — `_run_reindex` uses `subprocess.run(cmd, shell=True, check=False)` WITHOUT `capture_output=True`, confirming the plan's claim that stderr is currently lost on OOM.
- All Phase 2 SKILL.md files exist (research, ralph-research, plan, ralph-plan, ralph-impl, retro).
- `ralph-engine/packages/knowledge/src/` exists with `sqlite-knowledge-store.ts`, `parser.ts`, `fts-search.ts`, `vector-search.ts`, `hybrid-search.ts`, `types.ts` — all Phase 5 targets present.
- `plugin/ralph-hero/.claude-plugin/plugin.json` exists; ralph-unblock Stop hook precedent verified (`hooks/scripts/unblock-*.sh` present).

Minor: plan cites `embedder.ts:8-50` in the issue body and `23-43` in the plan body. Both ranges cover the relevant code; `embed()` and `embedDocument()` straddle these lines. No load-bearing inaccuracy.

The technical approach is consistent with codebase conventions:
- `transformers.js` `pipeline()` accepts `string[]` and returns a flattened Tensor (claim is correct for v3).
- `better-sqlite3` is already a dep — no new deps for Task 1.6.
- `cross-env` fallback for Task 1.4 is hedged appropriately ("if not already a dep, use bare `NODE_OPTIONS=...`" — the project is mac-first).

### 3. Clarity — PASS

Phase Success Criteria is split into **Automated Verification** and **Manual Verification** subsections with `- [ ]` checkbox format. Examples from Phase 1:

- Automated: `npm test --prefix plugin/ralph-knowledge -- embedder` — embedder tests pass including new `embedChunks` cases
- Manual: `sqlite3 ~/.ralph-hero/knowledge.db "SELECT memory_tier, COUNT(*) FROM documents GROUP BY memory_tier"` shows `reflection` count > 0

Each task's acceptance criteria is testable and specific (e.g., "Embeddings produced by `embedChunks([t1, t2, t3])` are numerically equal (within Float32 epsilon) to sequential `embed(t1), embed(t2), embed(t3)` for the mock pipeline" — directly implementable as a test). Quantified thresholds where appropriate: `EMBED_BATCH_SIZE=16`, `--max-old-space-size=4096`, `RALPH_REMEMBER_MIN_CHARS=200`, `<500ms` hook latency.

### 4. Scope — PASS

`What We're NOT Doing` section explicitly lists 8 out-of-scope items:
- Embedding model swap
- Auto-promotion reflection→wiki
- Migrating engine away from `metadata` JSON
- Native TS port of reflection synthesis
- Expanding Stop hook matcher beyond initial three skills
- Per-project DB isolation
- screenpipe ambient capture
- Refactoring `parser.ts` / `db.ts` in Phase 1

Each Phase ends with a `Creates for next phase` note that bounds inter-phase coupling. Shared Constraints #2 hard-locks the tier enum at four values, preventing scope creep into a fifth tier.

### 5. Dispatchability — PASS

Every Phase 1 task includes the full dispatch-required metadata table:

| Task | files | tdd | complexity | depends_on | acceptance |
|------|-------|-----|------------|------------|------------|
| 1.1 | embedder.ts (modify) | true | medium | null | 6 bullets |
| 1.2 | reindex.ts (modify), embedder.ts (read) | true | high | [1.1] | 8 bullets |
| 1.3 | embedder.ts (modify) | true | medium | [1.1] | 4 bullets |
| 1.4 | package.json (modify) | false | low | null | 4 bullets |
| 1.5 | ingest.py (modify) | true | medium | null | 6 bullets |
| 1.6 | verify-tiers.ts (create), package.json (modify) | false | low | [1.2, 1.4] | 4 bullets |

Phases 2-5 follow the same pattern. Task dependencies are explicit at both intra-phase (e.g., 1.2 depends on 1.1) and inter-phase (Phase 2 depends on Phase 1; Phase 5 only depends on Phase 1, NOT on Phases 2-4) levels.

The group plan also includes phase-level `Creates for next phase` annotations, which fulfill the "what does this phase produce for the next" requirement for multi-issue group plans.

## Group-Plan-Specific Checks

- Phase dependencies are explicit: `## Phase N: ... - depends_on: [phase-X]` lines present on all five phases.
- Integration Testing section at the end covers cross-phase interactions (smoke test from clean DB through all four tiers, role-based retrieval per skill, agent write loop end-to-end, fresh-machine simulation, cross-surface engine query).
- References section enumerates all 5 issue URLs plus 3 prior research docs (linked via `builds_on::` in Prior Work block).

## Minor Notes (non-blocking)

These do NOT trigger NEEDS_ITERATION — they are observations the implementer can resolve at coding time:

1. **Task 1.5 nuance**: The plan correctly notes that `ingest.py`'s `_run_reindex` already returns `rc`, so the "exits 0, hiding OOM" framing in the issue body is slightly imprecise. The plan body resolves this — the real ask is **stderr capture**, not exit-code propagation. The implementer should follow the plan body, not the issue body, for this task.

2. **Task 1.4 cross-env hedge**: The plan correctly offers a `cross-env`-or-not fallback. The implementer should check `package.json` deps first and pick the simpler path; do not add `cross-env` if the existing project is mac-only.

3. **Phase 2 fan-out cost**: `knowledge_recall` fans out one `hybrid.search` per tier in the role policy (2-3 calls). With `rerank=true`, the plan notes ~0.5-1s cold-start. No latency budget violation, but worth tracking via the `knowledge_memory_stats` health surface.

## Conclusion

This is a high-quality group plan. Phase 1 (the in-scope issue) is XS/Small, atomically decomposed, and grounded in verified codebase state. Ready for implementation.

Run `/ralph-impl 1203` to begin Phase 1.
