---
date: 2026-04-26
github_issue: 890
github_url: https://github.com/cdubiel08/ralph-hero/issues/890
plan_document: thoughts/shared/plans/2026-04-26-group-GH-0890-token-resolution-via-gh-auth.md
status: approved
type: review
tags: [plan-review, mcp-server, auth, token-resolution, doctor, setup, gh-cli]
---

# Plan Critique: GH-890 — Group plan for GH-877 token resolution via `gh auth`

## Summary

VERDICT: **APPROVED**

Group plan for issues #890, #891, #892 implementing the load-bearing MCP server `gh auth token` fallback (Phase 1), `just doctor` Token Resolution diagnostics (Phase 2), and setup-skill / README / CLAUDE.md doc updates (Phase 3). Plan is dispatchable, technically grounded against verified codebase locations, and explicitly preserves the load-bearing 2026-02-13 anti-collision invariant. Recommend approval with no required iterations.

## Dimension Assessment

### 1. Completeness — PASS
Three phases declared with explicit per-phase Overview, Tasks, Phase Success Criteria (split into Automated and Manual), and "Creates for next phase" handoff notes. Group-level sections (Prior Work, Overview, Shared Constraints, Current State Analysis, Desired End State, Verification, What We're NOT Doing, Implementation Approach, Integration Testing, References) are all present. Each task has the required dispatchability table fields (files, tdd, complexity, depends_on, acceptance).

### 2. Feasibility — PASS
Verified against the live tree:
- `plugin/ralph-hero/mcp-server/src/index.ts:42-43` — repo-token resolution chain matches exactly (`resolveEnv("RALPH_GH_REPO_TOKEN") || resolveEnv("RALPH_HERO_GITHUB_TOKEN")`).
- `plugin/ralph-hero/mcp-server/src/index.ts:49-69` — startup error block boundaries match (the `if (!repoToken)` console.error + process.exit(1)).
- `plugin/ralph-hero/mcp-server/src/index.ts:103-105` — `repoTokenSource` ternary matches.
- `plugin/ralph-hero/mcp-server/src/__tests__/init-config.test.ts:218` — contract test (`should only accept RALPH-prefixed env vars`) confirmed at the cited line; `forbiddenVars` array confirmed.
- `plugin/ralph-hero/mcp-server/src/__tests__/init-config.test.ts:35-42` — `resolveTokens()` simulator at the cited lines.
- `plugin/ralph-hero/justfile:235-290` — Environment Variables section confirmed; line 290 is the close of the env-vars loop; line 292 is `--- Dependencies ---`. Insertion target verified.
- `plugin/ralph-hero/justfile:276` — `resolved_token="$val"` inside the `RALPH_HERO_GITHUB_TOKEN` arm of the loop, matches the plan's reuse claim.

The `null`-sentinel cache contract (`null = not-yet-resolved`, `undefined = resolved-to-failure`) is internally consistent and correctly noted as a deliberate distinction. The decision to `export resolveGhAuthToken` solely to enable `vi.spyOn(child_process, "execSync")` is sound — the alternatives (module-mock magic, whole-file re-import) are flakier. The 3000ms timeout + `stdio: ["ignore", "pipe", "ignore"]` combo is correct for the "unauthenticated gh" path.

### 3. Clarity — PASS
Success criteria use the `- [ ] Automated:` / `- [ ] Manual:` split per phase. Verification commands are concrete (`npx vitest run -t "should only accept RALPH"`, `bash -n plugin/ralph-hero/justfile`, `just --list`). Each task's acceptance criteria are framed as verifiable artifacts (file edits, test counts, exit codes), not abstract goals. Manual smoke tests in Task 2.3 cover the full source-precedence matrix (gh-only, env-only, dual-token, expired, missing-scope, no-token).

### 4. Scope — PASS
"What We're NOT Doing" enumerates seven explicit deferrals, including the load-bearing one ("Changing the contract test that forbids `GH_TOKEN`/`GITHUB_TOKEN`"). Scope-creep risks (multi-account `--user`, GitHub App auth, auto-rotation, `/ralph-hero:doctor` skill wrapper, fragment-extraction follow-ups) are individually called out. Phase 3 explicitly notes that the install-scope detection logic at the heart of the setup skill must remain intact (line 313: "preserved verbatim").

### 5. Dispatchability — PASS
All 14 tasks (1.1–1.7, 2.1–2.3, 3.1–3.4) carry the five required fields (files, tdd, complexity, depends_on, acceptance). Phase dependencies are explicit:
- Phase 1: `depends_on: null`
- Phase 2: `depends_on: [phase-1]`
- Phase 3: `depends_on: [phase-1]`
- Phase 2 and Phase 3 are independent (parallelizable).

Intra-phase task dependencies are also explicit (e.g., Task 1.6 `depends_on: [1.1, 1.5]`, Task 1.7 `depends_on: [1.6]`). Cross-phase handoff is documented via per-phase "Creates for next phase" footers (Phase 1 creates the resolution chain that Phase 2 detects and Phase 3 documents).

The Integration Testing section covers the cross-phase end-to-end behaviors (gh-only, env-precedence, split-token preserved, doctor accuracy, expired-token diagnosis, fresh-checkout walkthrough) — appropriate for a group plan.

## Issues
(none — APPROVED)

## Notes
- The plan correctly identifies that this is the first subprocess call inside the MCP server source tree (zero existing `execSync`/`spawn`/`child_process` hits) and treats that responsibility seriously.
- The Phase 2 `repo_source` ternary in Task 2.1's acceptance bullet 2 (`"RALPH_HERO_GITHUB_TOKEN${source_label:-}"`) correctly reuses the `source_label` variable already populated by the existing line-276 loop — a nice frugal reuse rather than re-deriving.
- Phase 3 Task 3.3's CLAUDE.md edit will require the implementer to find the right spot in this very file; the row to flip is the `RALPH_HERO_GITHUB_TOKEN` row in the env-var table (currently `**Yes**`).

## Approval Recommendation
**APPROVE.** Ready for implementation; dispatch sequence Phase 1 → (Phase 2 || Phase 3) is correct.
