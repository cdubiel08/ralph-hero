---
date: 2026-05-22
github_issue: 1301
github_url: https://github.com/cdubiel08/ralph-hero/issues/1301
plan_document: thoughts/shared/plans/2026-05-22-GH-1301-pr-merged-routine.md
status: approved
type: review
tags: [plan-review, routines, pr-merged, observability, idempotency]
---

# GH-1301 Plan Critique

Plan: `thoughts/shared/plans/2026-05-22-GH-1301-pr-merged-routine.md`
Mode: AUTO (ralph-review)

## Completeness — PASS

Single-phase S plan with four atomic tasks (1.1–1.4). Each task lists files (with create vs modify), tdd, complexity, depends_on, and a checkbox acceptance list. The Overview, Shared Constraints, Current State Analysis, Desired End State, What We're NOT Doing, Implementation Approach, Phase Success Criteria (split into Automated and Manual), Integration Testing, and References sections are all present. Skill body length is constrained (≤150 lines), file paths and exact frontmatter fields are specified.

## Feasibility — PASS

Spot-checked referenced files and patterns; all claims are accurate:

- `plugin/ralph-hero/skills/ralph-pr-drain/SKILL.md` exists and uses the exact pattern the plan adopts: `model: haiku`-like, `user-invocable: true`, `argument-hint: "--pr <PR-NUMBER>"`, SessionStart hook calling `set-skill-env.sh RALPH_COMMAND=pr-drain`, idempotency label check via `gh pr view ... --json labels`, MCP tools incl. `knowledge_record_outcome` in allowed-tools.
- `plugin/ralph-hero/skills/ralph-merge/SKILL.md` contains Step 7.5 (record outcome via `knowledge_record_outcome`) and Step 9c (`PushNotification`, best-effort) at the line numbers referenced.
- `plugin/ralph-hero/hooks/scripts/outcome-collector.sh` matchers (per `hooks/hooks.json`) are limited to `ralph_hero__save_issue` and `Write` — direct `knowledge_record_outcome` MCP calls do NOT fan out to this collector. Plan's claim in Current State Analysis line 52 is correct.
- `plugin/ralph-hero/scripts/monitoring-bridge/launchd/com.ralph.monitoring-bridge.plist.template` exists with the structure the plan claims to mirror (`/bin/bash -lc`, `StartInterval: 300`, `RunAtLoad: false`, EnvironmentVariables PATH, `/tmp/*.out|.err`).
- `plugin/ralph-hero/hooks/scripts/set-skill-env.sh` exists and accepts `KEY=VALUE` args writing to `CLAUDE_ENV_FILE`.
- `plugin/ralph-hero/skills/director/IOS-REMOTE.md` exists (cross-link target in Task 1.4).
- MCP `ralph_hero__get_issue` returns `closedAt` (verified in `mcp-server/src/tools/issue-tools.ts:611`), and `ralph_hero__save_issue` auto-closes the GitHub issue when `workflowState` resolves to a terminal state (`TERMINAL_STATES = ["Done","Canceled"]` in `workflow-states.ts:27`, auto-close logic in `issue-tools.ts:1272`). The idempotency guard (`closedAt within 60s` => skip save) is therefore implementable as specified.
- `outcome_events` schema (in `outcome-collector.sh:34-49`) has `issue_number INTEGER NOT NULL` — plan's `issue_number=0` sentinel for unlinked PRs is correct and required.

The previously-blocking concern from the triage comment (cloud Routines cannot install ralph-hero plugins from `settings.json`) is addressed by the plan's adopted approach: one-time UI install during Routine creation, with the live `ralph-hero-pr-drain` Routine (GH-1348) cited as proof. This is internally consistent across the plan, Shared Constraints, and the research doc.

## Clarity — PASS

Success criteria are split into Automated (concrete shell commands — `npm run build`, `bash -n`, `xmllint --noout`, `grep -E`) and Manual (specific reproducible scenarios with expected observable signals: `outcome_events` row count, `pr-merged-handled` label presence, `payload.skip_transition=true` for the double-fire case). Each task's `acceptance` list is checkbox-formatted with one verifiable claim per box. The 60s window, the `issue_number=0` sentinel, and the unconditional outcome-recording-on-skip are called out explicitly multiple times across Shared Constraints, Task 1.1 acceptance, and Integration Testing — eliminating ambiguity about what "idempotent" means here.

## Scope — PASS

"What We're NOT Doing" section is explicit with six bullets covering: not auto-creating the Routine, not modifying `ralph-merge`, not extending `outcome-collector.sh`, not adding autopilot continuation (deferred to parent Phase 6), not building the GitHub Actions relay (Approach C, rejected), and not collapsing the ntfy bridge (deferred to parent Phase 2.5). Each "not doing" item names the deferral target or the rejection rationale. Single-phase, single-PR boundary is stated up front.

## Dispatchability — PASS

Each of the four tasks could be handed to a fresh subagent without reading the rest of the plan:

- Task 1.1 (skill) — frontmatter requirements, all 9 workflow steps with exact tool calls, the regex patterns for issue resolution, the 60s guard mechanics, the result-marker format, and the 150-line ceiling are all inlined into the acceptance list.
- Task 1.2 (setup helper) — required stdout sections, the literal `RemoteTrigger(...)` payload shape, verification commands, and the "documentation only" constraint (no external command invocations) are inlined.
- Task 1.3 (launchd template) — the macOS+Linux-portable date one-liner, the exact `gh pr list` invocation with jq filter, the `|| true` wrapping, plist field names + values, and the `__INSTALL_PATH__` placeholder convention are all spelled out.
- Task 1.4 (docs/routines.md) — required section headings (`## ralph-hero-pr-drain`, `## ralph-hero-pr-merged`, `## ralph-hero-critical-alert`, `## Offline fallback (launchd)`, `## See also`), the items each section must cover, and the cross-link targets are inlined.

Inter-task dependencies are correctly declared: 1.3 depends on 1.1 (needs the skill to invoke), 1.4 depends on 1.1+1.2+1.3 (rolls them up). 1.1 and 1.2 are parallel-eligible.

## Verdict

**APPROVED.**

Routes to In Progress. No iteration required. The plan is internally consistent, technically grounded (all referenced files and patterns verified on disk), atomic (single S issue, single PR), and self-contained at the task level. The idempotency contract — the only non-trivial correctness concern — is fully specified with the unconditional-outcome-recording carve-out for downstream consumers documented twice.

Minor optional nits (not blocking):

- Task 1.1 acceptance line 116 (Step 6 `PushNotification`) says "When `ISSUE_NUMBER == 0`, uses `Merged PR #<PR-NUMBER>` as title" but doesn't state what `body` is in the unlinked-PR case. Reasonably inferred to be the same `<PR title> (<PR URL>)`. Implementer can decide.
- Task 1.3 references a `pr-merged-handled` label filter in the `gh pr list` jq expression as the dedup mechanism for the polling fallback. This is correct and matches the skill's Step 8 label-write — worth noting the polling fallback shares the dedup vocabulary with the skill, which is good design.
