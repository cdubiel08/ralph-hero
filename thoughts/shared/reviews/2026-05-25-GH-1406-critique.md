---
date: 2026-05-25
github_issue: 1406
github_url: https://github.com/cdubiel08/ralph-hero/issues/1406
type: review
status: complete
verdict: APPROVED
plan_doc: thoughts/shared/plans/2026-05-25-GH-1406-caretake-watch-pr-mode.md
---

# Review: GH-1406 — `/ralph:caretake --mode watch-pr`

## Plan Reference

[`thoughts/shared/plans/2026-05-25-GH-1406-caretake-watch-pr-mode.md`](../plans/2026-05-25-GH-1406-caretake-watch-pr-mode.md)

## Strengths

- **Sharp diagnosis of the spec/reality gap.** Verified independently: `grep -rn "Deferred Verdict" ralph/` returns zero hits; the actual WAIT-pr path writes only a `## Triage Decision` comment + `blocked:pr-NNN` label. The default-`PROMOTE-plan` resolution is sound — `## Triage Decision` encodes no machine-parseable successor verdict, so reading it would gain nothing.
- **Correctly concluded no new tool and no new hook are needed** (verified against SKILL.md allowed-tools + the hygiene.md no-Stop-hook precedent).
- **Phase 4 deferral is correct** — `loop-wrapper.md` has no `caretake:watch-pr` row; the issue defers heartbeat fan-out to later phases.
- **Clean two-phase split by file ownership** with a dependency that fixes token strings before they're referenced.

## Gaps

- **Verification quality — branch-skip token reuse.** Phase 1 §Step 2 reused `WATCH-PR IDLE` for the not-on-main short-circuit, conflating "no parked items" with "refused due to branch." Other modes use a distinct skip token. **Fixed at review time** → dedicated `WATCH-PR SKIPPED — branch <name> is not main`.
- **Verification quality — `ADVANCED <N>` count semantics.** The closed-unmerged escalation counting toward `<N>` was non-obvious. **Fixed at review time** → `<N>` documented as items *resolved this sweep* (merged→promoted + closed→escalated; open excluded).

## Failures

None.

## Recommended Changes

All three folded into the plan at review time (Approve-with-edits):
1. Dedicated `WATCH-PR SKIPPED — branch …` token (parity with triage/unblock).
2. `WATCH-PR ADVANCED <N>` count semantics documented (resolved = advanced + escalated; IDLE = zero items).
3. Manual-verification AC now cites the exact files confirming the `## Deferred Verdict` gap.

## Verdict

**Verdict**: APPROVED

**Confidence**: high — every load-bearing claim (allowed-tools, the missing `## Deferred Verdict` comment, the hygiene no-Stop-hook precedent, the loop-wrapper Phase 4 deferral, epic #1417 intent) verified directly against the repo.

**Score**:
- Phase clarity: PASS
- File ownership: PASS
- Verification quality: GAP (token-naming/count semantics — both fixed at review time)
- Risk coverage: PASS
- Plan-of-plans alignment: PASS
- Estimate sanity: PASS
- Linked research present: PASS

## Caveats

- `gh` was reachable; reviewer read issue #1406 and epic #1417 bodies directly — both matched the plan's paraphrase.
- The GAPs were documentation refinements, not correctness defects; core architecture (no new tool, no new hook, default-`PROMOTE-plan`) is correct as written.
