---
date: 2026-05-15
github_issue: 1265
github_url: https://github.com/cdubiel08/ralph-hero/issues/1265
plan_document: thoughts/shared/plans/2026-05-15-GH-1265-ralph-merge-agent-dispatch.md
status: approved
type: review
tags: [plan-review, ralph-merge, finish, agent-dispatch, model-tier, context-window, haiku]
---

# Plan Critique: GH-1265 Convert finish's ralph-merge dispatch from Skill() to Agent()

## Verdict

**APPROVED** — The plan is complete, technically accurate, and dispatchable. All claims about file paths, line numbers, and existing dispatch topology were verified against the codebase. No iteration needed.

## Dimension Scores

### Completeness — PASS
- Phase 1 is fully defined with four tasks (1.1–1.4), each enumerating files, complexity, dependencies, and acceptance criteria.
- "What We're NOT Doing" section explicitly rejects model upgrades, frontmatter changes to merge-agent, edits to ralph-merge/SKILL.md, hello/SKILL.md, MCP server code, and CLAUDE.md — well-bounded scope.
- "Desired End State" and "Verification" sections give concrete grep-able outcomes.

### Feasibility — PASS
- `finish/SKILL.md:260` confirmed as the only inline `Skill("ralph-hero:ralph-merge"` callsite via `grep -rn` — verified zero other matches across `plugin/ralph-hero/`.
- `merge-agent.md` exists with the expected `skills: [ralph-hero:ralph-merge]` preload and a tool allowlist that covers the full ralph-merge execution path (Read, Glob, Grep, Bash, AskUserQuestion, get_issue, list_issues, save_issue, create_comment, advance_issue, list_sub_issues, list_dependencies).
- `val-agent.md` line 4 confirmed as `model: haiku`, and `ralph-val/SKILL.md` line 6 confirmed as `model: sonnet` — the doc mismatch the plan claims is real.
- `hero/SKILL.md` Merge phase bullet confirmed at line 464, reading `**Merge phase**: \`Skill(ralph-merge)\` inline (called from \`finish\`)` — matches the plan's claim and edit target.
- `ralph-merge/SKILL.md` confirmed leaf (zero `Skill\|Agent` matches in body) — the depth-2 safety claim holds.
- `model-tier-policy.md` is 53 lines with a final `## Why not preemptive Opus?` section at line 46 — a new `## Context Window and Inline Skill Calls` section can be cleanly appended.

### Clarity — PASS
- Acceptance criteria use `- [ ]` checkbox format and reference specific line numbers, exact strings, and grep commands.
- Automated Verification block lists four runnable grep/test commands.
- Manual Verification block defines the smoke test (large-context session, observe no compaction).
- Output-string contract (`MERGED`, `MERGE BLOCKED`, `MERGE NOT READY`) is explicitly preserved — finish's downstream branch logic is unaffected.

### Scope — PASS
- Single phase, single issue, four small markdown edits across four well-known files.
- "What We're NOT Doing" preempts the common temptations (model upgrade per Option B in the issue, frontmatter restructuring, MCP changes).
- Doc cleanups (Tasks 1.2–1.4) are correctly co-located with the load-bearing fix (Task 1.1) since they explain and stabilize the same architectural decision.

### Dispatchability — PASS
- Each task carries: `files` (with create/modify annotation), `tdd: false`, `complexity: low`, `depends_on`, and an `acceptance` checklist.
- Task 1.4 correctly declares `depends_on: [1.1]` since the hero/SKILL.md commentary update must reflect the actual finish/SKILL.md change.
- A subagent could pick up any single task with zero additional context — exact file paths, current text, and target text are all enumerated.

## Notes for Implementation

- The depth-2 safety argument relies on `ralph-merge/SKILL.md` remaining a leaf. The plan locks this in via Shared Constraints; future PRs that re-add nested `Skill()` or `Agent()` calls to ralph-merge would re-introduce the original depth conflict that GH-895 resolved.
- The Task 1.3 model-tier-policy.md addition is a strong forward-looking regression guard. Recommend the new section explicitly cite GH-1265 as the canonical example so future authors can trace the rationale.

## References
- Plan: `thoughts/shared/plans/2026-05-15-GH-1265-ralph-merge-agent-dispatch.md`
- Research: `thoughts/shared/research/2026-05-15-GH-1265-haiku-context-compaction-integrator-agents.md`
- Issue: https://github.com/cdubiel08/ralph-hero/issues/1265
- Related dispatch resolution: https://github.com/cdubiel08/ralph-hero/issues/895
