---
date: 2026-05-30
github_issue: 1471
github_url: https://github.com/cdubiel08/ralph-hero/issues/1471
type: review
status: complete
verdict: NEEDS_ITERATION
plan_doc: thoughts/shared/plans/2026-05-30-GH-1471-save-issue-infer-terminal-state.md (DOES NOT EXIST)
---

# Review: save_issue — infer terminal workflowState from issueState on close (GH-1471)

## Plan Reference

Expected plan doc: `thoughts/shared/plans/2026-05-30-GH-1471-save-issue-infer-terminal-state.md`

**This file does not exist.** It is absent from:
- the working tree (`/Users/dubiel/projects/ralph-hero/thoughts/shared/plans/`),
- all git history in the ralph-hero repo (`git log --all -- 'thoughts/shared/plans/*1471*'` → empty).

The commit cited as carrying the plan, `c850e3a3`, is **not a valid git object** (`git cat-file -t c850e3a3` → `fatal: Not a valid object name c850e3a3`).

No `## Implementation Plan` artifact comment exists on issue #1471 either — the issue had zero comments before this review. There is therefore no pointer to any plan doc by any route.

The issue is at workflow state **Plan in Progress** (not "Plan in Review" as the task context stated), consistent with the plan never having been completed/posted.

## TWO blocking facts

This review surfaced two independent problems, either of which alone forces NEEDS_ITERATION:

### 1. There is no plan document to review (FAIL)

The artifact this review gates on does not exist (see Plan Reference). The engineering sketch in the issue body is not a `plan-shapes.md`-shaped plan doc — it has no `## Phase N`, no `## Automated Verification`, no `## What We're NOT Doing`, and was never crystallized to `thoughts/shared/plans/`. The plan-review procedure's Step 2 ("Validate plan exists — if absent, escalate") is triggered.

### 2. The feature the plan would design ALREADY EXISTS in the source (FAIL)

This is the more important finding. Reading the live `mcp-server/src/tools/issue-tools.ts`, the reverse-inference logic the issue proposes building is **already present** at lines 1280-1293:

```ts
// Reverse inference: if issueState closes the issue and no explicit workflowState,
// default the board to the matching terminal workflow state (Done or Canceled).
let inferredFromClose = false;
if (args.workflowState === undefined && targetState === "CLOSED") {
  const key = `CLOSED:${stateReason ?? ""}`;
  const inferred = ISSUE_STATE_TO_TERMINAL_WORKFLOW[key];
  if (inferred) {
    resolvedWorkflowState = inferred;
    inferredFromClose = true;
    changes.workflowStateInferred = inferred;
  }
}
```

This is functionally identical to the issue's proposed Change #1 — the precedence guard (`args.workflowState === undefined && targetState === "CLOSED"`) matches the issue's "explicit-wins" requirement exactly, and `changes.workflowStateInferred` matches the issue's proposed change-record key. A mapping table `ISSUE_STATE_TO_TERMINAL_WORKFLOW` (containing `CLOSED:COMPLETED → Done`, `CLOSED:NOT_PLANNED → Canceled`, `CLOSED: → Done` fallback — visible in `workflow-states.ts:157-159`) is already wired in. The issue's design notes named an inline literal; the implemented version factored it into a lookup table. The intent and the precedence semantics are the same.

In other words: **the work appears to already be done.** A planner should not write a plan for an already-implemented feature. The correct next step is to confirm the implementation's completeness (does it actually satisfy all 5 acceptance criteria? are the unit tests present?) and, if so, close #1471 as completed rather than planning it.

## Strengths (of the issue-body sketch)

- **Accurate code grounding.** Cited landmarks check out: `hasProjectFields` at `issue-tools.ts:1226`; `TERMINAL_STATES = ["Done","Canceled"]` and `WORKFLOW_STATE_TO_STATUS` exist in `workflow-states.ts`. The forward auto-close path is at `:1273-1277`, exactly as described.
- **Sound, precedence-correct approach** — and the codebase already adopted essentially this approach.
- **Concrete, testable acceptance criteria** (5 bullets).
- **Linked research is real**: `thoughts/shared/research/2026-05-30-ralph-triage-autonomy-gaps.md` (Gap B).

## Gaps (only relevant if the planner is asked to write a real plan rather than verify/close)

- **Verification quality (GAP).** No runnable Automated Verification block. Test file name is ambiguous: issue body says `save-issue.test.ts`; task context says `issue-tools.test.ts`. Must name the real file + give `cd mcp-server && npm run build && npx vitest run <file>`.
- **Helper-naming drift (GAP).** Three names float for the same helper: inline + test helper `inferWorkflowFromClose()` (issue body), vs `mapTerminalStateToWorkflow()` / `terminalStateForIssueState()` wired into `resolveWorkflowState()` (task context) — but the SHIPPED code uses neither: it uses the `ISSUE_STATE_TO_TERMINAL_WORKFLOW` table inline. Any plan must reconcile against what is already in the tree.
- **Test-count traceability (GAP).** "3+ unit tests" bar is asserted but never mapped to acceptance bullets.

## Failures

- **FAIL — no plan doc exists** (see TWO blocking facts §1).
- **FAIL — feature already implemented in `issue-tools.ts:1280-1293`** (see §2). Planning an already-shipped change is wasted work and risks a regression-by-rewrite.

## Recommended Changes

The right next action is most likely **verify-and-close**, not re-plan. Specifically:

1. **Verify the existing implementation against the 5 acceptance criteria.** Run the test suite: `cd mcp-server && npm run build && npx vitest run`. Confirm there are tests covering: CLOSED→Done, CLOSED_NOT_PLANNED→Canceled, explicit-workflowState-wins, OPEN/metadata-only unaffected, lock-guard untouched. Check whether the board-update guard (`if (hasProjectFields)` at ~`:1428`) was extended to fire when `inferredFromClose` is true — if the guard was NOT broadened, the inferred `resolvedWorkflowState` may be computed but never written to the board, which would be a real bug to fix (this is the one part I could not fully confirm by reading and is the highest-value thing for the next pass to check).
2. **If the implementation is complete and tested** → close #1471 as completed; do not write a plan.
3. **If a real gap remains** (e.g. the guard was not broadened, or tests are missing) → write a narrow plan for ONLY the remaining delta, following `plan-shapes.md`, naming the real test file and exact verification commands, and explicitly noting that the core inference block already exists so impl does not duplicate it.

## Verdict

**Verdict**: NEEDS_ITERATION

**Confidence**: high — both blocking facts are directly verifiable: the plan artifact's absence (and invalid commit ref) is checkable, and the reverse-inference block is present and readable in the live source at `:1280-1293`.

**Score**:
- Phase clarity: FAIL (no plan doc; no phases exist)
- File ownership: GAP (sketch names real files accurately, but no plan doc owns them)
- Verification quality: GAP (no runnable Automated Verification block; test file name ambiguous)
- Risk coverage: GAP (lock-guard + explicit-wins noted, but no `## What We're NOT Doing`; bigger uncovered risk: re-planning already-shipped code)
- Plan-of-plans alignment: n/a (standalone P0, not a child)
- Estimate sanity: PASS (S matches the scope)
- Linked research present: PASS (research doc exists and is referenced)

Two FAILs → NEEDS_ITERATION.

## Caveats

- The Read/Bash harness returned empty output intermittently during this session. I confirmed the reverse-inference block (`:1280-1293`), the forward auto-close path (`:1273-1277`), `hasProjectFields` (`:1226`), and the `workflow-states.ts` constants via multiple independent successful reads. I could **not** reliably confirm whether the board-update guard at ~`:1428` (`if (hasProjectFields)`) was broadened to `if (hasProjectFields || inferredFromClose)` — this is the single most important thing for the next pass to verify, because without it the inferred state is computed but may never be persisted to the board (which would mean the bug is only half-fixed). I also could not confirm whether `inferredFromClose` / `ISSUE_STATE_TO_TERMINAL_WORKFLOW` are committed at HEAD vs. uncommitted working-tree changes.
- The task context stated the plan was on main at commit `c850e3a3` and the issue was at "Plan in Review"; both were found incorrect (no such commit/file; issue at Plan in Progress). This critique reflects actual repository state.
- The `ralph_review` command does not accept `Plan in Progress` as an output state (valid: In Progress / Ready for Plan / Human Needed), so the standard NEEDS_ITERATION → Plan in Progress transition could not be applied via the `command:"review"` path. The issue was left at Plan in Progress (its existing state) and the critique posted as a comment.
