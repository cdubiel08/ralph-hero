---
date: 2026-05-08
github_issue: 1142
github_url: https://github.com/cdubiel08/ralph-hero/issues/1142
plan_document: thoughts/shared/plans/2026-05-08-GH-1142-ralph-hero-unblock-skill.md
status: approved
type: review
tags: [plan-review, skills, escalation, human-needed, unblock, async-loop, state-machine]
---

# GH-1142 Plan Critique v2 — ralph-hero:unblock skill pair (revised)

## Verdict

**APPROVED** — all four issues raised in the v1 critique have been addressed with surgical edits. Plan is complete, feasible, clear, well-scoped, and dispatchable. Ready for implementation.

## Summary of Plan

Adds a paired skill set to bridge `Human Needed` issues back into the pipeline: `ralph-hero:ralph-unblock` (autonomous; posts blocking questions as `## Unblock Request` comments) and `ralph-hero:unblock` (interactive; captures answers via `AskUserQuestion`, posts `## Unblock Resolution`, transitions issue to one of four valid re-entry states). 4 phases: foundation (state graph + protocol headers), autonomous skill, interactive skill, surfacing/scheduling/docs. Estimate: M.

## Resolution of v1 Issues

| v1 Issue | Resolution Status |
|---|---|
| 1. Phase 1.5 structurally incorrect (`outcome-events.ts` doesn't exist; no event-type allowlist) | RESOLVED — Phase 1.5 reframed as "no code change required" with payload-schema documentation only. Migration Notes updated. Verified against `plugin/ralph-knowledge/src/db.ts:130` (free-form TEXT NOT NULL) and `index.ts:351` (z.string()). |
| 2. Phase 4.1 wrong file path | RESOLVED — Now cites `plugin/ralph-hero/mcp-server/src/tools/directions-tools.ts` with line 280 anchor. Verified against actual file. |
| 3. Phase 4.1 underspecified comment-derived signals | RESOLVED — Added explicit 4-step procedure for `unblockRequestAgeDays`/`questionCount` derivation (per-issue `get_issue` fetch, header match, age compute, idempotency carve-out for newer `## Escalation`). Includes acceptance criterion for unit test with two fixture issues. |
| 4. Phase 1.4 stale line numbers | RESOLVED — Diff snippet no longer cites a problematic range; the snippet is unambiguous on its own and shows insertion point relative to existing code. |

---

## Dimension Assessments

### 1. Completeness — PASS

All four phases define specific file changes with concrete contents. Each phase has explicit Success Criteria split into Automated/Manual sections. Implementation Approach, Out of Scope, Migration Notes, Performance Considerations, Testing Strategy, and References all present.

### 2. Feasibility — PASS

All technical claims verified against the codebase:
- `plugin/ralph-hero/mcp-server/src/tools/directions-tools.ts` exists (line 280 registers `ralph_hero__next_actions`)
- `plugin/ralph-knowledge/src/db.ts:130` confirms `event_type TEXT NOT NULL` (no enum)
- `plugin/ralph-knowledge/src/index.ts:351` confirms `z.string()` (no union to extend)
- `plugin/ralph-hero/mcp-server/src/lib/state-resolution.ts:36-54` defines `COMMAND_ALLOWED_STATES` (matches plan citation)
- `plugin/ralph-hero/hooks/scripts/ralph-state-machine.json` Human Needed entry has `required_by_commands: []` (matches plan)
- `plugin/ralph-hero/hooks/scripts/human-needed-outbound-block.sh` early-allow insertion point at lines 27-30 (command check) is correctly identified
- `plugin/ralph-hero/skills/shared/artifact-comment-protocol.md` exists with the table structure the plan extends
- `plugin/ralph-hero/mcp-server/src/lib/filter-profiles.ts` exists (the plan's "locate during implementation" hedge is unnecessary but harmless)
- `plugin/ralph-hero/skills/ralph-triage/` reference template confirmed (has `SKILL.md` + `eval-scenarios.md`)

### 3. Clarity — PASS

Success criteria are specific and testable; every Phase has both `- [ ] Automated:` and `- [ ] Manual:` checkboxes. Test commands are concrete (`npx vitest run src/__tests__/state-resolution.test.ts`, `plutil -lint ...`, `shellcheck ...`). Each phase ends with an "Implementation Note" pause for manual confirmation.

### 4. Scope — PASS

"What We're NOT Doing" lists six explicit non-goals with reasoning:
1. Environment-access channel (deferred to Phase 2)
2. Auto-firing from hero
3. Modifying every escalating skill
4. Question-mode for originator skills
5. Slack / PagerDuty / external notification routing
6. New "Plan in Review" return path

Boundaries between Phase 1 (foundation), Phase 2/3 (skills), and Phase 4 (polish) are crisp.

### 5. Dispatchability — PASS

Each phase is dispatchable to a builder agent without needing to read other phases:
- **Phase 1**: 5 sub-changes, each with file path + diff content + verification command. The TS-vs-JSON drift dependency is called out explicitly.
- **Phase 2**: Full skill frontmatter, full postcondition hook script, full agent definition, eval scenarios listed.
- **Phase 3**: Full skill frontmatter, full state-gate hook script, full eval scenarios.
- **Phase 4**: 4 sub-tasks (next_actions ranker, launchd template, doc updates, optional filter profile). The previously-flagged `unblockRequestAgeDays` signal computation now has a 4-step procedure with acceptance criterion — implementor will not be surprised mid-task.

Phase dependencies are explicit:
- Phase 1 wires the state machine before any skill exists
- Phase 2 requires Phase 1's outbound-block hook extension
- Phase 3 requires Phase 1's state-machine entries + Phase 2's skill template patterns
- Phase 4 requires Phase 2 + 3 to be testable end-to-end

Cross-phase integration testing is covered in the "Testing Strategy" section ("Key end-to-end happy path: escalate → autonomous unblock posts questions → interactive unblock answers and routes → next pipeline run picks it up").

---

## Strengths Worth Preserving

- "Current State Analysis" with file paths + line numbers makes design decisions auditable.
- The originating-command → return-state heuristic table (Phase 3.4) is exemplary UX detail.
- Idempotency via `## Unblock Request` after `## Escalation` is the right design choice; called out as a "Key Discovery".
- Phase 1.1 explicitly explains *why* `Human Needed` is in `COMMAND_ALLOWED_STATES` for `ralph_unblock` (no-op saves).
- The "Desired End State" walks the full happy path in 5 numbered steps with verification commands.
- Phase 1.5's reframing as "no code change required" with explicit canonical payload shapes is the cleaner choice than a doc-only edit — both skills will emit identical JSON without forcing a ralph-knowledge file edit.
- Phase 4.1's idempotency carve-out (`## Escalation` newer than `## Unblock Request` skips the direction) prevents a subtle data race surfacing the wrong signal.

---

## Recommendation

Approve and proceed to `In Progress`. The plan is implementation-ready.
