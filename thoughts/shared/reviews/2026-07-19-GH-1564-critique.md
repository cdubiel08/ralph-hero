---
date: 2026-07-19
github_issue: 1564
github_url: https://github.com/cdubiel08/ralph-hero/issues/1564
type: review
status: complete
verdict: APPROVED
decisions_open: 0
plan_doc: thoughts/shared/plans/2026-07-19-GH-1564-migrate-get-project-root-hooks.md
---

# Review: Migrate remaining get_project_root artifact-lookup hooks

## Plan Reference

`thoughts/shared/plans/2026-07-19-GH-1564-migrate-get-project-root-hooks.md`

## Strengths

- Every source citation is accurate: `impl-plan-required.sh` lines 22/43/72-73, `review-no-dup.sh` lines 14/26, `hook-utils.sh` 43-45 (`get_project_root`) and 55-68 (`resolve_root_from_path`), and both CI job line ranges (test-hooks 112-127, shellcheck-hooks 263-274) all match verbatim.
- Test-case designs correctly model the actual hook gating: Phase 1 rightly requires `RALPH_COMMAND=impl`, an explicit `RALPH_TICKET_ID`, a code `file_path` that dodges the early-allow, and `RALPH_PLAN_REFERENCE` for the second call site; Phase 2 correctly identifies both early-allow dimensions.
- The `artifact-write-tracker.sh`/`drift-tracker.sh` scope-exclusion claim holds up against source — both use `get_project_root()` to normalize `file_path` (prepend to a relative path, or strip as a prefix from an absolute path), not to resolve-root-then-search; the distinction is real and correctly drawn.
- Test cases are logically sound regression proofs — the workspace-root repro cases genuinely reproduce the pre-migration false-block/false-allow, with companion cases guarding against trivially-always-pass regressions.

## Gaps

None

## Failures

None

## Recommended Changes

None

## Verdict

**Verdict**: APPROVED

**Confidence**: high — every line citation, the hook gating logic, the env-var contract, the CI invocations, and both prior-work docs were verified directly against source and all matched.

**Score**:
- Phase clarity: PASS
- File ownership: PASS
- Verification quality: PASS
- Risk coverage: PASS
- Plan-of-plans alignment: n/a
- Estimate sanity: PASS
- Linked research present: PASS
- Decision honesty: PASS

## Caveats

- Phase 1 case 4 relies on the general "every `run_case` needs `RALPH_TICKET_ID`" rule stated once rather than restated per-case — an implementation detail, not a gap.
- Clean APPROVED, decision-free — no `PLAN AWAITING DECISION` hold applies.
