---
date: 2026-05-09
github_issue: 1121
github_url: https://github.com/cdubiel08/ralph-hero/issues/1121
plan_document: thoughts/shared/plans/2026-05-09-GH-1121-mcp-server-lib-unit-tests.md
status: approved
type: review
tags: [plan-review, testing, mcp-server, vitest, coverage]
---

# Plan Critique: GH-1121 — Direct Unit Tests for Untested mcp-server Lib Modules

## Summary

**Verdict: APPROVED**

Single-phase plan adding three vitest files covering `rate-limiter.ts`, `group-detection.ts`, and `dashboard-fetch.ts`. The plan is concrete, dispatch-ready, and grounded in existing test patterns in the repository. No issues identified that warrant iteration.

## Dimension Assessments

### Completeness — PASS

Phase 1 enumerates three tasks with file paths (with create/read annotations), tdd flag, complexity, depends_on, and itemized acceptance criteria. The "Current State Analysis" section quantifies LOC and identifies the public surface for each target module.

### Feasibility — PASS

All referenced files verified to exist on disk:
- Target source modules: `lib/rate-limiter.ts`, `lib/group-detection.ts`, `lib/dashboard-fetch.ts` — present.
- Pattern references: `__tests__/cache.test.ts`, `github-client.test.ts`, `dashboard.test.ts`, `hygiene.test.ts` — all present.
- Coverage tooling (`@vitest/coverage-v8`) noted as already landed via parent epic Phase 2 (GH-1120 closed).
- ESM `.js` import suffix constraint correctly stated.

### Clarity — PASS

Acceptance criteria use the `- [ ]` checkbox format throughout. Criteria specify concrete, testable behaviors with sample values (e.g., "remaining=75 with defaults", "60_000 ms cap", "two project IDs"). Phase Success Criteria split into Automated and Manual sections per template. Manual verification includes a behavior-mutation sanity check.

### Scope — PASS

Explicit "What We're NOT Doing" section bounds the work:
- No refactors of code under test.
- No GH-1122 / GH-1123 work.
- No replacement of existing indirect coverage.
- No shared test helpers.

### Dispatchability — PASS

Each of the three tasks (1.1, 1.2, 1.3) is self-contained with all required fields:

| Task | files | tdd | complexity | depends_on | acceptance |
|------|-------|-----|------------|------------|------------|
| 1.1  | yes (with create/read tags) | true | low | null | 8 verifiable bullets |
| 1.2  | yes (with create/read tags) | true | high | null | 9 verifiable bullets |
| 1.3  | yes (with create/read tags) | true | medium | null | 11 verifiable bullets |

Tasks share no files and can be dispatched in parallel. No task requires reading the full plan to understand its scope.

## Minor Observations (non-blocking)

- Coverage threshold checks (≥80% lines per module) are stated as both per-task acceptance criteria and Phase Success Criteria — this redundancy is fine; it makes per-task dispatch self-contained.
- Task 1.2 marks complexity as `high` for `group-detection.ts` (650 LOC) which is appropriate; the implementer should expect to spend the most time here.

## Recommendation

Move to **In Progress**. Plan is implementation-ready.
