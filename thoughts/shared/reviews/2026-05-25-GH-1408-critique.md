---
date: 2026-05-25
github_issue: 1408
github_url: https://github.com/cdubiel08/ralph-hero/issues/1408
type: review
status: complete
verdict: APPROVED
plan_doc: thoughts/shared/plans/2026-05-25-GH-1408-caretake-heartbeat-watch-fanout.md
---

# Review: GH-1408 — caretake heartbeat watch fan-out

## Plan Reference

[`thoughts/shared/plans/2026-05-25-GH-1408-caretake-heartbeat-watch-fanout.md`](../plans/2026-05-25-GH-1408-caretake-heartbeat-watch-fanout.md)

## Strengths

- One phase, one file, exact before/after matching the issue body character-for-character (hygiene → watch-pr → watch-upstream → report → trends). Current 3-mode list verified at SKILL.md:143-147; both watch modes verified present (`modes/watch-pr.md`, `modes/watch-upstream.md`).
- Ordering rationale correct: watch modes before report/trends means the dashboards reflect post-watch board state within the same tick.
- Risk framing sound (additive; watch modes are owner-scoped no-ops). XS estimate correct.

## Gaps

- **Stale mode-table row** (risk/consistency): SKILL.md line 94's `**all**` row describes the fan-out as "hygiene + catch-up report + trends" — would contradict the new 5-mode Step 1 list. **Fixed at review time** → Phase 1 now includes updating that row.
- **Off-main SKIPPED path** (risk coverage): the watch modes' branch gate emits `SKIPPED` (not `IDLE`) when the heartbeat fires off main. **Fixed at review time** → documented in Migration Notes.

## Failures

None.

## Recommended Changes

All folded in at review time (Approve-with-edits):
1. Phase 1 now updates the `**all**` mode-table row (SKILL.md:94) to list all 5 fan-out modes + a verification check for it.
2. Migration Notes acknowledge the off-main `SKIPPED` path alongside `IDLE`.
3. Migration Notes note the issue body's stale "30-min" claim (actual heartbeat default is 1h; not changed by this edit).

## Verdict

**Verdict**: APPROVED

**Confidence**: high — every load-bearing claim (3-mode current list, mode existence, target order vs issue body, additive risk) verified directly against main; the findings were minor doc-consistency items.

**Score**:
- Phase clarity: PASS
- File ownership: PASS
- Verification quality: PASS
- Risk coverage: GAP (off-main SKIPPED + stale table row — both fixed at review time)
- Plan-of-plans alignment: PASS
- Estimate sanity: PASS
- Linked research present: n/a

## Caveats

- The issue body's "30-min loop" contradicts the actual 1h `--mode all` default; the plan correctly sided with the code. Issue-body error, not a plan error.
- Plan correctly targets the slim `ralph/` plugin, not `plugin/ralph-hero/` (parallel-period convention).
