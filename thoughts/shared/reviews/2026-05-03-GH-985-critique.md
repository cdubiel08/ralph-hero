---
date: 2026-05-03
github_issue: 985
github_url: https://github.com/cdubiel08/ralph-hero/issues/985
plan_document: thoughts/shared/plans/2026-05-03-GH-0985-catch-up-cursor-hook.md
status: approved
type: review
tags: [plan-review, catch-up, hooks, cursor, refactor]
---

# Plan Critique: GH-985 — replace catch-up LLM Write with PostToolUse hook

## Verdict

APPROVED — ready for implementation.

## Dimension Assessment

### Completeness — PASS

Two phases with four atomic tasks total. Each task identifies the exact file, the create/modify intent, the code patterns to follow (with line-range citations), and the specific AC checkpoints to satisfy. Phase 1 covers the script and its tests; Phase 2 wires it in and removes the LLM-driven Write. All five issue AC items map to plan tasks.

### Feasibility — PASS

All referenced files were verified to exist on disk at the cited paths and line ranges:
- `plugin/ralph-hero/hooks/scripts/outcome-collector.sh` (pattern source for `INPUT=$(cat)` + jq extraction + best-effort exit 0)
- `plugin/ralph-hero/hooks/scripts/__tests__/record-activity.test.sh` (harness pattern: `mktemp -d` + trap, `assert_eq`, `assert_file_exists`, PASS/FAIL counters, `[ "$FAIL" -eq 0 ]` terminator)
- `plugin/ralph-hero/hooks/hooks.json` (PostToolUse array structure at lines 107-168 confirmed)
- `plugin/ralph-hero/skills/catch-up/SKILL.md` (line 10-13 allowed-tools and 59-67 Step 5 confirmed)

The proposed `RALPH_CURSOR_DIR` env-var override pattern matches the existing `RALPH_ACTIVITY_DIR` / `RALPH_KNOWLEDGE_DB` patterns used by sibling hook scripts.

### Clarity — PASS

Phase Success Criteria use explicit `Automated:` / `Manual:` checkboxes with runnable commands (`bash plugin/ralph-hero/hooks/scripts/__tests__/cursor-advance-catch-up.test.sh`, `jq . plugin/ralph-hero/hooks/hooks.json`, `grep -c "^## Step "`). Each test case in Task 1.2 includes the literal stdin payload and the expected assertions on cursor file presence/absence and exit code.

### Scope — PASS

"What We're NOT Doing" section enumerates 7 explicit exclusions: cursor file path/shape, MCP response shape, activity log writer, skill Step 1, PostSkillInvoke mechanism, new cursor-management abstractions, and other skills' state files. Boundaries are tight and aligned with issue scope.

### Dispatchability — PASS

All four tasks declare `files`, `tdd`, `complexity`, `depends_on`, and `acceptance`. Task IDs are sequenced (1.1 → 1.2, 1.1 → 2.1, independent 2.2). Each task contains enough internal context (line citations, code idiom references, patterns to mirror) for a subagent to implement without reading the full plan.

## Specific Verifications Requested by Reviewer

- **Hook matcher format**: Plan correctly uses bare server-side name `ralph_hero__recent_activity`, matching the convention in `hooks.json:37, 54, 67, 109, 122, 131`. The Shared Constraints section explicitly calls this out, distinguishing it from the `mcp__plugin_...` prefix used in skill `allowed-tools` lists.
- **Phases are atomic**: Phase 1 is testable in isolation (script + tests run without touching the registration or skill). Phase 2 is config-and-prose that's verified by JSON parse, grep checks, and manual /hello.
- **File ownership clear**: Each file appears in exactly one task's `files` array; no overlap.
- **Tests cover all AC cases**: Issue AC test list (non-null cursor written, null cursor skipped, missing parent dir auto-created, malformed stdin doesn't crash) maps 1:1 to Task 1.2 Test cases 1-4, plus a bonus case for the entirely-missing field.
- **No scope creep**: Plan stays inside issue AC; does not refactor adjacent hooks, MCP server, or activity log writer.

## Minor Observations (non-blocking)

- Task 2.1 ordering ("Inserted before the matcher-less `record-activity.sh tool_called` entry") is acknowledged as best-effort by the plan itself. Acceptable.
- The plan correctly notes that matching on `ralph_hero__recent_activity` will fire the hook for any caller (not only the catch-up skill). This is treated as an acceptable behavior expansion per the research doc's "successful synthesis gate is not load-bearing" finding, and is explicitly tested in Integration Testing.

## Recommendation

Proceed with implementation. The plan is well-grounded in existing codebase patterns, all referenced files and line ranges are accurate, the AC mapping is complete, and the test coverage matches the issue's stated requirements.
