---
date: 2026-05-09
github_issue: 1123
github_url: https://github.com/cdubiel08/ralph-hero/issues/1123
plan_document: thoughts/shared/plans/2026-05-09-GH-1123-hook-gate-tests-impl-pr-merge.md
status: approved
type: review
tags: [plan-review, testing, hooks, ci, bash]
---

# Plan Critique: GH-1123 Hook Gate Tests for impl/pr/merge State Machine

## Summary

**Verdict: APPROVED**

Single-phase, single-issue (S) plan to add 8 `.test.sh` files cloning the established `val-postcondition.test.sh` pattern. Scope is tight, dispatchability is high, and Phase 1 CI wiring (#1119) is already landed so the tests will execute on merge.

## Dimension Assessment

### Completeness — PASS

All 8 task entries (1.1–1.8) are defined with target file paths, plus a Task 1.9 sweep/regression check. Plan includes Overview, Shared Constraints, Current State Analysis, Desired End State, Verification, What We're NOT Doing, Implementation Approach, Phase Success Criteria, and Integration Testing.

### Feasibility — PASS

- Pattern reference `plugin/ralph-hero/hooks/scripts/__tests__/val-postcondition.test.sh:1-65` exists in the repo and matches the described shape.
- All 8 hook scripts under test exist at the cited paths.
- `ralph-state-machine.json` (needed for `lock-claim-validator`) exists at the cited co-located path.
- `hook-utils.sh::read_input` stdin contract matches the test approach (pipe JSON per invocation).

### Clarity — PASS

Every task has explicit checkbox acceptance criteria specifying env vars, JSON input shape, and expected exit code (0 or 2). Phase Success Criteria are split into Automated and Manual buckets, both runnable.

### Scope — PASS

"What We're NOT Doing" enumerates 5 explicit exclusions (hook-script edits, helper-module factoring, the other ~60 hooks, CI wiring, bats rewrite). Out-of-scope boundaries are well-bounded.

### Dispatchability — PASS

Per-task fields verified:

| Task | files | tdd | complexity | depends_on | acceptance |
|------|-------|-----|------------|------------|------------|
| 1.1  | yes   | yes | low        | null       | yes        |
| 1.2  | yes   | yes | low        | [1.1]      | yes        |
| 1.3  | yes   | yes | low        | [1.1]      | yes        |
| 1.4  | yes   | yes | medium     | [1.1]      | yes        |
| 1.5  | yes   | yes | medium     | [1.1]      | yes        |
| 1.6  | yes   | yes | low        | [1.1]      | yes        |
| 1.7  | yes   | yes | low        | [1.1]      | yes        |
| 1.8  | yes   | yes | medium     | [1.1]      | yes        |
| 1.9  | yes   | yes | low        | [1.1–1.8]  | yes        |

Tasks 1.2–1.8 form a clean fan-out from the canonical template (1.1), terminating in a sweep (1.9). Each task is self-contained for subagent dispatch.

## Minor Notes (non-blocking)

- Task 1.4 Case C says "asserts the documented exit code from the script (read script body to confirm whether this path is exit 2)" — slight ambiguity, but the instruction to read the script body before asserting is acceptable for a medium-complexity task.
- `pr-state-gate.sh` alternate-key (`targetState`) coverage is explicitly called out in Task 1.6 Case B — good catch from research.

## Recommendation

Approve. Ready for implementation via `/ralph-impl 1123`.
