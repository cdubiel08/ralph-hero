---
date: 2026-05-07
github_issue: 1129
github_url: https://github.com/cdubiel08/ralph-hero/issues/1129
plan_document: thoughts/shared/plans/2026-05-07-GH-1129-list-issues-totalcount-misleading.md
status: approved
type: review
tags: [plan-review, list-issues, mcp-tool, response-shape, bug-fix]
---

# GH-1129 Plan Critique

**Plan**: [2026-05-07-GH-1129-list-issues-totalcount-misleading.md](https://github.com/cdubiel08/ralph-hero/blob/main/thoughts/shared/plans/2026-05-07-GH-1129-list-issues-totalcount-misleading.md)
**Verdict**: APPROVED

## Summary

Single-phase XS plan to remove the misleading `totalCount` field from the `ralph_hero__list_issues` MCP tool response (Option 1 from research) and add a structural regression test. Plan is tightly scoped, technically verified, and ready for implementation.

## Dimension Scoring

### 1. Completeness — PASS

- Single phase, two tasks (source change + regression test).
- All `files`, `tdd`, `complexity`, `depends_on`, `acceptance` fields present on both tasks.
- Phase has explicit Automated and Manual verification sections.
- Integration testing section addresses live-call sanity check.

### 2. Feasibility — PASS

Verified against the actual codebase:

- `plugin/ralph-hero/mcp-server/src/tools/issue-tools.ts:496` — confirmed contains exactly `totalCount: itemsResult.totalCount,`. The surrounding `toolSuccess` block matches the plan's quoted snippet verbatim.
- `plugin/ralph-hero/mcp-server/src/tools/issue-tools.ts:215-221` — confirmed `paginateConnection` call selects `totalCount` on the project items connection, exactly as described.
- `plugin/ralph-hero/mcp-server/src/__tests__/issue-tools.test.ts` — confirmed loads `issueToolsSrc = fs.readFileSync(...)` at top of file. The proposed structural assertions follow the file's established pattern (existing `expect(issueToolsSrc).toContain(...)` style).
- Research doc exists at the referenced path; the plan correctly cites it as prior work.

### 3. Clarity — PASS

Acceptance criteria are specific and testable:
- Negative substring assertion: `totalCount: itemsResult.totalCount` absent.
- Positive substring assertion: `filteredCount: formattedItems.length` present.
- Build/test pass gates explicit.
- Manual verification calls out comparing against `gh issue list` for parity.

### 4. Scope — PASS

"What We're NOT Doing" is explicit and well-bounded:
- Pagination helper preserved (correctly identifies cross-tool dependency in `project-tools.ts`).
- GraphQL selection-set untouched (cosmetic optimization deferred).
- Options 2/3 from research explicitly rejected with reasoning.
- No deprecation period (justified: no internal consumers).
- Tool description string left as-is (correctly noted it does not mention `totalCount`).

### 5. Dispatchability — PASS

Both tasks self-contained with all required fields. Task 1.2 explicitly `depends_on: [1.1]` (test asserts on the source change, so order matters). A subagent could pick up either task with zero additional context.

## Notes

- Auto-release awareness is called out in Shared Constraints — this response-shape change will trigger a patch bump on merge to `main`. Documenting this in the commit message is good practice and the plan flags it.
- `tdd: false` is appropriate here: the change is a one-line deletion and the regression test is a structural source-file assertion, not a behavioral test that could be written first.
- No internal callers were found to consume `list_issues`'s `totalCount`, per the research doc — confirming this is a low-risk removal.

## Recommendation

Approve and dispatch to `ralph-impl`.
