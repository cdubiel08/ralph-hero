---
date: 2026-05-12
github_issue: 1130
github_url: https://github.com/cdubiel08/ralph-hero/issues/1130
plan_document: thoughts/shared/plans/2026-05-12-GH-1130-mcp-boolean-coercion.md
status: approved
type: review
tags: [plan-review, mcp-server, zod, type-coercion, bug-fix]
---

# Plan Critique: GH-1130 MCP Boolean Param Coercion (Fresh Re-Review)

## Verdict: APPROVED

Single-phase atomic XS plan. All five quality dimensions PASS. This re-review
re-verifies the live tree against the plan's claims and confirms the prior
critique (`2026-05-12-GH-1130-critique.md`) still holds. The plan is
dispatchable to a subagent without additional context.

## Dimension Assessment

### Completeness — PASS

Phase 1 defines five concrete tasks (1.1–1.5), each with explicit file paths,
TDD flag, complexity, dependency edges, and a checkbox acceptance list. The
inventory table in `Current State Analysis` enumerates the 15 `z.boolean()` /
`.boolean()` occurrences across 10 tool files — verified against live grep
output:

```
plugin/ralph-hero/mcp-server/src/tools/debug-tools.ts:267
plugin/ralph-hero/mcp-server/src/tools/decompose-tools.ts:197
plugin/ralph-hero/mcp-server/src/tools/project-management-tools.ts:577, 591
plugin/ralph-hero/mcp-server/src/tools/dashboard-tools.ts:71, 101
plugin/ralph-hero/mcp-server/src/tools/batch-tools.ts:262
plugin/ralph-hero/mcp-server/src/tools/relationship-tools.ts:175, 1085
plugin/ralph-hero/mcp-server/src/tools/activity-tools.ts:23
plugin/ralph-hero/mcp-server/src/tools/issue-tools.ts:524, 531, 1215
plugin/ralph-hero/mcp-server/src/tools/plan-graph-tools.ts:91
plugin/ralph-hero/mcp-server/src/tools/project-tools.ts:186
```

Closed worklist, no ambiguity for the implementer.

### Feasibility — PASS

Re-verified live tree:

- `plugin/ralph-hero/mcp-server/src/tools/` contains all 10 referenced files.
- `plugin/ralph-hero/mcp-server/src/lib/` exists (target dir for new
  `zod-helpers.ts`).
- `plugin/ralph-hero/mcp-server/src/__tests__/empty-params.test.ts` exists
  as the cited template for the new regression test.
- `grep -rn "z\.boolean\|\.boolean()" plugin/ralph-hero/mcp-server/src/tools/`
  returns exactly **15** hits, matching the plan's table.
- `validateToolInput` patch confirmed at `src/index.ts:472-474` (variable
  binding at 472, assignment at 473-474 — plan citation accurate).
- `z.coerce.number()` precedent confirmed at `tools/project-management-tools.ts:49`
  (and 4+ other call sites).

The `z.preprocess` mitigation around `Boolean("false") === true` is verified
live in Node REPL: `Boolean("false")` returns `true` in JavaScript. The
chosen `z.preprocess` shim (mapping only the literal `"true"`/`"false"` to
booleans) is the idiomatic, safe fix. The plan explicitly rejects naive
`z.coerce.boolean()` for this exact reason — correctly reasoned.

### Clarity — PASS

Every task has a checkbox acceptance list with verifiable predicates:

- Task 1.1: `.parse()` results enumerated for both happy-path and rejection
  cases (`zBoolish().parse("yes")` throws; `zBoolish().parse(1)` throws).
- Task 1.2: Regression test acceptance enumerates the exact wire-shape
  patterns to exercise.
- Tasks 1.3–1.4: `grep` returns 0 as the migration-completeness predicate.
- Task 1.5: `npm run build` clean + `npm test` clean.

Phase Success Criteria splits Automated vs. Manual verification explicitly.
Success criteria are testable, not aspirational.

### Scope — PASS

"What We're NOT Doing" is explicit:

- ralph-knowledge `z.boolean()` declarations explicitly out of scope.
- Option 2 (`validateToolInput` patch) explicitly rejected with rationale.
- Option 3 (upstream filing) deferred.
- No `undefined`/optional behavior changes.
- No existing-test churn.

Scope sits comfortably inside XS.

### Dispatchability — PASS

Each task carries the required dispatch fields:

| Task | files                | tdd  | complexity | depends_on   | acceptance |
|------|----------------------|------|------------|--------------|------------|
| 1.1  | yes (create)         | true | low        | null         | yes        |
| 1.2  | yes (create + read)  | true | low        | [1.1]        | yes        |
| 1.3  | yes (modify)         | false| low        | [1.1]        | yes        |
| 1.4  | yes (modify, 9 files)| false| low        | [1.1]        | yes        |
| 1.5  | none (verify only)   | false| low        | [1.2,1.3,1.4]| yes        |

Dependency DAG sound: 1.1 must land first; 1.2/1.3/1.4 parallelizable;
1.5 gates on all three. Task 1.4 is the only broad task (9 files), but the
work is mechanical and the live grep output bounds the worklist exactly.

## Notes

- **Minor (consistent with prior critique)**: Plan body line 49 says "14
  `z.boolean()` declarations" but line 69 says "15 occurrences" — the table
  and live grep both confirm 15. Cosmetic drift, not load-bearing.
- **Minor (consistent with prior critique)**: Plan cites `src/index.ts:472-474`
  for the `validateToolInput` patch site; live source has the binding at 472
  and assignment at 473-474. The patch span matches.
- **Out-of-scope observation (intentional)**: `src/lib/routing-types.ts` has
  3 additional `.boolean()` declarations at lines 56, 128, 157. These are
  routing-config schemas (parsed from disk), not MCP tool input schemas, so
  the plan correctly excludes them — they never traverse the harness wire
  shape. Worth flagging for awareness but no plan change needed.

Neither warrants an iteration cycle.

## Recommendation

Re-affirm APPROVED. The plan was previously approved on 2026-05-12 and the
underlying tree has not drifted. Proceed to In Progress / implementation.
