---
date: 2026-05-25
github_issue: 1404
github_url: https://github.com/cdubiel08/ralph-hero/issues/1404
type: review
status: complete
verdict: APPROVED
plan_doc: thoughts/shared/plans/2026-05-25-GH-1404-8-verdict-triage-schema.md
---

# Review: GH-1404 — Define the 8-verdict triage schema + update RALPH_TRIAGE_ACTION validation

## Plan Reference

[`thoughts/shared/plans/2026-05-25-GH-1404-8-verdict-triage-schema.md`](../plans/2026-05-25-GH-1404-8-verdict-triage-schema.md)

## Strengths

- **Exceptionally accurate repo grounding.** Every load-bearing line reference verified against the live repo: slim hook regex at `ralph/hooks/scripts/triage-postcondition.sh:48`, palette test `KEEP`/`TRIAGED valid` no-match assertions at lines 64-65, plugin hook `case` allowlist at line 36. The plan did not trust the issue body blindly.
- **Correctly caught the issue body's two factual errors** (documented in Key Discoveries): the slim hook greps terminal tokens (does NOT read `RALPH_TRIAGE_ACTION`), and the issue omits the two active-path files. This is the gap that would have left the new tokens unenforced on the live path.
- **"Slim triage.md no longer has a literal KEEP verdict" is correct and material** — §Step 4 uses `CLOSE / SPLIT / RE-ESTIMATE / ROUTE-TO-* / HUMAN`. The plan reconciles the stale issue premise honestly.
- **Verdict→terminal-token design is internally consistent** with the current regex + palette test: keeps the existing alternation verbatim, adds 8 new 1:1 branches, anticipates the `WAIT-pr=.+` suffix, and flags the 1:1-vs-reuse decision for the gate.

## Gaps

- **Verification quality (minor):** Phase 1's `grep -c "^- \`TRIAGED " … increased by 8` is not self-checking — "increased by 8" needs a before/after baseline the command doesn't capture, and it counts pre-existing lines. Fix: assert an absolute count of only the new tokens. (Phase 1 → Automated Verification.)
- **Verification quality (minor):** Phase 2's hook-invocation command depends on being run from repo root (the hook sources `hook-utils.sh` via `$(dirname "$0")`). Worth a one-line "run from repo root" note. (Phase 2 → Automated Verification.)

## Failures

None.

## Recommended Changes

1. Phase 1 Automated Verification: replace "increased by 8" with `grep -cE '^- \`TRIAGED (CLOSE-done|CLOSE-canceled|SPLIT|PROMOTE-research|PROMOTE-plan|WAIT-)' …` returns 8.
2. Phase 2 Automated Verification: add "run from repo root" note to the hook-invocation commands.
3. (Polish) Explain the `WAIT-upstream=URL` → `TRIAGED WAIT-upstream` (suffix dropped) vs `WAIT-pr=NNN` → `TRIAGED WAIT-pr=NNN` (suffix kept) asymmetry — one sentence in the mapping table caption.

All three applied to the plan at review time (Approve-with-edits).

## Verdict

**Verdict**: APPROVED

**Confidence**: high — every file path and line reference was spot-checked against the live repo; all load-bearing claims verified true.

**Score**:
- Phase clarity: PASS
- File ownership: PASS
- Verification quality: GAP
- Risk coverage: PASS
- Plan-of-plans alignment: PASS
- Estimate sanity: PASS
- Linked research present: n/a

## Caveats

- The palette test / regex were not executed against the 8 new sample tokens (implementation not yet written). The regex *design* is consistent with the current file; over-match of the `WAIT-pr=.+` branch against adjacent prose can only be confirmed once implemented + the test extended — called out as an implementation watch-point.
- Issue is labeled XS; plan self-flags as ~S (the two correctly-added active-path files are the source of the bump). Scored PASS because the upsizing is transparent and justified, not scope creep.
