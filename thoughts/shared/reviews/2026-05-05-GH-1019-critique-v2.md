---
date: 2026-05-05
github_issue: 1019
github_url: https://github.com/cdubiel08/ralph-hero/issues/1019
plan_document: thoughts/shared/plans/2026-05-05-GH-1019-product-performance-over-time.md
status: approved
type: review
tags: [plan-review, metrics, snapshots, time-series, dispatchability]
---

# Plan Critique (v2): GH-1019 — Track product performance over time

This is the second-pass critique after the planner addressed v1 feedback
(Dispatchability FAIL, Feasibility minor-FAIL). Verdict: **APPROVED**.

## Dimension Scores

### Completeness — PASS
All five phases have explicit Overview, per-task table, Changes Required prose,
Success Criteria (Automated + Manual), and Implementation Notes. 16 atomic
tasks total. No phase is a stub.

### Feasibility — PASS
Verified referenced files exist:
- `plugin/ralph-hero/mcp-server/src/lib/dashboard.ts` (exists)
- `plugin/ralph-hero/mcp-server/src/lib/metrics.ts` (exists, `calculateMetrics` at L147)
- `plugin/ralph-hero/mcp-server/src/lib/transition-comments.ts` (exists, exports `parseAllTransitions`)
- `plugin/ralph-hero/mcp-server/src/tools/dashboard-tools.ts` (exists)
- `scripts/dream/launchd/com.dubiel.dream-loop.plist.template` (exists; precedent valid)

The v1 blocker — `fetchDashboardItems` reference to a non-existent helper —
is resolved by Task 1.0, which explicitly extracts the helper from the
existing inline fetch in `dashboard-tools.ts`. Phase 1 sketch now imports it
correctly. Confirmed via grep: helper does not exist yet (correct; plan creates it).

### Clarity — PASS
Success Criteria use the canonical `- [ ] Automated:` / `- [ ] Manual:` format
across all phases. Verification checks are concrete (e.g., "calling
`ralph_hero__capture_snapshot` against a fixture client appends exactly one
line", "`plutil -lint` on rendered plist succeeds"). No vague "works correctly"
language.

### Scope — PASS
"What We're NOT Doing" section is explicit and well-bounded: no SQLite, no web
UI, no Langfuse/OTel, no backfill, no signature changes to `pipeline_dashboard`
or `metrics.ts`, no retention policy in v1. Each exclusion is justified.

### Dispatchability — PASS
v1 blocker resolved. Every task in the per-phase tables now carries the
required structured fields:

| Field        | Present | Notes                                       |
|--------------|---------|---------------------------------------------|
| files        | yes     | Each task lists create/modify paths         |
| tdd          | yes     | yes/no for every task                       |
| complexity   | yes     | XS / S / M used appropriately               |
| depends_on   | yes     | em-dash for none; ID list otherwise         |
| acceptance   | yes     | One-line verifiable per task                |

`depends_on` graph is internally consistent — Task 1.2 depends on 1.0+1.1;
Task 1.3 on 1.2; Task 2.x chain on 2.0; Task 3.x on 1.1+3.0; Task 4.x on
3.2/1.2; Task 5.x on prior phase outputs. No cycles. No orphan dependencies.

A subagent given any single task can execute it without reading the whole plan.

## Sizing Note (informational, not blocking)

The plan still represents M-sized work (5 self-contained phases). The previous
critique recommended `/ralph-hero:ralph-split` to produce XS/S children. The
planner acknowledged this in the Implementation Approach section ("This issue
is a strong candidate for `/ralph-hero:ralph-split`") and the per-task
`depends_on` graph is structured to carry cleanly into per-phase sub-issues.

However, this skill's review charter is **plan quality**, not sizing
enforcement. The plan is dispatchable as-is whether executed monolithically or
after a split. Approving on plan quality; sizing/split is an orchestration
decision left to the operator.

## Strengths

- Schema-versioned JSONL with explicit migration story (`schemaVersion: 1`,
  `readSnapshots` filters unknown versions).
- Reuses existing primitives (`parseAllTransitions`, `calculateMetrics`,
  `buildDashboard`) rather than reimplementing.
- launchd template follows established dream-loop precedent.
- Performance Considerations section quantifies the cost (≤365 rows/year/project,
  ~10 GraphQL calls per snapshot at daily cadence).
- Each phase is independently shippable — Phase 1 alone delivers value.

## Verdict

**APPROVED**. All five dimensions pass. Both v1 blockers resolved cleanly.
Ready for implementation (or for `/ralph-hero:ralph-split` if the operator
prefers per-phase sub-issues).
