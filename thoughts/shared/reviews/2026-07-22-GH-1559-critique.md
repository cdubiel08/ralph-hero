---
date: 2026-07-22
github_issue: 1559
github_url: https://github.com/cdubiel08/ralph-hero/issues/1559
type: review
status: complete
verdict: APPROVED
decisions_open: 0
plan_doc: thoughts/shared/plans/2026-07-22-GH-1559-group-capture-custody-chain.md
---

# Review: Capture custody chain — polymorphic brain-dump capture + background enrichment (group: GH-1559 + GH-1560)

## Plan Reference

`thoughts/shared/plans/2026-07-22-GH-1559-group-capture-custody-chain.md` — group plan for #1559 + #1560 (children of #1554, Feature D of epic #1550).

## Strengths

- **Current-state analysis accurate against source**: the `--mode draft` 5-step body, the draft template's `status: draft` / missing `captured`, the 6-step `--mode all` fan-out with its verbatim "one line per child — 6 total" phrasing, the outcome-tokens shape, and the watch-mode branch-guard/SKIPPED pattern all verified by direct reads.
- **Plan-of-plans alignment complete on both documents**: all six epic Feature D constraints (capture never starts work; GH-706 extract-first; three bounded lookups; no new scheduler; no new verb; frontmatter contract as the whole D→C interface) and all three split-record requirements (#1559 first, #1560 contract dependency, one group PR).
- **Commit-to-main decision has in-repo precedent**: `modes/postmortem.md` already commits + pushes from a caretake mode; `branch-gate.sh` is inert unless `RALPH_REQUIRED_BRANCH` is set.
- **Grep checks are non-vacuous** (both "extract" and "kick off" return 0 pre-change) and the verification framing is honest about markdown-surface limits.
- **Decision honesty**: five decided decisions with real alternatives; sentinel present; no hold applies.

## Gaps

1. **[Phase clarity]** The mode-count bump target was wrong ("Eleven") — 11 mode bodies already exist on disk (the current "Ten" prose is stale); enrich makes 12. The Phase 2 change list also undercounted SKILL.md sync points (mode table row, `all`-row fan-out summary, frontmatter `description` list, `argument-hint` enum). *Addressed post-review: plan updated to "Twelve" with the full sync-point list.*
2. **[Risk coverage]** Unbounded per-pass N (a 30-draft backlog would strain one heartbeat tick), no push-failure handling, and the `status: forming` collision (form Step 6c's hand-off path already writes `forming` today) were unaddressed. *Addressed post-review: oldest-5 per-pass cap, rebase-retry-once push rule with `ENRICH SKIPPED push-rejected` fallback, and the contract section now reconciles both `forming` writers (enrichment markers are `enriched`/`## Enrichment`, not `forming` alone).*

## Failures

None.

## Recommended Changes

All four applied by the dispatcher 2026-07-22 (see Gaps above plus: outcome-tokens.md § Loop continuation note; postmortem commit-precedent citation in the commit-behavior decision context).

## Verdict

**Verdict**: APPROVED

**Confidence**: high — every load-bearing claim verified against actual source; the gaps found are change-list completeness details folded in without altering scope, sequencing, or the D→C contract shape.

**Score**:
- Phase clarity: GAP
- File ownership: PASS
- Verification quality: PASS
- Risk coverage: GAP
- Plan-of-plans alignment: PASS
- Estimate sanity: PASS
- Linked research present: PASS
- Decision honesty: PASS

(0 FAIL, 2 GAP ≤ 2 → APPROVED; sentinel present, zero open `#### Decision:` blocks → clean advance to In Progress.)

## Caveats

- Issue bodies #1559/#1560 were taken on trust (reviewer barred from GitHub access); consistent with the split record.
- The "kick off" grep confirms presence, not DEFAULT-flow-vs-draft-mode placement — the manual check covers placement.
