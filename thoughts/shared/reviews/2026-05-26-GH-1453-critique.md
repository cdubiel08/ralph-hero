---
date: 2026-05-26
github_issue: 1453
github_url: https://github.com/cdubiel08/ralph-hero/issues/1453
type: review
status: complete
verdict: APPROVED
plan_doc: thoughts/shared/plans/2026-05-26-GH-1453-archive-stale-plugin-ralph-hero-docs.md
---

# Review: GH-1453 — Archive stale plugin/ralph-hero docs

## Plan Reference

[`thoughts/shared/plans/2026-05-26-GH-1453-archive-stale-plugin-ralph-hero-docs.md`](https://github.com/cdubiel08/ralph-hero/blob/main/thoughts/shared/plans/2026-05-26-GH-1453-archive-stale-plugin-ralph-hero-docs.md)

## Strengths

- Every factual claim verified against `main`: all 3 live docs exist, `docs/archive/` does not yet exist, `ralph/README.md:7` carries the exact "Plan 7 of 11" stale text. Honest, verifiable Current State.
- Excellent, explicitly-defended scope discipline: the 748/693 ref breakdown is correct; `thoughts/**` + `docs/plans/**` + `docs/superpowers/plans/**` named out-of-scope; deferral to sibling #1457 stated in three places. The only non-plans `docs/` hits for `plugin/ralph-hero` are the 3 files being archived.
- `git mv` is the right history-preserving approach, verified with `git log --oneline --follow` (proves rename, not delete+add).
- Disjoint phases (Phase 1 = 3 docs → archive; Phase 2 = `ralph/README.md`), both `depends_on: null`; the hooks-suite regression guard points at a real populated dir.

## Gaps

- **Risk coverage (Phase 2 precision):** the "## Migration progress" section in `ralph/README.md` is a 12-row status table (lines ~20-34), not just prose. Row ~10 (line ~34) falsely states "Wave 3 (deletion) blocked on real-session dogfooding" — directly false now that GH-1438 deleted the plugin. The plan's Changes Required ("update/remove the section… one-line note") is adequate and the `grep -n 'plugin/ralph-hero' ralph/README.md` check incidentally catches row 34, but the gating is accidental rather than designed; the plan doesn't name the false "blocked" claim as the thing to kill.

## Failures

None.

## Recommended Changes

Applied at impl (not blocking; verdict APPROVED):
- Phase 2: name the table explicitly — drop the entire 12-row "## Migration progress" table (or replace with a one-line "Migration complete (GH-1438): all 9 verbs shipped, `plugin/ralph-hero/` deleted") so the false "Wave 3 blocked on dogfooding" row is intentionally killed, not accidentally grep-caught.

## Verdict

**Verdict**: APPROVED

**Confidence**: high — every claim verified against the live tree, scope correctly bounded, and the one GAP is a prose-precision nit the plan's own grep checks already gate against. Addressed at impl by killing the whole stale table.

**Score**:
- Phase clarity: PASS
- File ownership: PASS
- Verification quality: PASS
- Risk coverage: GAP
- Plan-of-plans alignment: PASS
- Estimate sanity: PASS
- Linked research present: n/a

## Caveats

- Assessed `main` as of 2026-05-26; did not fetch #1453/#1457/#1459 from GitHub (trusted the plan's internally-consistent sibling-ownership framing).
- Did not execute the hooks suite; confirmed the test files exist (4 present).
