---
date: 2026-05-08
github_issue: 1138
github_url: https://github.com/cdubiel08/ralph-hero/issues/1138
plan_document: thoughts/shared/plans/2026-05-08-GH-1138-autopilot-tick-body.md
status: approved
type: review
tags: [plan-review, autopilot, hero, dispatch, worktree, ralph-review]
---

# Plan Critique: GH-1138 — Autopilot Phase 2 (Tick Body)

## Summary

**Verdict: APPROVED**

The plan is a focused, single-issue, single-phase append to a skill body that has been correctly scoped, references real files at correct line numbers, and substantively corrects an inconsistency in the parent plan-of-plans (the `--review-mode` flag vs. `RALPH_REVIEW_MODE` env var). All five plan-quality dimensions pass.

## Dimension Scoring

### 1. Completeness — PASS

The plan defines six tasks (1.1 through 1.6) covering all four required steps (3, 4, 5, 6) plus end-of-file marker maintenance and a verification task. Each task lists the file(s) modified, complexity, dependencies, and acceptance checklist. The `Desired End State` and `Verification` sections enumerate concrete success conditions including grep-checkable headings, YAML frontmatter parse check, and `npm test` baseline.

### 2. Feasibility — PASS

Verified file references:
- `plugin/ralph-hero/skills/hero/SKILL.md:3` → confirmed `argument-hint: <issue-number>` exists at exactly that line.
- `plugin/ralph-hero/skills/hero/SKILL.md:50` → confirmed `Merge review: !\`echo ${RALPH_REVIEW_MODE:-interactive}\`` exists.
- `plugin/ralph-hero/skills/hero/SKILL.md:517` → confirmed `RALPH_REVIEW_MODE` table row exists.
- `plugin/ralph-hero/scripts/ralph-loop.sh` → confirmed exists (cited as the R1 anti-pattern reference).
- Parent plan lines 222-292 (Phase 2) and 265-274 (8-row diff table) → confirmed exist and table content matches verbatim.
- Parent plan-of-plans `2026-05-07-GH-1136-autopilot-skill.md` → confirmed exists.
- Phase 1 plan `2026-05-08-GH-1137-autopilot-scaffold.md` → confirmed exists.

The autopilot skill directory does not yet exist on disk — this is correctly anticipated by the plan's "Note on merge order" (lines 46-47) which states Phase 1 (#1137) must merge first. The dependency is enforced via the GitHub `blockedBy` edge, not a `depends_on` field.

The plan correctly identifies and corrects a discrepancy: the parent plan-of-plans (line 255) shows `Skill("ralph-hero:hero", args="<picked> --review-mode <interactive|auto>")`, but hero/SKILL.md:3 only declares `argument-hint: <issue-number>`. Shared Constraint #9 and Task 1.3 acceptance items document the env-var-based control that hero actually supports. This is a substantive feasibility correction, not a regression.

### 3. Clarity — PASS

Success criteria are split into Automated Verification (10 grep/test commands) and Manual Verification (5 scenarios), each as `- [ ]` checkboxes. Each task's acceptance list uses the same testable format. The 8-row outcome derivation table is reproduced verbatim with row-precedence rules ("rows are evaluated top-to-bottom and the first match wins"), avoiding ambiguity in multi-match cases.

### 4. Scope — PASS

`What We're NOT Doing` enumerates 9 explicit exclusions tied to specific later phases (counter increment → Phase 3 Step 7; termination → Phase 3 Step 8; ScheduleWakeup → Phase 3 Step 9; final summary → Phase 3 Step 10; audit log → Phase 4; hook gate → Phase 4; docs → Phase 5; worktree auto-cleanup; text-grep on hero output). The exclusions are concrete enough that an implementer can detect scope creep at task-acceptance time.

### 5. Dispatchability — PASS

Each of the 6 tasks declares all five required fields (files, tdd, complexity, depends_on, acceptance). The dependency chain (1.1 → 1.2 → 1.3 → 1.4 → 1.5 → 1.6) is linear and matches the natural file-edit order. Each task's acceptance criteria are self-contained: an implementer can grep the file post-edit to verify the expected heading, body content, and behavioral notes without re-reading the parent plan or the broader codebase. The Step 6 row-precedence note specifically resolves the only multi-match ambiguity in the diff table.

For a single-phase plan, integration testing collapses to manual verification of the four primary code paths through Steps 3-6 — explicitly noted in the `Integration Testing` section (line 235).

## Notable Strengths

1. **Substantive correction over copy-paste.** The plan does not merely transcribe the parent plan's Phase 2 markdown. Shared Constraint #9 and Task 1.3 explicitly redirect `--review-mode <flag>` (parent line 255) to `RALPH_REVIEW_MODE` env var, citing hero/SKILL.md:3 and :50 as evidence. This is exactly the kind of correction a planning step should make when downstream realities diverge from upstream specs.
2. **Catch-all `other_change` row preserved with rationale.** Task 1.4 acceptance item explicitly cites R3 origin and the rationale ("prevents silent loss of unexpected state transitions"), satisfying the parent plan's R3 contract.
3. **Anti-pattern callout retained.** Both Shared Constraint #4 and Task 1.3 acceptance restate the R1 text-grep rejection, with a working file reference (`plugin/ralph-hero/scripts/ralph-loop.sh`) for context.
4. **Worktree-collision behavior is unambiguous.** Task 1.1 acceptance items specifically forbid `git worktree remove` and require explicit "safe default" language in the body.

## Minor Observations (non-blocking)

- The acceptance for Task 1.6 cites a baseline of "1184 passed | 2 skipped" from Phase 1. This number will need to be reconciled against the actual Phase 1 merge result before Phase 2 is implemented; a stale baseline could cause a false test-failure assertion. This is a documentation hygiene point, not a planning defect.
- Shared Constraint #6 states that `--dry-run` "may still" capture pre-state for logging. Task 1.3 / Task 1.4 acceptance leaves this as implementer's discretion. Either codifying it or marking it explicitly TBD-by-implementer would tighten the spec, but the ambiguity is bounded and recoverable.

Neither observation rises to the level of `NEEDS_ITERATION`.

## Result

```json
{
  "issue": 1138,
  "result": "APPROVED",
  "critique_path": "thoughts/shared/reviews/2026-05-08-GH-1138-critique.md",
  "issues": [],
  "escalation_reason": ""
}
```
