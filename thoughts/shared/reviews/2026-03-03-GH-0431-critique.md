---
date: 2026-03-03
github_issue: 431
github_url: https://github.com/cdubiel08/ralph-hero/issues/431
plan_document: thoughts/shared/plans/2026-03-02-GH-0431-list-groups-tool.md
status: approved
type: review
---

# GH-431: Add `list_groups` Tool -- Plan Critique

## Verdict: APPROVED

The plan is well-structured, technically accurate, and ready for implementation. It demonstrates strong alignment with existing codebase patterns and produces a clear, unambiguous implementation path.

## Completeness (Pass)

- All changes are specified with exact files, line numbers, and code samples.
- The handler algorithm is written as a numbered 12-step procedure with no ambiguity.
- Both GraphQL query variants (base and extended with `showChildren`) are provided verbatim.
- The Zod schema is fully specified with types, defaults, and descriptions.
- Tests cover both structural verification and unit-level assertions.
- Success criteria are separated into Automated (`npm run build`, `npm test`) and Manual categories.

## Feasibility (Pass)

All referenced files, functions, and patterns verified against the current codebase:

- `relationship-tools.ts` exists and `registerRelationshipTools(server, client, fieldCache)` receives the correct arguments (line 118).
- `paginateConnection` from `lib/pagination.ts` has the exact signature the plan expects.
- `resolveFullConfig`, `ensureFieldCache`, `fieldCache.getProjectId` are all available from `lib/helpers.ts`.
- The `RawProjectItem` interface in the plan matches `issue-tools.ts:1742-1756` (intentionally omits `text?`/`number?` fields not needed for single-select value reads).
- `getFieldValue` helper matches `issue-tools.ts:1758-1768`.
- `registerRelationshipTools` is already called at `index.ts:345` -- no registration changes needed.
- The `list_issues` pagination pattern (`node.items` path, `maxItems: 500`) is accurately replicated.
- Research document confirms `subIssuesSummary` is available in project items content fragments.

## Clarity (Pass)

- The 12-step algorithm leaves no room for interpretation.
- The response shape is documented with a concrete JSON example.
- Test assertions use specific `toContain`/`toMatch` patterns.
- Filter behavior is described by referencing the existing `list_issues` filter chain.

## Scope (Pass)

The "What We're NOT Doing" section explicitly bounds the work with five specific exclusions:
- Not modifying the existing `list_issues` content fragment.
- Not adding child-level filtering.
- Not handling >50 sub-issues (existing codebase limit; `hasMore` flag).
- Not supporting nested depth > 1 for children.
- Not adding `percentCompleted` sort option.

These boundaries are reasonable and prevent scope creep.

## Minor Observations (Non-Blocking)

1. **`RawProjectItem` local definition**: The plan correctly duplicates the interface locally rather than exporting from `issue-tools.ts`, maintaining module independence. The omitted `text?`/`number?` fields are irrelevant for single-select field reads.

2. **`resolveFullConfigOptionalRepo` mention**: Step 1a mentions potentially importing `resolveFullConfigOptionalRepo`, but the handler algorithm uses `resolveFullConfig`. Both are valid since the tool accepts `repo` as a parameter. The handler code is internally consistent.

3. **Research alignment**: The plan directly follows Research Approach A (Recommended) with single-pass `subIssuesSummary` detection. The research doc validates that no `GraphQL-Features` header is needed and that the API fields are generally available.
