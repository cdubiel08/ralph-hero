---
date: 2026-05-08
github_issue: 1140
github_url: https://github.com/cdubiel08/ralph-hero/issues/1140
plan_document: thoughts/shared/plans/2026-05-08-GH-1140-autopilot-audit-hook.md
status: approved
type: review
tags: [plan-review, autopilot, audit-log, hook-gate, schedulewakeup, jsonl]
---

# Plan Critique — GH-1140 Phase 4: Audit Log JSONL + ScheduleWakeup Hook Gate

**Verdict: APPROVED**

This is a tight, defensively-written single-issue plan that mirrors the canonical parent-plan reference (lines 387-503 of `2026-05-07-GH-1136-autopilot-skill.md`) without drift. The R2 design decision (drop `RALPH_COMMAND` env-var check) is captured both in Shared Constraints and in Task 1.1 acceptance criteria — implementer cannot accidentally re-add it. All five plan-quality dimensions PASS.

## Completeness — PASS

All file changes are enumerated with create/modify markers:

- `plugin/ralph-hero/hooks/scripts/autopilot-wakeup-gate.sh` (create, executable)
- `plugin/ralph-hero/skills/autopilot/SKILL.md` (modify — body + frontmatter + trailing HTML comment)

Five tasks (1.1-1.5) cover create-script, append-section, add-frontmatter-matcher, fix-trailing-comment, and final invariant verification. Task 1.5 explicitly runs `npm test`, `shellcheck`, YAML-parse, and a `git diff --stat` invariant — no completeness gap. The `mkdir -p ~/.ralph-hero` + `jq -nc` append pattern is documented verbatim from parent plan lines 428-435.

## Feasibility — PASS

Verified technical claims against the live repo:

- `plugin/ralph-hero/hooks/scripts/split-estimate-gate.sh` exists (5.3 KB, executable) — reference style anchor is real.
- `plugin/ralph-hero/hooks/scripts/hook-utils.sh` exists (5.3 KB, executable) — `read_input`, `get_field`, `allow`, `block`, `warn` helpers are available for sourcing.
- `grep -rn ScheduleWakeup plugin/` returns zero results — the gate cannot break any current skill caller. The plan correctly asks the implementer to re-run this grep at merge time as a defensive re-check (Task 1.5 acceptance).
- `plugin/ralph-hero/skills/autopilot/` does NOT yet exist on main — this is correct because Phases 1-3 (#1137, #1138, #1139) build it. The plan flags this as a merge-order dependency in §Current State Analysis ("the Phase 4 implementer will work from a fresh branch off main *after* Phases 1-3 land").
- Parent plan `2026-05-07-GH-1136-autopilot-skill.md` lines 387-503 contain the canonical audit-log shape (lines 405-419), STOP/null rule (line 426), `mkdir -p` + `jq -nc` pattern (lines 428-435), 30-line hook script body (lines 442-472), and frontmatter `hooks:` block (lines 478-489) — every reference checks out.

The plan also acknowledges a subtle ordering reconciliation in Shared Constraint #10: the parent plan says "between Step 7 and Step 8" but the data needed (`next_action`, `next_delay_seconds`) is only known after Step 8's branch resolves. The plan correctly documents that the write executes after Step 8's decision but before Step 9's side-effect — a non-trivial clarification that prevents an implementer from blindly inserting the section in the wrong location.

## Clarity — PASS

Every task carries the required dispatchability fields and acceptance is unambiguous:

- Task 1.1 has 14 acceptance bullets including five concrete `echo '{...}' | ./autopilot-wakeup-gate.sh ; echo "exit=$?"` smoke tests covering all four gate paths (block-on-prompt, block-on-300, allow-good, passthrough-non-ScheduleWakeup, allow-1200).
- Task 1.2 enforces verbatim documentation of the 13-field schema and per-outcome variations against the parent plan's lines 405-426.
- Task 1.5 includes the regression-defense `grep -rn ScheduleWakeup plugin/ | grep -v ...` invariant catching any new caller introduced during the development window.
- Phase Success Criteria splits Automated and Manual buckets cleanly. Automated covers `test -f`, `test -x`, `shellcheck`, `grep -q`, YAML-parse, `npm test`, and `git diff` invariants. Manual covers JSONL validity, line counts, the `{60, 1200, null}` distribution check, and the two manual block tests.

## Scope — PASS

`What We're NOT Doing` is explicit and well-bounded:

- No log rotation (deferred per parent plan §Performance Considerations)
- No audit-log MCP tool (parent plan §Follow-up Work)
- No `RALPH_COMMAND` env-var check in the hook (R2 design choice — explicitly forbidden)
- No PostToolUse matcher (PreToolUse-only)
- No new MCP server code
- No README / CLAUDE.md / eval-scenarios edits (Phase 5 / #1141)
- No retroactive replay
- No JSONL schema versioning
- No collision with `RALPH_DEBUG` JSONL

The "Follow-up Work" section captures four future enhancements (rotation, MCP tool, schema versioning, gate allowlist) without scope-creep into this phase.

## Dispatchability — PASS

Each of Tasks 1.1-1.5 is self-contained:

| Task | files | tdd | complexity | depends_on | acceptance |
|------|-------|-----|------------|------------|------------|
| 1.1  | autopilot-wakeup-gate.sh (create) | false | low | null | 14 bullets |
| 1.2  | SKILL.md (modify) | false | low | null | 9 bullets |
| 1.3  | SKILL.md (modify, frontmatter only) | false | low | [1.1, 1.2] | 7 bullets |
| 1.4  | SKILL.md (modify, trailing comment) | false | low | [1.2] | 4 bullets |
| 1.5  | verify (read-only) | false | low | [1.1, 1.2, 1.3, 1.4] | 13 bullets |

Dependencies are explicit (1.3 needs 1.1 and 1.2; 1.4 needs 1.2; 1.5 needs all). Each task can be dispatched to a subagent with zero additional context — Task 1.1 in particular is fully reproducible from the parent-plan-quoted hook body and the smoke-test commands.

Phase dependencies are explicit at the plan level (`depends_on: null` since this is single-phase) and the wider GH-1136 sequencing is captured in §Current State Analysis (Phase 4 starts after #1139 lands, enforced via GitHub `blockedBy` edges, not `depends_on` in this plan).

## Notes for the implementer (non-blocking)

1. The plan correctly notes (Shared Constraint #10) that the parent plan's "between Step 7 and Step 8" phrasing is a *narrative ordering hint*, not a literal placement directive. Read Constraint #10 before inserting the section — the data ordering matters more than the textual ordering.
2. Task 1.1 acceptance allows `#!/bin/bash` OR `#!/usr/bin/env bash`. The parent plan's reference body uses `#!/usr/bin/env bash`; `split-estimate-gate.sh` uses `#!/bin/bash`. Either is acceptable per the plan; pick one for consistency.
3. Sourcing `hook-utils.sh` is encouraged but not required (parent plan's reference body inlines `cat | jq` without sourcing). Either is acceptable per Task 1.1 acceptance.
4. The 5-min cache-window check is a single-value blacklist (`delaySeconds == 300` only). Do NOT widen it to a range — Constraint #3 explicitly says other values like `270` or `1800` remain technically acceptable per the `ScheduleWakeup` tool's own [60, 3600] clamp.

## Conclusion

The plan is ready for implementation. No iteration needed.
