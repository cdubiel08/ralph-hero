---
date: 2026-05-03
github_issue: 983
github_url: https://github.com/cdubiel08/ralph-hero/issues/983
plan_document: thoughts/shared/plans/2026-05-03-GH-0983-impl-branch-gate-cwd-bug.md
status: approved
type: review
tags: [plan-review, hooks, impl-branch-gate, worktree, bug-fix]
---

# Plan Review: GH-983 — Fix impl-branch-gate.sh worktree-aware branch detection

## Verdict: APPROVED

The plan is single-phase, atomic, well-bounded, and grounded in verified codebase facts. Every referenced file exists; every claimed pattern is present in sibling hooks. Tasks are dispatchable with explicit acceptance criteria, file lists, complexity, and dependencies. Approve and proceed to implementation.

## Dimension Assessments

### 1. Completeness — PASS
Single phase with three tasks (1.1 implement, 1.2 test, 1.3 smoke check). Every task lists files (with create/modify/read), tdd flag, complexity, depends_on, and bullet-level acceptance criteria. Phase Success Criteria split cleanly into Automated and Manual. Prior Work, Current State Analysis, Desired End State, and What We're NOT Doing are all populated.

### 2. Feasibility — PASS
All referenced files verified to exist:
- `plugin/ralph-hero/hooks/scripts/impl-branch-gate.sh` (target — broken `git branch --show-current` confirmed at line 41)
- `plugin/ralph-hero/hooks/scripts/hook-utils.sh` (provides `allow`/`block`/`warn` helpers — confirmed at lines 48/61/68)
- `plugin/ralph-hero/hooks/scripts/impl-worktree-gate.sh` (already consumes `RALPH_WORKTREE_PATHS` at lines 40-47 — confirmed)
- `plugin/ralph-hero/hooks/scripts/pre-worktree-validator.sh`, `prune-merged-worktrees.sh` (sibling cd-then-query patterns — confirmed)
- `plugin/ralph-hero/hooks/scripts/__tests__/test-agent-phase-gate.sh` (test pattern reference — confirmed)
- Research document at `thoughts/shared/research/2026-05-03-GH-0983-impl-branch-gate-cwd-bug.md` — confirmed
The proposed three-tier resolution (cd-prefix parse → `RALPH_WORKTREE_PATHS` substring scan → warn-and-allow fallback) uses only existing primitives (`git -C`, parameter expansion, sed regex) — no new dependencies. No `eval` of user input. Backward-compatibility constraints (exit codes, stdin contract, env vars, error message format) are preserved.

### 3. Clarity — PASS
Success criteria are specific and testable. Five concrete test cases enumerated in Task 1.2 (cd-prefix to feature branch, `RALPH_WORKTREE_PATHS` substring without leading cd, explicit cd to main repo, git checkout always-allowed, non-impl falls through). Phase Success Criteria use the canonical `- [ ] Automated:` / `- [ ] Manual:` checkbox format. Manual repro command is given verbatim with expected exit code.

### 4. Scope — PASS
"What We're NOT Doing" explicitly excludes: agent-phase-gate.sh, hooks.json, agent definitions, new env vars, refactoring early-return guards, error message format changes, CI integration, pathological command strings (multi-line/here-docs/command substitution). Single XS phase, two files (one modify, one create). No scope creep risk.

### 5. Dispatchability — PASS
Each task is self-contained:

| Task | files | tdd | complexity | depends_on | acceptance |
|------|-------|-----|------------|------------|------------|
| 1.1 | impl-branch-gate.sh (modify) + 2 read refs | true | medium | null | 7 bullets |
| 1.2 | test-impl-branch-gate.sh (create) + 2 read refs | true | medium | [1.1] | 9 bullets |
| 1.3 | 2 read refs | false | low | [1.1, 1.2] | 5 bullets |

A subagent dispatched with Task 1.1 has the exact regex anchor, the substitution rule for `~`, the path resolution base (`${CLAUDE_PROJECT_DIR:-$(pwd)}`), the helper function semantics, and the line-ranges to preserve verbatim — no need to read the full plan to execute. Task 1.2 specifies the test fixture pattern (`mktemp -d` + `git init` + `git checkout -b`), the assertion style (`pass=0; fail=0; assert_eq` from sibling test), and exact JSON payloads for each case.

## Minor Observations (non-blocking)

- The Acceptance bullet "When the resolved branch (from either helper or fallback) is `main` or `master` AND the helper returned a non-empty value, the existing `block` ... call fires unchanged" is slightly redundant with the warn-and-allow fallback rule on the previous bullet — the implementer should read both together to avoid creating two divergent code paths. Not a blocker; acceptance bullets in 1.1 #5 cover the consistency check.
- Test case 3 description says "second tmpdir with `git init` (default branch `main` or `master`)". The implementer should explicitly set the default branch (`git init -b main` or post-init `git checkout -b main`) so the test is deterministic across git versions where the default branch name is configurable. Not a blocker — implementer can resolve at write-time.

Neither observation rises to NEEDS_ITERATION; both are normal implementation-time judgment calls.

## Result JSON

```json
{
  "issue": 983,
  "result": "APPROVED",
  "critique_path": "thoughts/shared/reviews/2026-05-03-GH-0983-critique.md",
  "issues": [],
  "escalation_reason": ""
}
```
