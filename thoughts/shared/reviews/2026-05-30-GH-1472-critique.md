# Critique: triage.md — authoritative actionable-phase rule + WAIT-issue=NNN verdict (#1472)

**Verdict:** APPROVED
**Reviewed:** 2026-05-30
**Plan doc:** thoughts/shared/plans/2026-05-30-GH-1472-triage-actionable-phase-wait-issue.md

## Summary

A tight, correctly-scoped doc-only plan that closes Gap C of the triage-autonomy research. Every acceptance criterion maps to a phase, the cited anchors in `ralph/skills/caretake/modes/triage.md` exist, and the proposed WAIT-issue=NNN verdict is structurally parallel to the WAIT-pr verdict already in the file. No code change is warranted and the plan correctly says so.

## Rubric scoring

| Axis | Verdict | Notes |
|---|---|---|
| Problem fidelity | PASS | The two stated gaps (no authoritative actionable-phase rule; no issue→issue WAIT verdict) match the issue body and research Gap C exactly. No scope drift. |
| Phase coverage | PASS | All 5 acceptance criteria are covered: AC1→Phase 1; AC2/AC3→Phase 2; AC4→Phase 2+3; AC5→Phase 1+3. No orphaned criteria. |
| File accuracy | PASS | Only file touched is `ralph/skills/caretake/modes/triage.md`. The cited anchors exist: the verdict table (rubric lines, WAIT-pr/WAIT-upstream rows) and the picker section that already states "Backlog state OR null workflow state." Insertion point ("after intro, before verdict list") is valid — the Decision rubric header sits below the intro/picker. |
| Verification adequacy | PASS | Doc change has concrete grep checks (`grep -n "WAIT-issue"`, `grep -n "Backlog or null"`) plus explicit manual cross-reference and consistency reads. Correctly notes `npm test` is unaffected. Appropriate for a doc edit. |
| Scope discipline | PASS | "What we're NOT doing" is present and honest: no mcp-server change, no picker-filter change (#1470 shipped it), no new verdicts beyond WAIT-issue=NNN, no other caretake modes. Estimate S matches a ~3-edit doc pass. |
| Testability | PASS | Each criterion is observable: grep-able verdict/precondition strings and a mutual cross-reference check a reviewer can confirm without guessing. |

## Coherence checks (grounded in triage.md as it stands on main)

- **No contradiction with existing behavior.** triage.md already states (picker section) that the `caretaker-triage` profile filters to "Backlog state OR null workflow state" and that pipeline-state issues are "owned by their phase agents and out of scope here." Phase 1 promotes that inline statement to an authoritative precondition block; it does not conflict with any acting-on-non-Backlog behavior — there is none in the file.
- **WAIT-issue=NNN is structurally consistent.** The existing WAIT-pr row already prescribes "add dependency edge; leave in Backlog; comment." The plan's WAIT-issue actions (ensure add_dependency blocked-by edge, post `## Triage` comment, leave in Backlog) are a faithful parallel — same shape, issue target instead of PR target.
- **Consistent with Gap A (#1470).** Leaving an issue-blocked ticket in Backlog with a real blocked-by edge is compatible with the shipped picker, which reads real blockedBy edges. The edge is the durable signal; the comment is the human-readable trail. No drift introduced.
- **Phase 3 is achievable as written.** The picker already uses the exact phrase the precondition should adopt ("Backlog or null"), so the "same language" consistency goal is directly satisfiable rather than aspirational.

## Required changes (if NEEDS_ITERATION)

None — APPROVED.

## Strengths

- Correctly resists scope creep into mcp-server (verified there is no triage logic to change).
- Reuses the established WAIT-pr verdict shape, minimizing reader cognitive load and keeping the three WAIT-* verdicts symmetric.
- Verification section is doc-appropriate (grep + manual cross-reference) rather than gold-plated with irrelevant test runs.
