---
date: 2026-04-25
github_issue: 870
github_url: https://github.com/cdubiel08/ralph-hero/issues/870
plan_document: thoughts/shared/plans/2026-04-25-GH-0870-archive-open-children-guard.md
status: approved
type: review
tags: [plan-review, hygiene-ts, archive, sub-issues, ralph-hygiene, mcp-server]
---

# Plan Critique: GH-870 Archive open-children guard for findArchiveCandidates()

## Summary

**Verdict: APPROVED**

Single-phase XS plan adding a `subIssueCount > 0` guard to `findArchiveCandidates()`. All technical claims were verified against the live codebase. Tasks are atomic, dependencies are wired, and acceptance criteria are testable. Ready for implementation.

## Verification Performed

Read directly to confirm the plan's claims:

- `plugin/ralph-hero/mcp-server/src/lib/hygiene.ts:94-107` — `findArchiveCandidates()` exists exactly as described, with two guards (terminal-state + age) and no sub-issue check.
- `plugin/ralph-hero/mcp-server/src/lib/dashboard.ts:40` — `subIssueCount: number` is a non-optional field on `DashboardItem`. The proposed `item.subIssueCount > 0` guard is type-safe.
- `plugin/ralph-hero/mcp-server/src/__tests__/hygiene.test.ts:28-41` — `makeItem()` factory exists at the cited lines and is missing `subIssueCount: 0` from defaults (consistent with the plan's claim).
- `plugin/ralph-hero/skills/ralph-hygiene/SKILL.md:138` — limitation note exists verbatim with both the "open sub-issues" clause AND the trailing "Sub-issue guard is a follow-up — see future ticket" sentence the plan targets.

## Dimension Assessment

### 1. Completeness — PASS

Single phase with four discrete tasks:
- Task 1.1: Test factory default
- Task 1.2: Production guard (one-line filter)
- Task 1.3: Two regression tests
- Task 1.4: SKILL.md cleanup

Each task names exact files with modify markers. Phase has both Automated and Manual verification blocks.

### 2. Feasibility — PASS

Every file path referenced in the plan exists at the cited line numbers. The proposed guard is syntactically and semantically valid (no new imports, no new types, no breaking change to consumers). The plan correctly notes that `subIssueCount` is a plain `number` (not `number | undefined`), so the guard `item.subIssueCount > 0` cannot throw.

### 3. Clarity — PASS

Success criteria use the `- [ ]` checkbox format throughout. Phase Success Criteria is split into Automated (`npm run build`, `npm test`, focused vitest) and Manual (diff review, SKILL.md readability, commit prefix). Acceptance bullets per task are concrete and testable.

### 4. Scope — PASS

"What We're NOT Doing" section enumerates six explicit exclusions:
- `subIssuesSummary` extension (Approach B from research)
- All-done vs open child distinction
- Open PR / recent comment guards
- Other hygiene functions (`findStaleItems`, etc.)
- Consumer (`archive_items`) logic
- New CHANGELOG.md file

Boundaries are tight. The plan explicitly accepts the trade-off that any `subIssueCount > 0` excludes the parent.

### 5. Dispatchability — PASS

Per-task field check:

| Task | files | tdd | complexity | depends_on | acceptance |
|------|-------|-----|------------|------------|------------|
| 1.1  | yes (modify) | false | low | null | yes (3 bullets) |
| 1.2  | yes (modify) | true  | low | [1.1] | yes (5 bullets) |
| 1.3  | yes (modify) | true  | low | [1.1, 1.2] | yes (5 bullets) |
| 1.4  | yes (modify) | false | low | null | yes (4 bullets) |

A subagent could execute any task using only the plan as context. Dependency graph is acyclic and minimal.

## Minor Observations (Non-Blocking)

- **Task 1.1 acceptance bullet 2** — The phrasing "alphabetically/structurally between `assignees: []` and `blockedBy: []` (or append before spread)" is slightly ambiguous, but the factory is only 14 lines so any reasonable insertion point will be correct.
- **Frontmatter `builds_on::` self-reference** — The plan lists `builds_on:: [[2026-04-25-GH-0870-archive-open-children-guard]]`, which is the plan's own slug. Likely a copy-paste artifact from a research-doc template. Cosmetic only — doesn't affect implementation.

Neither warrants iteration.

## Recommendation

Approve and advance to In Progress. Plan is dispatch-ready for `/ralph-impl 870`.
