---
date: 2026-07-22
github_issue: 1555
github_url: https://github.com/cdubiel08/ralph-hero/issues/1555
type: review
status: complete
verdict: APPROVED
decisions_open: 0
plan_doc: thoughts/shared/plans/2026-07-22-GH-1555-scheduled-brief-preparation.md
---

# Review: Scheduled daily brief preparation — local Claude Code scheduled task + runbook

## Plan Reference

`thoughts/shared/plans/2026-07-22-GH-1555-scheduled-brief-preparation.md` (GH-1555, Feature E of epic #1550, S).

## Strengths

- **Every cited precedent verified against source**: the dream-loop plist template's substitution tokens / `StartCalendarInterval` / `RunAtLoad` / explicit `PATH`; `bootstrap.sh:151-155`'s sed substitution; Feature C's § Prepare (headless) + `~/.ralph-hero/brief/last-prepared` marker (merged at `ceaf1563`); the cos-phase3 weekday-array reasoning and no-installer-under-4-plists precedent; the `run_headless()` headless-invocation shape; a fresh grep confirming zero CronCreate/claude-cron usage in-repo; `which claude` → `~/.local/bin/claude` (hence the plist PATH entry).
- **Scope discipline honors the cos post-mortem**: one plist template + one runbook section, zero brief-logic changes, idempotency reused from Feature C rather than duplicated.
- **Both key decisions argued on the merits**: launchd-over-native-cron (proven pattern vs zero in-repo precedent), and skip-permissions-now / scoped-allowlist-hardening-later (an untested allowlist risks a silently hung fire-and-forget job — the worse failure, masked by the daily push).
- **Concrete, runnable verification**: `plutil -lint`, weekday-dict count grep, marker-path drift check.

## Gaps

1. **[Risk coverage]** The plist's `/bin/bash -lc` login shell does not source the interactive zsh profile, so RALPH_* scope vars exported only there never reach the scheduled run — a board-blind `owner is required` failure the runbook did not pre-warn about. *Addressed post-review: the Setup subsection now carries the env-prerequisite line (scope vars must live in the tracked `.claude/settings.json`) and Verify requires real counts, not the error.*

## Failures

None.

## Recommended Changes

Both applied by the dispatcher 2026-07-22: the env-prerequisite + verify-counts lines, and the zero-mutation blast-radius note strengthening the skip-permissions decision.

## Verdict

**Verdict**: APPROVED

**Confidence**: high — every cited precedent verified directly against source; the single gap is a runbook-completeness note, not a structural defect.

**Score**:
- Phase clarity: PASS
- File ownership: PASS
- Verification quality: PASS
- Risk coverage: GAP
- Plan-of-plans alignment: PASS
- Estimate sanity: PASS
- Linked research present: PASS
- Decision honesty: PASS

(0 FAIL, 1 GAP → APPROVED; sentinel present, zero open `#### Decision:` blocks → clean advance to In Progress.)

## Caveats

- Whether a launchd cold-start `claude -p "/ralph:catch-up --mode brief --prepare"` loads the plugin + MCP server end-to-end in a non-interactive context is inherently a manual-verification item; the plan places it there and cites the in-repo precedent.
- The cron-absence evidence proves absence in this repo, not in Claude Code the product; the plan scopes the claim honestly.
