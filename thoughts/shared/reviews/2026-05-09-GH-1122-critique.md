---
date: 2026-05-09
github_issue: 1122
github_url: https://github.com/cdubiel08/ralph-hero/issues/1122
plan_document: thoughts/shared/plans/2026-05-09-GH-1122-github-client-test-coverage.md
status: approved
type: review
tags: [plan-review, testing, mcp-server, github-client]
---

# Plan Critique: GH-1122 github-client.ts Test Coverage

## Completeness — PASS
Single-phase atomic plan with 5 tasks (1.1–1.5). Each task lists files (with create/modify/read), tdd flag, complexity, depends_on, and an acceptance checklist. All four behaviors from the issue are mapped to dedicated describe blocks.

## Feasibility — PASS
References real files: `plugin/ralph-hero/mcp-server/src/__tests__/github-client.test.ts` (existing, 102 lines), `src/github-client.ts` (existing). Mocking pattern (`vi.mock("@octokit/graphql")`) matches the existing test file approach. Line-number references (`github-client.ts:84-104`, `:119-134`, `:122-134`) are specific.

## Clarity — PASS
Each acceptance criterion is testable: specific assertion targets (`mock.calls.length === 2`, `client.config.projectOwner === undefined`, query strings containing/excluding `rateLimit`). Phase Success Criteria has both Automated (`npm run build`, `npm test`, coverage > 85%) and Manual (toggle injection block, confirm test fails) checklists.

## Scope — PASS
"What We're NOT Doing" section explicitly excludes: refactoring github-client.ts, lib-module tests (deferred to Phase 3/GH-1121), restPost path, project-owner GraphQL dual-shape resolution. Clarifies that the latter lives in helpers.ts and is out of scope here. Resolves potential ambiguity from the issue body proactively.

## Dispatchability — PASS
Every task is self-contained with files, tdd, complexity, depends_on, and acceptance fields. Task 1.1 establishes the `beforeEach` cleanup; tasks 1.2–1.5 each depend only on 1.1, so they could parallelize. No task requires reading the full plan to execute.

## Verdict
APPROVED. Atomic XS plan, well-scoped, all five quality dimensions pass. Ready for implementation.
