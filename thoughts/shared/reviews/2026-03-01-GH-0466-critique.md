---
date: 2026-03-01
github_issue: 466
github_url: https://github.com/cdubiel08/ralph-hero/issues/466
plan_document: thoughts/shared/plans/2026-03-01-GH-0466-idle-notification-spam.md
status: approved
type: review
---

# Plan Critique: GH-466 — Reduce Idle Notification Spam

## Summary

The plan proposes two independent changes to reduce idle notification spam in ralph-team sessions: (1) delete the `worker-stop-gate.sh` hook and remove its references from all three worker agent definitions, and (2) add self-notification detection guidance to all three worker agent definitions as a workaround for GH-52. The plan is well-scoped, clearly written, and technically sound.

## Completeness

**Rating: Complete**

All required changes are enumerated with specific file paths, line numbers, and exact content to add or remove:

- Deletion of `worker-stop-gate.sh` is explicit.
- Removal of the `Stop:` hook block from all three agent definitions (ralph-analyst, ralph-builder, ralph-integrator) is specified with correct line numbers (13-16 in each file, verified).
- The self-notification guidance paragraph is provided verbatim and the insertion point ("after the Check TaskList line") is clear in each agent file.
- Success criteria cover both automated checks (file existence, grep counts) and manual verification.

No missing steps were identified.

## Feasibility

**Rating: Feasible — all claims verified**

Every file referenced in the plan exists and contains exactly what the plan describes:

| Claim | Verified |
|-------|----------|
| `worker-stop-gate.sh` exists at stated path | Yes — 41-line script with re-entry safety pattern |
| `ralph-analyst.md` has Stop: hook block at lines 13-16 | Yes — exact YAML block matches plan |
| `ralph-builder.md` has Stop: hook block at lines 13-16 | Yes — exact YAML block matches plan |
| `ralph-integrator.md` has Stop: hook block at lines 13-16 | Yes — exact YAML block matches plan |
| All agents have "Check TaskList again for more work before stopping" | Yes — analyst L25, builder L27, integrator L31 |
| `team-teammate-idle.sh` exists and is not being modified | Yes — minimal 24-line guidance-only hook |
| `team-stop-gate.sh` exists and is not being modified | Yes — separate mechanism for team lead, different scope |

The re-entry safety concern is noted in integration testing (bullet 2), which is appropriate since the deleted hook was the only thing providing re-entry protection for workers.

## Clarity

**Rating: Clear**

- The current state analysis cleanly separates already-mitigated causes (GH-231, GH-353) from the two remaining targets.
- Each change has a rationale explaining why it is safe.
- Success criteria are specific and testable: three automated checks with exact commands and expected outputs, plus one manual verification step.
- The insertion point for the self-notification guidance ("after the Check TaskList line") is unambiguous in all three files.

## Scope

**Rating: Well-bounded**

The "What We're NOT Doing" section explicitly excludes four related concerns:

1. Modifying `team-teammate-idle.sh` — correct, it is already minimal.
2. Automated enforcement of GH-231 phantom teammate prevention — correctly deferred.
3. SDK-level fix for GH-52 — correctly scoped as workaround only.
4. Modifying `team-stop-gate.sh` — correctly identified as a different mechanism.

The XS estimate is appropriate for the scope: one file deletion, three small YAML block removals, and three one-paragraph additions.

## Verdict

**APPROVED** — The plan is complete, technically accurate, well-scoped, and ready for implementation. No iterations needed.
