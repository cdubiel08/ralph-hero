---
date: 2026-05-14
github_issue: 1250
github_url: https://github.com/cdubiel08/ralph-hero/issues/1250
plan_document: thoughts/shared/plans/2026-05-13-GH-1250-model-tier-optimization-hero.md
status: approved
type: review
tags: [plan-review, hero, model-tiering, token-efficiency, plan-dedup, impl-agent, v2]
---

# Plan Critique v2 — GH-1250 Hero Model-Tier Optimization

## Summary

This is a re-review following v2 of the plan, which addressed the three NEEDS_ITERATION items from the v1 critique (`thoughts/shared/reviews/2026-05-14-GH-1250-critique.md`). All three items are resolved cleanly. Spot-checked file:line claims and structural assertions hold. Recommendation: **APPROVED**.

## Verification of v1 fixes

| v1 issue | Status | Evidence |
|---|---|---|
| `plugin/ralph-hero/CLAUDE.md` paths wrong (4 occurrences) | Fixed | `grep -nE "plugin/ralph-hero/CLAUDE\.md" plan` returns 0 matches; all CLAUDE.md references are now repo-root-relative. |
| Phase 3 transcript-discovery hand-wave | Fixed | Phase 3 step 2 now specifies `INPUT=$(read_input)` + `jq -r '.transcript_path'` + `grep -qE '^IMPL BLOCKED '`, mirroring `val-postcondition.sh:28-37`. Confirmed `read_input` exists in `hook-utils.sh:11`. |
| Phase 4 numbering (`a./b./c.` mixed with numbered list) | Fixed | Renumbered to `1.1/1.2/1.3` for mapping rules and `2.1-2.7` for the short-path action steps. Downstream references updated to `rule 1.2` and `rule 1.3`. |
| Bonus: Phase 4 prompt-passes-child-number assertion | Fixed | Added Automated Verification: `grep -E 'GH-\{[a-z_]+\}' plugin/ralph-hero/skills/ralph-plan-epic/SKILL.md`. Verified live: matches `GH-{feature_number}` at SKILL.md:255. |

## Five-dimension assessment

### 1. Completeness — PASS
All five phases have specific file changes (target files, before/after diffs, exact insertion points). The Plan Reference end-to-end path (reader, hook acceptance, comment posting, state advance) is fully specified. References sections lists every dependency.

### 2. Feasibility — PASS
Spot-checked all cited file:line refs:
- `Agent(model=...)` precedent at `ralph-impl/SKILL.md:298,311,324` — verified (`model=selected`, `model="haiku"`, `model="opus"`).
- Plan Reference reader at `ralph-impl/SKILL.md:112-125` — verified, includes `RALPH_PLAN_REFERENCE` env var setting.
- Hook gate at `impl-plan-required.sh:59-67` — verified, "Check 4" handles `RALPH_PLAN_REFERENCE`.
- val-agent verdict-prefix protocol at `ralph-val/SKILL.md:442-452` — verified, strict prefix doc.
- `--parent-plan` flag at `ralph-plan/SKILL.md:152-159` — verified, "Child Plan Mode" section.
- `plan-epic-agent` dispatch at `ralph-plan-epic/SKILL.md:234-270` — verified, dispatch at line 255.
- `read_input` helper in `hook-utils.sh:11` — verified.
- `plan-postcondition.sh` exists — verified in hooks/scripts/ directory.

### 3. Clarity — PASS
Every phase has separate Automated Verification (executable commands) and Manual Verification (human-spot-check) checklists in the canonical `- [ ] Automated:` / `- [ ] Manual:` format. Implementation Notes per phase signal pause points. Success Criteria are specific (e.g. `grep -m1 '^model:' file → model: sonnet`).

### 4. Scope — PASS
"What We're NOT Doing" lists six explicit out-of-scope items (telemetry, research-agent changes, auto-replanning, MCP/pipeline-detection changes, plan-epic/review tier changes, retroactive landcrawler-ai dedup). Migration Notes makes rollback explicit.

### 5. Dispatchability — PASS (with one caveat)
Each phase is self-contained. A subagent dispatched to "implement Phase 2" needs only the phase block + the policy doc location to do the work. Each phase explicitly states its file targets, exact patches, and verification — no need to read the full plan to execute one phase.

**Caveat (non-blocking)**: Phase 3 and Phase 4 are riskier than Phase 1, 2, 5 — the plan internally flags these ("This phase is the riskiest", "This is the biggest token saver") but the issue still carries an `M` estimate. Splitting GH-1250 into two issues (S+S, with Phases 1+2+5 in one and Phases 3+4 in the other) would be cleaner, but is not a blocker since the Implementation Notes already mandate pause-and-test gates between phases.

## Non-blocking observations

- The note in Phase 3 step 2 about `read_input > /dev/null` becoming `INPUT=$(read_input)` is correct — `read_input` in `hook-utils.sh` echoes the cached input, so capturing the echo via command substitution works as written.
- The Phase 5 ASCII diagram is fine for a README but worth verifying it renders cleanly inside a GitHub markdown code block (long lines, box-drawing characters). The plan calls for "manual verification" of this in Phase 5 — appropriate.
- Test C in the Testing Strategy uses synthetic 2-child plan-of-plans — good. Test D covers the negative path. Both are necessary; both are present.

## Verdict

**APPROVED.**

All v1 critique items resolved. Plan is dispatchable, internally consistent, and grounded against the live codebase. Proceed to implementation via `/ralph-impl 1250`.
