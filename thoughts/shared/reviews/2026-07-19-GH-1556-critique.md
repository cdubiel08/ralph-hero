---
date: 2026-07-19
github_issue: 1556
github_url: https://github.com/cdubiel08/ralph-hero/issues/1556
type: review
status: complete
verdict: APPROVED
decisions_open: 0
plan_doc: thoughts/shared/plans/2026-07-19-GH-1556-plan-research-required-file-path-rooting.md
---

# Review: plan-research-required.sh mis-roots to session CWD — file-path-derived rooting

## Plan Reference

`thoughts/shared/plans/2026-07-19-GH-1556-plan-research-required-file-path-rooting.md`

## Strengths

- Line references are accurate across all three source files: `hook-utils.sh:43-45` (`get_project_root`), `plan-research-required.sh:37/39/47/52/53/90-102`, and the test harness `CLAUDE_PROJECT_DIR="$SBX"` setup all verify against source.
- Tight scope with real precedent: the fix mirrors the established GH-983 `resolve_target_branch()` tiered-fallback pattern; both linked research docs plus the GH-983 plan exist on disk. Sibling exposure explicitly deferred, keeping this genuinely XS (helper + one call site + tests, <100 LOC).
- Design Decisions are substantive and consistent with the phase: manual `.git`-marker walk (vs `git rev-parse`) and silent `get_project_root()` fallback both match the proposed code; the `-e` test correctly handles the linked-worktree `.git` file case.
- Behavior-preservation reasoning is sound: existing cases resolve to `$SBX` either via a found marker or fallback, and the block message consuming `$research_dir` is correctly identified as auto-fixed by a correct root.

## Gaps

- **Verification quality — test-case miscount.** Plan said "All 8 existing cases"; the test file has 10 existing `run_case` invocations. *(Folded into the plan post-review: count corrected to 10.)*
- **Verification quality — contradictory negative-control expectation.** The fallback control case was described with two incompatible expected outcomes (allow via the GH-1 fixture vs block at the `$SBX` root). *(Folded into the plan post-review: one unambiguous shape — no `.git` ancestor, no matching doc under `CLAUDE_PROJECT_DIR`, expect block/exit 2.)*
- **Minor — CI command is a simplified quote.** The `test-hooks` invocation drops the CI job's `rc=$?` tail; functionally equivalent and runnable as-is. Cosmetic only.

## Failures

None

## Recommended Changes

Both substantive gaps were applied to the plan immediately after the critique (test-case count 8→10; negative-control case redefined with an explicit expected exit 2). No further changes required before implementation.

## Verdict

**Verdict**: APPROVED

**Confidence**: high — every load-bearing line reference, the research/precedent docs, and the CI `test-hooks`/`shellcheck-hooks` job shapes were verified against source; the flagged issues were prose inaccuracies in the test description, not defects in the fix design.

**Score**:
- Phase clarity: PASS
- File ownership: PASS
- Verification quality: GAP (test-case miscount + contradictory negative-control expected exit; runnable commands themselves accurate — both folded into the plan post-review)
- Risk coverage: PASS
- Plan-of-plans alignment: n/a
- Estimate sanity: PASS
- Linked research present: PASS
- Decision honesty: PASS (two resolved decisions match the phase; sentinel present; no open `#### Decision:` blocks)

## Caveats

- One GAP total; no FAIL and not >2 GAP → aggregation yields APPROVED.
- No open `#### Decision:` blocks (`decisions_open: 0`) → clean APPROVED, no decision hold.
