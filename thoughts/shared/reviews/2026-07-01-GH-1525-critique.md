---
date: 2026-07-01
github_issue: 1525
github_url: https://github.com/cdubiel08/ralph-hero/issues/1525
type: review
status: complete
verdict: APPROVED
plan_doc: thoughts/shared/plans/2026-07-01-GH-1525-hygiene-missing-workflow-state-fieldgap.md
---

# Review: `project_hygiene` missingWorkflowState fieldGap — Implementation Plan

## Plan Reference

`thoughts/shared/plans/2026-07-01-GH-1525-hygiene-missing-workflow-state-fieldgap.md` (reviewed against the linked research doc and cited source in `mcp-server/src/lib/hygiene.ts`, `mcp-server/src/tools/hygiene-tools.ts`, `mcp-server/src/__tests__/hygiene.test.ts`).

## Strengths

- **Every technical claim verifies line-for-line against main**: `findFieldGaps` non-terminal filter (`hygiene.ts:180-197`, `!ws` branch admits null states), the two interface literals (`:65`, `:87`), `totalGaps` gate (`:531-534`), `repoTotalGaps` + `renderedAny` (`:636-660`), `formatItemRow` em-dash null rendering (`:469-471`), verbatim `...report` passthrough in `hygiene-tools.ts` JSON (`:209-212`) and markdown (`:201-207`).
- **The acceptance's "summary counts include it" is read correctly.** `HygieneSummary` (`:51-58`) has no per-gap count at all; `fieldCoveragePercent` (`:420-426`) is a blended estimate+priority metric. The summary never counted field gaps, so "summary counts" can only mean the renderer gate totals — the plan extends `totalGaps`/`repoTotalGaps` and explicitly scopes out `fieldCoveragePercent`.
- **The subtle failure mode gets a dedicated regression test**: a board whose *only* gap is stateless items must still render `## Field Gaps` — exactly the bug that slips through if only the collector + interfaces were extended.
- **Both render paths are covered**: `groupBy=repo` formats each repo as a single-repo report (top-level renderer); the default multi-repo path hits the per-repo breakdown renderer. The plan modifies both sections plus the shared collector.

## Gaps

None rising to rubric-GAP level.

## Failures

None.

## Recommended Changes

- **Minor, non-blocking — tighten manual-verification wording.** `groupBy: "repo"` renders each repo as its own single-repo report (`hygiene-tools.ts:172-174`), so the item appears under the **top-level `### Missing Workflow State`** nested under `## <repoName>`; the `##### Missing Workflow State` depth appears only in the **default (non-groupBy) merged report** when 2+ repos trigger `## Per-Repository Breakdown`. The code edits are correct for both; only the prose conflates the modes.

## Verdict

**Verdict**: APPROVED

**Confidence**: high — every cited file:line independently verified; the one ambiguous acceptance phrase ("summary counts") is resolved correctly with code-grounded reasoning.

**Score**:
- Phase clarity: PASS
- File ownership: PASS
- Verification quality: PASS
- Risk coverage: PASS
- Plan-of-plans alignment: n/a
- Estimate sanity: PASS
- Linked research present: PASS

## Caveats

- The heading-depth imprecision is cosmetic; all render paths route through the two modified sections.
- A strict reader might expect a new numeric `missingWorkflowStateCount` summary field; the plan exposes the count via the JSON array length instead, consistent with how the existing two buckets are surfaced (also no summary count). Correct and consistent — flagged in case the human specifically wants a numeric field later.
- Reviewer did not execute build/tests; the four cited test pattern sites all exist and match the plan's descriptions.
