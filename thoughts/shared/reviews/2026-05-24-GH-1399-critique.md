---
date: 2026-05-24
github_issue: 1399
github_url: https://github.com/cdubiel08/ralph-hero/issues/1399
type: review
status: complete
verdict: APPROVED
plan_doc: thoughts/shared/plans/2026-05-24-GH-1399-next-actions-foreign-repo-pr-leak-fix.md
---

# Review: Tighten `next_actions` PR-search radius to open-item repos only

## Plan Reference

[`thoughts/shared/plans/2026-05-24-GH-1399-next-actions-foreign-repo-pr-leak-fix.md`](../plans/2026-05-24-GH-1399-next-actions-foreign-repo-pr-leak-fix.md)

## Strengths

- Single-phase, surgical scope. The change is one filter inside `uniqueRepos()` + one regression test. No ranker surface change, no consumer renames, no env-var plumbing.
- Concrete file:line references throughout (`directions-tools.ts:253-259`, `:421-446`, `dashboard.ts:37-57`, `lib/directions.ts:898-903`). A reader can locate every edit without guessing.
- The regression test asserts BOTH the absence of the PR direction in output AND the absence of the foreign-repo GraphQL search call. The second assertion is load-bearing — it proves the *radius* tightened, not just the *output* filtering. This is the right level of test.
- `## What We're NOT Doing` is honest about deferring Option 2 (board-linked PR filter) with explicit "until evidence warrants" reasoning. No premature abstraction.

## Gaps

None blocking.

Minor observation (informational only): the plan adds a `repository` override parameter to the `rawIssue` test fixture. That is a fixture extension, not a test-only path — confirm the default preserves `owner/repo` so existing fixtures don't shift. The plan already calls this out explicitly ("backwards-compatible parameter addition (default preserves existing fixtures)"). No change required.

## Failures

None.

## Recommended Changes

None.

## Verdict

**Verdict**: APPROVED

**Confidence**: high — the fix is well-scoped, the test is observable at the right boundary (mocked GraphQL call counts), and the research doc cleanly traces the root cause.

**Score**:
- Phase clarity: PASS
- File ownership: PASS
- Verification quality: PASS
- Risk coverage: PASS
- Plan-of-plans alignment: n/a
- Estimate sanity: PASS
- Linked research present: PASS

## Caveats

None. The plan is small enough to assess fully, the research doc covers the failure mode, and the test asserts the right observable.
