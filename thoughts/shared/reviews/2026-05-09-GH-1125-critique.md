---
date: 2026-05-09
github_issue: 1125
github_url: https://github.com/cdubiel08/ralph-hero/issues/1125
plan_document: thoughts/shared/plans/2026-05-09-GH-1125-skill-frontmatter-snapshot-tests.md
status: approved
type: review
tags: [plan-review, testing, skills, agents, frontmatter, mcp-server]
---

# Plan Critique: GH-1125 Skill + Agent Frontmatter Snapshot Tests

## Summary

Verdict: **APPROVED**. Single-phase, single-task XS plan with strong scope discipline and explicit acceptance criteria. Plan correctly reconciles a known inaccuracy in the issue body (asserting `tools:` on SKILL.md when it actually lives on the agent file) by citing the MEMORY.md allowlist-semantics feedback note.

## Completeness — PASS

All phases defined. Phase 1 enumerates the new test file path, every skill and agent file read, the helper signature (`readFrontmatter`), the constants (`SKILLS`, `AGENTS`), and the exact assertion set per group. Verification section lists three induced-failure scenarios.

## Feasibility — PASS

- `yaml@^2.7.0` already in `mcp-server/package.json` — confirmed by plan.
- `plugin/ralph-hero/mcp-server/src/__tests__/` exists and uses plain `describe`/`it` — matches plan's stated convention.
- All five skill SKILL.md files and five agent .md files exist at the paths the plan resolves to.
- Plan correctly identifies that agent `tools:` is a comma-separated string in the current files and instructs the test to split + trim, with future-proofing for an array form.

## Clarity — PASS

Phase Success Criteria split into Automated (`npm run build`, `npm test`, targeted vitest) and Manual (failure-output legibility). Task 1.2 explicitly defines two induced-failure cases with revert instructions. Failure messages required to embed skill/agent name via `%s` template label.

## Scope — PASS

"What We're NOT Doing" section explicitly excludes:
- Behavior / prompt evals
- Schema validation beyond presence + type
- Non-autonomous skills
- Full per-agent tool list (drift hazard)
- New package.json scripts

Floor-only assertion on the GitHub MCP tool list (`get_issue` + `save_issue`) is a deliberate anti-drift choice — well reasoned.

## Dispatchability — PASS

| Field | Task 1.1 | Task 1.2 |
|-------|----------|----------|
| files | yes (create + 10 reads) | yes (2 reads) |
| tdd | true | false |
| complexity | low | low |
| depends_on | null | [1.1] |
| acceptance | 7 specific bullets | 3 bullets w/ revert |

Each task is self-contained — a sub-agent could execute Task 1.1 without reading the rest of the plan.

## Recommendation

Approve. No iteration needed.
