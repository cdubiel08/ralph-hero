---
date: 2026-07-22
github_issue: 1552
github_url: https://github.com/cdubiel08/ralph-hero/issues/1552
type: review
status: complete
verdict: APPROVED
decisions_open: 0
plan_doc: thoughts/shared/plans/2026-07-22-GH-1552-pipeline-status-summary-phase-completed-event.md
---

# Review: pipeline_status_summary tool + phase-completed activity event

## Plan Reference

`thoughts/shared/plans/2026-07-22-GH-1552-pipeline-status-summary-phase-completed-event.md` (GH-1552, Feature B of epic #1550)

## Strengths

- **The load-bearing discovery is correct and independently verified.** `readActivity` (`mcp-server/src/lib/activity.ts:73-79`) globs `YYYY/MM/` then filters `/^\d{2}\.jsonl$/` files directly in the month dir — i.e. it reads `YYYY/MM/DD.jsonl`. `hero-dispatch-log.sh:35-36,42` writes `YYYY/MM/DD/HH.jsonl` (an hourly file inside a day subdirectory), which the reader never descends into. On-disk evidence corroborates: `~/.ralph-hero/activity/2026/05/25` is a stranded empty directory while sibling `05/DD.jsonl` files hold the events the reader actually returns. Phase 2 correctly targets the reader's real contract rather than propagating the bug.
- **Every named source claim checks out.** `dashboard.ts`: `aggregateByPhase` (:241), `detectHealthIssues` (:342), `PhaseSnapshot` (:60-75), `HealthWarning` (:77-89), `HealthConfig`/`DEFAULT_HEALTH_CONFIG` (:167-181). `metrics.ts`: `calculateVelocity` (:57-69), `calculateRiskScore` (:81-89), `determineStatus` (:98-105) all exist as described, and `calculateMetrics` (:152-167) genuinely requires a full `DashboardData`, justifying the direct-sub-function-call decision.
- **Tool-registration and fetch-pipeline reuse is real** — `registerDashboardTools` already registers two tools back-to-back; the owner/projectNumbers resolution, `fetchDashboardItems` loop, and `criticalStuckHours` convention are exactly as cited.
- **Hook insertion point is sound.** `impl-verify-commit.sh:58` is the unconditional fallthrough `allow`, reached only on the true success path; the `git commit`-only gate and `RALPH_COMMAND` scope guard behave as the plan claims.
- **Epic alignment is precise** — all five Feature B specifics honored (compact tool in dashboard-tools reusing lib/dashboard, one `phase_completed` event from the impl hook chain, no flow_state split, B→C soft, Wave-1 parallel).

## Gaps

None that rise to a rubric GAP.

## Failures

None.

## Recommended Changes

Polish items, not blockers — both applied by the dispatcher 2026-07-22:

1. **Phase 1 #1 wording** — `PhaseSnapshot.issues[]` entries carry no `state` field; reworded to "iterate `phases[]`, tagging each issue with its enclosing `phase.state`."
2. **Phase 2 test harness note** — the referenced bash-test harness asserts exit codes only; added the note to point `RALPH_ACTIVITY_DIR` at a temp dir and read the JSONL back with `jq` for content assertions.

## Verdict

**Verdict**: APPROVED

**Confidence**: high — every load-bearing source claim (activity path shapes, dashboard/metrics symbols, tool registration, hook fallthrough, test harness, commit convention, epic alignment) was verified directly against the files, including on-disk corroboration of the activity-log bug.

**Score**:
- Phase clarity: PASS
- File ownership: PASS
- Verification quality: PASS
- Risk coverage: PASS
- Plan-of-plans alignment: PASS
- Estimate sanity: PASS
- Linked research present: PASS
- Decision honesty: PASS

(0 GAP, 0 FAIL → APPROVED; sentinel present, zero open `#### Decision:` blocks → clean advance to In Progress.)

## Caveats

- The reviewer did not execute `npm test`/`npm run build` or the bash tests — the new test files are plan deliverables; automated-verification commands were assessed for runnability-as-written.
- The `health` field maps `determineStatus`'s `ON_TRACK|AT_RISK|OFF_TRACK` enum onto the idea doc's loosely-specified `health` name — internally consistent, worth an eyeball during the plan's manual field-shape verification step.
