---
date: 2026-05-04
github_issue: 576
github_url: https://github.com/cdubiel08/ralph-hero/issues/576
plan_document: thoughts/shared/plans/2026-05-04-group-GH-0576-extract-shared-fragments.md
status: approved
type: review
tags: [plan-review, shared-fragments, skill-templates, dry, refactor]
---

# Plan Critique: GH-576 — Extract Shared Content to Fragments

## Summary

VERDICT: APPROVED

Group plan covering parent #576 + 4 atomic children (#840, #841, #842, #843). Five phases, all mechanically scoped, with explicit byte-equivalence constraints and grep-based automated verification. Plan is highly accurate against the live codebase, identifies and documents three legitimate scope deviations from the original GitHub issue bodies, and follows the established `!cat ${CLAUDE_PLUGIN_ROOT}/...` fragment-include pattern.

## Dimension Assessment

### 1. Completeness — PASS

All 5 phases defined with specific file paths and clear descriptions. Every task identifies which files are created vs modified vs read-only. Phase 5 is correctly scoped as a verification rollup with no code changes. Shared Constraints section captures cross-phase invariants (fragment self-containment, byte-equivalence, !cat silent-failure mode).

### 2. Feasibility — PASS

Verified all referenced files exist:
- 9 in-scope skills with `## Link Formatting` confirmed via `grep -rln "## Link Formatting" plugin/ralph-hero/skills/` (10 matches; ralph-pr correctly excluded)
- ralph-pr correctly identified as out-of-scope: line 104 has `### Link Formatting in PR Bodies` (different scope), and the file already documents the planned exclusion at line 161
- `team/SKILL.md:115` confirmed as the sole location of `| Phase | Subject Pattern |...` table (hero has no equivalent)
- Stream Detection sections confirmed at the cited line numbers in both team and hero
- Existing fragment-include pattern verified at `ralph-impl/SKILL.md:459` (`!cat ${CLAUDE_PLUGIN_ROOT}/skills/shared/fragments/escalation-steps.md`)
- Prior research doc `thoughts/shared/research/2026-03-01-GH-0471-shared-fragments-and-skill-prompt-refactor.md` exists
- Existing 7 fragments listed in `skills/shared/fragments/` confirmed (artifact-discovery.md, ask-user-question.md, error-handling.md, escalation-steps.md, knowledge-metadata.md, skill-vs-agent-dispatch.md, team-reporting.md)

Three scope deviations from original issue bodies are correctly identified:
1. GH-840 said "10 skills"; plan correctly excludes ralph-pr (different content) → 9 skills
2. GH-842 said "share between hero and team"; plan correctly excludes hero (no equivalent table)
3. GH-841 disambiguates the existing duplication between `shared/artifact-comment-protocol.md` and `shared/fragments/artifact-discovery.md`

### 3. Clarity — PASS

Success criteria are specific and testable. Every phase has both Automated Verification (concrete grep commands with expected match counts) and Manual Verification (visual inspection steps). Examples:
- Phase 1: `grep -rn "Single-repo (default)" plugin/ralph-hero/skills/` returns exactly 1 match
- Phase 3: `grep -rn "| Phase | Subject Pattern | Owner | Command | activeForm |"` returns exactly 1 match
- Phase 5: cumulative grep suite with 4 distinct expected counts

Acceptance criteria use `- [ ]` checkbox format throughout.

### 4. Scope — PASS

"What We're NOT Doing" section is explicit and well-bounded:
- ralph-pr `Link Formatting in PR Bodies` excluded with reason (different scope)
- hero/SKILL.md task template excluded with reason (no equivalent table)
- ralph-val and bridge-artifact artifact discovery excluded with reasons (different shapes / producer-side)
- No prose changes during extraction (byte-equivalence)
- No `npm run build` (skill markdown is runtime-loaded)
- No new test infrastructure (verification is grep + visual diff)

The decision to NOT modify ralph-val's discovery is well-justified — it has a different discovery shape (looking for plan paths in comments, not section headers).

### 5. Dispatchability — PASS

Every task has all required fields:

| Field | Coverage |
|-------|----------|
| files | Yes — every task lists specific paths with (create/modify/read) |
| tdd | Yes — explicitly `false` for all tasks (mechanical refactor, no tests) |
| complexity | Yes — `low` or `medium` per task |
| depends_on | Yes — explicit `null` or `[task IDs]` (e.g., 1.2 depends_on [1.1]) |
| acceptance | Yes — checkbox lists with verifiable criteria |

Phase dependencies are explicit: Phases 1-4 are independent (`depends_on: null`); Phase 5 depends on `[phase-1, phase-2, phase-3, phase-4]`. The plan explicitly notes Phases 1-4 are parallel-capable for hero/team orchestrator scheduling.

Integration testing section covers cross-phase interactions: load a modified skill via Claude Code's loader and verify `!cat` resolution at runtime, plus per-skill regression check.

## Group Plan Specifics

For multi-issue group plans, the plan covers:

- **Phase dependencies explicit**: Each phase's `depends_on` declared at the phase level. Parent rollup (Phase 5) explicitly waits for all 4 children.
- **What each phase creates for the next**: Phase 1 establishes the working `!cat` pattern for Phases 2-4 to copy. Phase 2 demonstrates partial-section inclusion with skill-specific glue prose. Phases 3-4 are independent. Phase 5 is the verification rollup.
- **Integration testing covers cross-phase interactions**: Confirms no skill regression across all modified skills (ralph-plan, ralph-impl, ralph-review, ralph-research, ralph-merge, ralph-split, ralph-triage, finish, hero, team).

## Minor Observations (non-blocking)

These do not affect APPROVAL but the implementer should be aware:

1. **Task 2.1 contains a binary decision** ("does the long-form `shared/artifact-comment-protocol.md` reference doc add value?"). The plan defers this decision to implementation time, with the commit message documenting the choice. This is acceptable for a `medium` complexity task but the implementer should make the call early in Phase 2.

2. **Task 4.3 acceptance includes another deferred decision** ("Decide in Task 4.1 whether the fragment is one combined block or three subsections"). Same pattern — acceptable, but flagged.

3. **Phase ordering note**: The plan documents that Phases 1-4 *could* be parallel but sequences them 1→2→3→4 for "reviewable PR diffs". Since this is a single-PR group, the sequencing is for implementer cognitive load, not data dependency. This is appropriate.

## Verdict

The plan satisfies all five quality dimensions and follows the established fragment-extraction pattern from prior work (GH-471, GH-550). All technical claims verify against the live codebase. Scope deviations from original issue bodies are correctly identified, justified, and documented. Verification is grep-based and reproducible.

Ready for implementation.
