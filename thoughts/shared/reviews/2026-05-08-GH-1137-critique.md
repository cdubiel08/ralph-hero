---
date: 2026-05-08
github_issue: 1137
github_url: https://github.com/cdubiel08/ralph-hero/issues/1137
plan_document: thoughts/shared/plans/2026-05-08-GH-1137-autopilot-scaffold.md
status: approved
type: review
tags: [plan-review, skill, autopilot, scaffold, hero, autonomous]
---

# GH-1137 Plan Critique — Phase 1 Scaffold

**Plan**: [2026-05-08-GH-1137-autopilot-scaffold.md](https://github.com/cdubiel08/ralph-hero/blob/main/thoughts/shared/plans/2026-05-08-GH-1137-autopilot-scaffold.md)
**Verdict**: APPROVED

## Summary

Single-phase plan that creates `plugin/ralph-hero/skills/autopilot/SKILL.md` with frontmatter, Configuration block, and Steps 0 through 2.5. Inherits all design from the approved parent plan-of-plans `2026-05-07-GH-1136-autopilot-skill.md` (R3 APPROVED). Plan is dispatchable to impl-agent without further iteration. Sizing is XS — appropriate.

The `needs-iteration` label currently on the issue appears to be stale from before the parent plan's R3 approval (no prior 1137-specific critique exists in `thoughts/shared/reviews/`). The label can be removed on approval transition.

## Per-dimension assessment

### 1. Completeness — PASS

- All seven tasks (1.1 through 1.7) defined with `files`, `tdd`, `complexity`, `depends_on`, `acceptance`.
- Linear dependency chain: 1.1 → 1.2 → 1.3 → 1.4 → 1.5 → 1.6 → 1.7.
- Single-phase plan correctly annotates `depends_on: null` on the lone phase.
- Phase Success Criteria split into Automated (4 items) and Manual (5 items).

### 2. Feasibility — PASS

Verified all referenced files exist:

- `plugin/ralph-hero/skills/hero/SKILL.md` — exists; cited as frontmatter shape reference.
- `plugin/ralph-hero/skills/finish/SKILL.md` — exists; cited line 4 confirmed for multi-flag `argument-hint` syntax.
- `plugin/ralph-hero/hooks/scripts/set-skill-env.sh` — exists; cited as `SessionStart` helper.
- `plugin/ralph-hero/mcp-server/src/lib/directions.ts` — exists; lines 180-185 confirmed (`ACTIONABLE_PHASES` set including `"In Review"`).
- `thoughts/shared/plans/2026-05-07-GH-1136-autopilot-skill.md` — exists; lines 116-138 (frontmatter), 140-147 (Configuration), 153-200 (Steps 0/1/2/2.5) confirmed and contain drop-in copy as claimed.
- `plugin/ralph-hero/skills/autopilot/` — absent on disk (correctly — Task 1.1 creates it).

Minor citation drift (informational, not blocking): plan refers to `hero/SKILL.md:47-53` for the Configuration block; actual lines are 44-50. The verbal anchor ("matches the shape of") and the parent plan's drop-in copy make the off-by-3 unambiguous in practice.

### 3. Clarity — PASS

Acceptance criteria are checkbox-formatted and verifiable. Concrete predicates throughout:

- `test -f plugin/ralph-hero/skills/autopilot/SKILL.md`
- `python -c "import yaml; yaml.safe_load(open(...).read().split('---')[1])"` exits 0
- `cd plugin/ralph-hero/mcp-server && npm test` passes (unchanged)
- `git diff --stat` shows only the new skill file
- Manual checks for `/help` listing, refusal-without-enable, empty-backlog stop, `Ready for Plan` pick, `In Review` filter

Step 2.5's two filter rules are stated in code form (not just prose), preserving the R3-approved precision.

### 4. Scope — PASS

`## What We're NOT Doing` section is explicit and tagged with the deferring phase per bullet:

- No dispatch logic → Phase 2
- No `git worktree list` check → Phase 2
- No pre/post `get_issue` diff → Phase 2
- No `ScheduleWakeup` call → Phase 3
- No termination conditions beyond Step 2.5 → Phase 3
- No audit log file → Phase 4
- No `PreToolUse` hook gate → Phase 4
- No README / CLAUDE.md / eval-scenarios.md edits → Phase 5

Prevents Phase 1 from absorbing Phase 2-5 work. Boundary is clean.

### 5. Dispatchability — PASS

Each task is self-contained for subagent dispatch. An impl-agent given any single task plus the parent plan reference can execute without reading the full plan body:

| Task | Files | tdd | Complexity | depends_on | Acceptance count |
|------|-------|-----|------------|------------|------------------|
| 1.1  | autopilot/SKILL.md (create) | false | low | null | 6 |
| 1.2  | autopilot/SKILL.md (modify) | false | low | [1.1] | 4 |
| 1.3  | autopilot/SKILL.md (modify) | false | low | [1.2] | 4 |
| 1.4  | autopilot/SKILL.md (modify) | false | low | [1.3] | 6 |
| 1.5  | autopilot/SKILL.md (modify) | false | low | [1.4] | 5 |
| 1.6  | autopilot/SKILL.md (modify) | false | low | [1.5] | 6 |
| 1.7  | autopilot/SKILL.md (modify) | false | low | [1.6] | 3 |

`tdd: false` is appropriate — this task creates a markdown skill file with no automated test surface beyond YAML parse + `npm test` pass-through (covered in Phase Success Criteria). Integration testing section explicitly states "Phase-1-only — full integration verified in later phases. For Phase 1, the manual verification list above is the integration test."

## Notes for impl-agent

- The parent plan provides drop-in copy for frontmatter (lines 116-138), Configuration block (lines 140-147), and Steps 0/1/2/2.5 (lines 153-200). No invention required.
- The `PreToolUse` slot for the wakeup gate is intentionally absent in this phase; Task 1.1 acceptance allows either omission or stub. Phase 4 (GH-1140) creates the gate file and adds the registration.
- Stale `needs-iteration` label on GH-1137 will be cleared on approval transition.

## Verdict rationale

All five plan-quality dimensions pass with no findings that warrant iteration. The plan is bounded, files exist, dependencies linearize, acceptance criteria are testable, and tasks dispatch cleanly. APPROVE.
