---
date: 2026-05-18
github_issue: 1299
github_url: https://github.com/cdubiel08/ralph-hero/issues/1299
plan_document: thoughts/shared/plans/2026-05-18-GH-1299-pushnotification-alongside-ntfy.md
status: approved
type: review
tags: [plan-review, push-notification, ntfy, ralph-merge, ralph-unblock, caretake, hero, dispatch]
---

# Plan Review: GH-1299 — P2: Add native `PushNotification` alongside ntfy

## Summary

VERDICT: **APPROVED**

This is a tight, well-scoped atomic plan (S estimate, 4 file edits, identical pattern across tasks). Phase 2 of parent plan-of-plans `2026-05-17-claude-code-dispatch-incremental-adoption.md`. Every referenced file, line number, and code anchor was independently verified. The plan correctly resolves the parent plan's open question about graceful no-op behavior, preserves ntfy as fallback, and keeps per-file rollback. One minor inaccuracy (step number) noted below — does not block approval.

## Completeness — PASS

All four tasks (1.1–1.4) are fully specified with files, complexity, depends_on, tdd flag, and acceptance criteria. Frontmatter has all required fields (`github_issue`, `github_urls`, `parent_plan`, `tags`, `status`, `type`, `date`). Prior Work block, Shared Constraints, Current State Analysis, Desired End State, and Verification sections are all present. Phase Success Criteria has both Automated and Manual sub-sections. Integration Testing section enumerates three end-to-end flows.

## Feasibility — PASS

Verified all four target files exist:
- `plugin/ralph-hero/skills/ralph-merge/SKILL.md` ✓
- `plugin/ralph-hero/skills/ralph-unblock/SKILL.md` ✓
- `plugin/ralph-hero/skills/caretake/SKILL.md` ✓
- `plugin/ralph-hero/skills/hero/SKILL.md` ✓
- `plugin/ralph-hero/scripts/lib/push-on-completion.sh` ✓

Spot-checked line references:
- `ralph-merge/SKILL.md` Step 9c at line 380, ntfy invocation at lines 388-395 — plan's "(line 388-395)" reference is accurate.
- `hero/SKILL.md` line 449 contains the `__ESCALATE__` transition — plan's "currently at line ~449" is accurate.
- `ralph-unblock/SKILL.md` posts `## Unblock Request` at **Step 5** (line 139), not Step 4 as the plan claims twice (in Current State Analysis row table and Task 1.2 acceptance).

**Minor inaccuracy**: The Step 4/Step 5 mismatch in ralph-unblock is cosmetic — the plan correctly identifies the action ("after the step that posts the `## Unblock Request` comment") so the implementer will land in the right place regardless. Worth fixing during implementation but does not gate approval.

Verified zero existing `PushNotification` references in `plugin/ralph-hero/` (allowlists or skill bodies), confirming the "additive change" framing. `PushNotification` is a Claude Code native tool (per the parent plan-of-plans), so no plugin-side wiring is needed.

## Clarity — PASS

Every acceptance criterion uses verifiable, testable language. Examples:
- "`PushNotification` appears in the `allowed-tools` list (frontmatter)" — directly greppable
- "`grep -c "PushNotification" ... returns at least 2 per file` — fully automated
- "The fire-and-stop order is preserved: `save_issue(__ESCALATE__)` → `PushNotification(...)` → STOP" — explicit invariant

Phase Success Criteria uses the required `- [ ] Automated:` / `- [ ] Manual:` style. Verification block in Desired End State enumerates six concrete trigger scenarios with expected outcomes.

## Scope — PASS

The "What We're NOT Doing" section is explicit and well-bounded with five concrete exclusions:
1. Phase 2.5 (collapse ntfy to fallback) — deferred
2. Removing/modifying `push-on-completion.sh` — out of scope
3. Adding `PushNotification` to non-terminal skills (`ralph-research`, `ralph-impl`, `ralph-pr`)
4. Per-day rate caps
5. Telemetry-only changes (OTel attributes)

Each exclusion has a rationale. The Shared Constraints section also bounds scope by referencing parent-plan invariants (additive change, ntfy stays as fallback, no allowlist drift).

## Dispatchability — PASS

Each of the four tasks has the required fields:

| Task | files | tdd | complexity | depends_on | acceptance |
|------|-------|-----|------------|------------|------------|
| 1.1 (ralph-merge) | ✓ | ✓ false | ✓ low | ✓ null | ✓ 4 checkboxes |
| 1.2 (ralph-unblock) | ✓ | ✓ false | ✓ low | ✓ [1.1] | ✓ 4 checkboxes |
| 1.3 (caretake) | ✓ | ✓ false | ✓ low | ✓ [1.2] | ✓ 3 checkboxes |
| 1.4 (hero) | ✓ | ✓ false | ✓ low | ✓ [1.3] | ✓ 4 checkboxes |

Each task is self-contained — an implementer dispatched to any single task can complete it without reading the others (the shared pattern is documented in Implementation Approach). The chained `depends_on` annotation is for review clarity per the plan's note, not strict dependency — the plan explicitly says "could be done in any order".

For a multi-issue group plan note: this is a single-issue atomic plan (S, one phase) so phase-level dependency is N/A. Integration Testing section covers the cross-file interaction surface (multiple skills firing notifications during a typical flow).

## Risks Considered

The plan explicitly addresses:
- Graceful no-op when Remote Control unpaired (constraint + verification + acceptance language)
- Graceful no-op on Bedrock/Vertex routing (constraint + verification scenario)
- Double-notification during validation cycle (called out as acceptable; matches parent plan)
- Failure isolation: `PushNotification` failure must not fail parent skills (each task's acceptance criteria reiterate this)

## Minor Suggestions (Non-Blocking)

1. Fix the Step 4 → Step 5 reference in ralph-unblock to match the actual skill file (current line 139, "Step 5: Post `## Unblock Request` Comment").
2. Consider whether Task 1.3 (caretake allowlist-only) is needed in this PR or can be deferred — the plan notes the actual fire happens in `ralph-unblock`, and the "future Heartbeat-mode" use case is speculative. Could ship without it. Not a blocker.

Neither rises to NEEDS_ITERATION.

## Recommendation

**APPROVE.** Ready for `/ralph-impl 1299`.
