---
date: 2026-05-25
github_issue: 1407
github_url: https://github.com/cdubiel08/ralph-hero/issues/1407
type: review
status: complete
verdict: APPROVED
plan_doc: thoughts/shared/plans/2026-05-25-GH-1407-caretake-watch-upstream-mode.md
---

# Review: GH-1407 — `/ralph:caretake --mode watch-upstream`

## Plan Reference

[`thoughts/shared/plans/2026-05-25-GH-1407-caretake-watch-upstream-mode.md`](../plans/2026-05-25-GH-1407-caretake-watch-upstream-mode.md)

## Strengths

- **Dual stale-comment claim fully verified.** The issue body references both a `## Blocker` and a `## Deferred Verdict` comment; neither exists — `triage.md` §Step 5 `WAIT-upstream` writes the URL into the `## Triage Decision` comment + applies `blocked:upstream` + `ralph-triage`. The plan reads `## Triage Decision` and defaults to `PROMOTE-plan`, matching the merged watch-pr.
- **`list_issues(label: "blocked:upstream")` exact-label filtering verified correct** (`issue-tools.ts` exact `l.name === args.label`) — simpler than watch-pr's `blocked:pr-*` client-side prefix filter, and the plan calls out the divergence.
- **All four watch-pr review lessons folded in up front** (verified against the `6ab56134` fix commit): explicit `labels=` strip on both advance + escalate, dedicated `SKIPPED` token, `ADVANCED <N>` = advanced+escalated, verdict→state mapping + ungated-transition note.
- **Mode-count bump (Nine→Ten) and plan-of-plans alignment verified accurate.**

## Gaps

- **Stuck-forever risk was unacknowledged** (risk coverage). The escalate branch fires only on a dead/unparseable URL; an item with a live-but-unconfirmable condition stays parked indefinitely with no age/sweep escape — genuinely worse than watch-pr (where an open PR is an unambiguous state that will flip). **Fixed at review time** → documented as a deliberate scoping choice in `## What We're NOT Doing`, with a future-phase sweep-count escalation noted.

## Failures

None.

## Recommended Changes

Both folded into the plan at review time (Approve-with-edits):
1. Acknowledge the stuck-forever risk in `## What We're NOT Doing` (live-but-unconfirmable items park indefinitely; rely on human/manual escape; future phase could add sweep-count escalation).
2. Add a concrete "confident MET" heuristic for non-GitHub URLs in Phase 1 §Step 4 (package-registry version/tag match, or explicitly-named HTTP status; anything else → still-blocked) so "best-effort" doesn't widen scope at impl time.

## Verdict

**Verdict**: APPROVED

**Confidence**: high — every load-bearing claim (dual stale-comment references, `## Triage Decision` URL source, exact-label filtering, the four folded-in watch-pr lessons, Nine→Ten) spot-checked against the repo and holds.

**Score**:
- Phase clarity: PASS
- File ownership: PASS
- Verification quality: PASS
- Risk coverage: GAP (stuck-forever risk — fixed at review time)
- Plan-of-plans alignment: PASS
- Estimate sanity: PASS
- Linked research present: PASS

## Caveats

- The "best-effort fetch" mechanism for non-GitHub URLs was under-specified in the draft; the review-time edit pins a concrete heuristic, but the implementing agent still makes real design calls there. The conservative-by-default posture bounds the blast radius.
