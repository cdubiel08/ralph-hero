---
date: 2026-05-13
github_issue: 1192
github_url: https://github.com/cdubiel08/ralph-hero/issues/1192
plan_document: thoughts/shared/plans/2026-05-13-GH-1192-setup-skill-delegation-docs-polish.md
status: approved
type: review
tags: [plan-review, llm-delegation, setup-skill, documentation, onboarding]
---

# Plan Critique: GH-1192 — F6 Setup-skill integration + delegation docs polish

## Summary

XS-estimated, four-task documentation/skill closeout for the GH-965 LLM delegation epic. All four tasks are independent (`depends_on: null` each), affect only Markdown files, and ship in one commit. Plan is dispatchable as-is.

## Verification of Key Plan Decisions

1. **Root `CLAUDE.md` vs plugin `CLAUDE.md`**: Verified — only `/Users/dubiel/projects/ralph-hero/CLAUDE.md` exists; `plugin/ralph-hero/CLAUDE.md` does NOT exist. The plan correctly targets the root file and calls out the issue-body discrepancy explicitly in Current State Analysis (line 62 of the plan).
2. **`ralph-delegate.sh --health-check`**: Verified at `plugin/ralph-hero/scripts/ralph-delegate.sh:65-66`. Returns 0 on success, 127 on unreachable, with a hard 2-second timeout. Wrapper reused unchanged.
3. **Plugin README delegation section (lines 249-307)**: Verified canonical and complete. Contains env-var table, exit-code crib, quick-check, audit-log spec, F5 telemetry references, and links to F3 authoring guide. Plan correctly states "No changes needed here."
4. **Setup skill insertion feasibility**: Verified. Step 6b ends near line 619; Step 7 starts at line 637. The new Step 6c insertion point sits cleanly between them. Skill already follows the `AskUserQuestion` + record-state + Final-Report-line pattern in Step 6b — F6's Step 6c follows the same shape.
5. **`delegation-conventions.md:33` "upcoming" sentence**: Verified present and ready to rewrite. F5 (#1191) is closed; promoting the reference to "merged" is accurate.

## Dimension Assessment

### Completeness — PASS

All four tasks have explicit `files`, `tdd`, `complexity`, `depends_on`, and `acceptance` fields. The Phase Success Criteria block has 7 automated checks (`grep` invocations) and 5 manual checks (live probe-succeeds, probe-fails, already-enabled scenarios). Final Report extension specified in both simple-setup and split-owner variants.

### Feasibility — PASS

All referenced files exist on disk and were verified. The `--health-check` wrapper flag is real. Insertion points in the setup skill (between Step 6b and Step 7) are clean. The plugin README's env-var table (lines 257-263) is available to mirror verbatim in the top-level README. CLAUDE.md Architecture block has a clear slot between "Caching Strategy" (line 174) and "Hook Patterns" (line 180).

### Clarity — PASS

Acceptance criteria use checkbox format with specific assertions:
- "Probe block uses `bash "$CLAUDE_PLUGIN_ROOT/scripts/ralph-delegate.sh" --health-check` with `2>/dev/null` and captures `rc=$?`"
- "Branch on `$rc`: if 0, continue to the AskUserQuestion; if 127 (unreachable) or anything else, silently skip onboarding"
- "Total subsection length: 5-8 lines (this is a quick-reference doc, not a tutorial)"

Manual verification scenarios (Live test 1/2/3) describe exact operator actions and expected outcomes.

### Scope — PASS

"What We're NOT Doing" has 9 explicit bullets covering settings-file migration, auto-install of gemma-lab, auto-starting the LLM server, per-task env-var prompts, modifying existing delegating skills, touching the canonical plugin README, adding a setup CLI subcommand, auto-writing per-task overrides, and touching ralph-knowledge/dream-loop. Boundary is unambiguous.

### Dispatchability — PASS

Each task has the required fields:

| Task | files | tdd | complexity | depends_on | acceptance |
|------|-------|-----|------------|------------|------------|
| 1.1 setup skill | yes (modify) | false | medium | null | 9 checkboxes |
| 1.2 top-level README | yes (modify) | false | low | null | 6 checkboxes |
| 1.3 root CLAUDE.md | yes (modify) | false | low | null | 6 checkboxes |
| 1.4 conventions doc | yes (modify) | false | low | null | 3 checkboxes |

All four tasks are independent (no inter-task dependencies). Each task body contains enough context to dispatch in isolation.

## Verdict

**APPROVED** — no iteration needed. Plan is XS-appropriate, addresses the issue body's incorrect path with explicit reasoning, preserves the no-regression invariant (silent probe failure, confirmation gate before writing `RALPH_DELEGATE_ENABLED=true`), and inherits the existing setup skill's scope-detection decision rather than duplicating it. Ready for `/ralph-impl 1192`.
