---
date: 2026-05-05
github_issue: 1022
github_url: https://github.com/cdubiel08/ralph-hero/issues/1022
plan_document: thoughts/shared/plans/2026-05-05-GH-1022-snapshot-capture-jsonl.md
status: approved
type: review
tags: [plan-review, snapshots, persistence, jsonl, mcp-server]
---

# GH-1022 Plan Critique — Snapshot Capture & JSONL Persistence

## Verdict: APPROVED

## Dimension Assessment

### Completeness — PASS
Four atomic tasks (1.1 fetch extraction, 1.2 persistence module, 1.3 capture tool, 1.4 server wiring) cover the full Phase 1 scope. Each task names exact file paths with create/modify markers. Phase 1 boundaries with sibling phases (1023-1026) are explicit.

### Feasibility — PASS
All referenced files exist and line numbers match:
- `plugin/ralph-hero/mcp-server/src/tools/dashboard-tools.ts` L440-450 inline `paginateConnection<RawDashboardItem>` + `toDashboardItems` block confirmed.
- `lib/metrics.ts:147` is `calculateMetrics` signature, exactly as cited.
- `lib/dashboard.ts`, `index.ts`, `types.ts`, `github-client.ts`, and `__tests__/dashboard.test.ts` all present.
- `registerXyzTools(server, client, fieldCache)` registration pattern confirmed against `tools/activity-tools.ts` neighbor.
- `os.homedir()` choice over `process.env.HOME` is correct for cross-platform safety.

### Clarity — PASS
Acceptance criteria use checkbox format with verifiable conditions (file paths, function signatures, command invocations). Phase Success Criteria splits Automated vs Manual verification cleanly. Snapshot interface is fully typed in the acceptance bullets.

### Scope — PASS
"What We're NOT Doing" section explicitly defers cycle-time, trends tool, skill/scheduler, docs, retention, and backfill to named sibling phases. Additive-only constraint (no signature changes to `buildDashboard`/`calculateMetrics`) is restated.

### Dispatchability — PASS
Each task has files (with create/modify), tdd, complexity, depends_on, and acceptance fields. Dependency DAG (1.1, 1.2 parallel; 1.3 depends on both; 1.4 depends on 1.3) is explicit. Task 1.3 fully describes the handler pipeline (`fetchDashboardItems → buildDashboard → calculateMetrics → toSnapshot → appendSnapshot → toolSuccess`) so a builder needs no extra context.

## Notes
- Minor inconsistency: issue body labels tasks 1.0-1.3 but the plan uses 1.1-1.4. Cosmetic only — the plan is self-consistent and the implementer will follow the plan numbering.
- `Snapshot.cycleTime` reservation is well-flagged for Phase 2 continuity.

## Recommendation
Approve and proceed to implementation.
