---
date: 2026-05-25
github_issue: 1410
github_url: https://github.com/cdubiel08/ralph-hero/issues/1410
type: review
status: complete
verdict: APPROVED
plan_doc: thoughts/shared/plans/2026-05-25-GH-1410-triage-reject-keep.md
---

# Review: GH-1410 — triage-postcondition rejects KEEP

## Plan Reference

[`thoughts/shared/plans/2026-05-25-GH-1410-triage-reject-keep.md`](../plans/2026-05-25-GH-1410-triage-reject-keep.md)

> **Outcome:** Independent rubric review scored **5 PASS / 2 GAP / 0 FAIL** (rubric-approvable per "0–2 GAP + 0 FAIL → APPROVED"). Both GAPs were valid and have been **fixed at review time** (Approve-with-edits) → final verdict **APPROVED**.

## Strengths

- **Core exit-2 design verified correct.** Confirmed against the repo: `hook-utils.sh` `warn()`=exit 0 / `block()`=exit 2; the `*)` catch-all calls `warn`; `RALPH_TRIAGE_ACTION=KEEP` currently exits 0 "(legacy)". So removing KEEP from the allowlist alone would leave it allowed-with-warning — the plan's explicit `KEEP)`→`block` branch (before `*)`) is exactly right.
- **Slim "no change" verified** — `KEEP` is not a slim terminal token; the palette test asserts `assert_no_match "KEEP …"`. Regression guard is real.
- **Scope tension surfaced, not buried** — the behavior-line ("only the 8") vs title/ACs (KEEP-only) conflict is explicitly flagged with reasoning, scoped to KEEP-only, broader removal left as a reviewer decision.
- Clean 2-phase split; XS estimate apt.

## Gaps (both fixed at review time)

1. **File ownership** — the plan missed a 4th stale ref: `specs/issue-lifecycle.md:137` `| KEEP | *(no state change)* |` in a normative "Triage-specific actions" table, which contradicts the plan's own Desired End State. **Fixed** → added as Phase 2 change #4 (delete/annotate the row) + a verification check + Key Discoveries note.
2. **Verification quality** — Phase 1 check #4 (`grep -A2 … | grep KEEP`) would match the docstring/reject-branch comment, so it couldn't cleanly assert "KEEP not in an allow arm." **Fixed** → replaced with a precise allow-arm-line assertion, leaning on the exit-2 behavioral test as the real guard.

## Failures

None.

## Recommended Changes

Both folded in at review time:
1. Add `specs/issue-lifecycle.md` KEEP-row purge to Phase 2 (change #4 + verification).
2. Replace the unreliable Phase 1 check #4 with a precise allow-arm assertion.

## Verdict

**Verdict**: APPROVED (after edits)

**Confidence**: high — every load-bearing claim (exit-2 nuance, slim no-change, the 4th stale ref) verified against the repo; both GAPs are mechanical and resolved.

**Score**:
- Phase clarity: PASS
- File ownership: PASS (after adding specs/issue-lifecycle.md)
- Verification quality: PASS (after fixing check #4)
- Risk coverage: PASS
- Plan-of-plans alignment: PASS (Phase 6/6; merge closes epic #1417)
- Estimate sanity: PASS
- Linked research present: n/a

## Caveats

- `specs/issue-lifecycle.md` is a normative spec (not one of the three #1404-touched triage docs); it's pulled in because the plan's own end-state ("no doc tells an author KEEP is usable") makes it in-scope, and leaving a spec that says "KEEP → no state change" would contradict the enforced postcondition.
- Other repo-wide KEEP hits (`RALPH_DELEGATE_LOG_KEEP`, `KEEP_GOING`) are unrelated and correctly out of scope.
