---
date: 2026-05-08
github_issue: 1151
github_url: https://github.com/cdubiel08/ralph-hero/issues/1151
plan_document: thoughts/shared/plans/2026-05-08-GH-1151-hello-catch-up-context-flood.md
status: approved
type: review
tags: [plan-review, hello, catch-up, activity-log, context-budget, retention]
---

# Plan Critique — GH-1151: Reduce /hello context flood + add activity log retention

## Verdict

APPROVED. Five tightly-scoped phases, every referenced file exists, every cited line matches the live source, success criteria are concrete and grep-able, and scope boundaries are explicit. Two minor reference-path inaccuracies noted under Feasibility — neither blocks implementation.

## Five Plan-Quality Dimensions

### 1. Completeness — PASS

All five phases define specific file changes with paths, file mode (new vs modify), and inline code/diff. Each phase has its own Success Criteria block with both Automated and Manual sub-sections. Cross-cutting sections present: Overview, Current State Analysis, Desired End State, What We're NOT Doing, Implementation Approach, Testing Strategy, Performance Considerations, Migration Notes, References. No phases are stubs.

### 2. Feasibility — PASS (with two minor reference-path notes)

Spot-checked references against the live tree:

- `plugin/ralph-hero/skills/hello/SKILL.md:31` contains `Invoke `Skill("ralph-hero:catch-up")`` exactly as the plan claims. Replacement diff lands correctly.
- `plugin/ralph-hero/skills/hello/SKILL.md:142` contains `Skill() is fork-safe` exactly as the plan claims. Replacement diff lands correctly.
- `plugin/ralph-hero/skills/hello/SKILL.md` already has `Agent` in `allowed-tools` (verified — line 14). Plan's "no change needed" claim holds.
- `plugin/ralph-hero/mcp-server/src/tools/activity-tools.ts` Zod schema currently sets `limit: z.number().int().min(1).default(100)` — exactly what the plan proposes to lower to 50.
- `plugin/ralph-hero/mcp-server/src/lib/activity.ts` — `ActivityEvent` and `ActivityReadConfig` shapes match the plan's proposed extension. `events.slice(0, config.limit)` is on line 99, matching the plan's insertion point.
- `plugin/ralph-hero/agents/unblock-agent.md` template shape (frontmatter with name/description/model/tools/skills) matches the proposed `catch-up-agent.md` exactly — same haiku/single-skill/narrow-tools pattern.
- `plugin/ralph-hero/skills/shared/fragments/skill-vs-agent-dispatch.md` confirms the inline-Skill / forked-Agent dichotomy that motivates the fix.
- `plugin/ralph-hero/scripts/snapshot/launchd/com.ralph.snapshot.plist.template` exists — proposed `com.ralph.activity-rotate.plist.template` mirrors the same shape.

**Minor reference-path inaccuracy** (non-blocking):

- Plan's References section (line 741) cites `plugin/ralph-hero/scripts/dream/logrotate.sh` as the existing rotation pattern. Actual path is `scripts/dream/logrotate.sh` (repo top level, not under `plugin/`). Implementation does not depend on this path — the plan re-derives logic from scratch. Suggest fixing the reference during Phase 4 doc polish.

### 3. Clarity — PASS

Success Criteria use the standard `- [ ] Automated:` / `- [ ] Manual:` format throughout. Automated checks are concrete commands (every one is a grep, test -f, or test -x against a known path). Manual checks are short, observable, and unambiguous (e.g., "narrative paragraph appears, no tool dump, cursor advances"). End-state Verification block (lines 87-109) gives an end-to-end script that an implementer can run wholesale.

### 4. Scope — PASS

"What We're NOT Doing" section enumerates seven explicit out-of-scope decisions, each with a one-line rationale (YAGNI for server-side aggregation, retention not removal for meta events, hooks unchanged, no other tool surfaces touched, no PR-list change, other Skill() call sites preserved, no new dependencies). Migration Notes correctly flag the only behavioral break (default `limit` 100→50) and verify there are no external callers.

### 5. Dispatchability — PASS

Each phase is self-contained: a sub-agent given Phase 1 alone could land it without reading Phases 2-5. Phases declare their dependencies via Implementation Notes ("pause for confirmation before starting Phase N+1") and the plan opener notes "Phases 1 and 2 land together; Phase 3 ships separately; Phase 4 is doc polish; Phase 5 is one-shot cleanup."

For each phase the implementer has:
- Files (paths + create/modify mode)
- Inline diffs / full file content
- TDD path (Phase 2 explicitly adds three new test cases; Phase 3 calls out a new bash test harness mirroring `record-activity.test.sh`)
- Acceptance checklist (Automated + Manual)

No task requires re-reading the full plan to act on.

## Summary

This is a textbook small-ticket plan: research is current and accurate, the fix is decomposed into independently shippable phases, every file path resolves, every cited line matches the live source, and the success criteria are grep-able. The two minor reference-path notes (logrotate.sh path) are nits that can be fixed during Phase 4 doc polish or ignored.

Approved for implementation. Next step: `/ralph-impl 1151`.
