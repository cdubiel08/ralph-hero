---
date: 2026-05-19
github_issue: 1317
github_url: https://github.com/cdubiel08/ralph-hero/issues/1317
plan_document: thoughts/shared/plans/2026-05-19-GH-1317-extract-shared-ui-heuristic.md
status: approved
type: review
tags: [plan-review, refactor, bash, scouts, playwright]
---

# Plan Critique: GH-1317 — Extract Shared UI Heuristic

## Summary

**Verdict: APPROVED.**

Single-phase XS refactor with verbatim regex preservation, explicit sourceability acceptance criteria, and a comprehensive verification matrix (8 automated checks + 2 manual). The plan correctly identifies the heuristic source location (`scout-heuristic-smoke.sh:32-38`), reconstructs missing parent-plan context from the issue body and sibling phases, and binds the refactor to the exact downstream consumers (Phase 2 GH-1318, Phase 3 GH-1319). All five plan-quality dimensions pass.

## Dimension Scoring

### Completeness — PASS
One phase with three explicitly-numbered tasks (1.1, 1.2, 1.3), each carrying `files`, `tdd`, `complexity`, `depends_on`, and checkbox acceptance criteria. The "Creates for next phase" footer establishes the handoff contract for siblings.

### Feasibility — PASS
Verified `plugin/ralph-hero/scripts/scout-heuristic-smoke.sh` exists and contains the `_ui_heuristic` function at lines 32-38 with the exact regex referenced (`\.(tsx|svelte|vue|css|scss)$|/components/|(^|/)storybook/`). The target directory `plugin/ralph-hero/scripts/shared/` does not yet exist (correctly expected — Task 1.1 creates it). Sibling reference `scout-merge-gate-smoke.sh` exists. The sourcing pattern (`BASH_SOURCE[0]` + `cd && pwd`) is the canonical bash idiom and works in GitHub Actions runners.

### Clarity — PASS
Every verification item is concrete and shell-runnable. The Desired End State section contains 8 `- [ ]` checkable items, plus Phase Success Criteria adds 7 more `Automated:` items and 2 `Manual:` items. Acceptance criteria reference exact line numbers (32-38, 84-106, 43-56) and exact assertion counts (17 → 19+).

### Scope — PASS
"What We're NOT Doing" enumerates seven explicit exclusions covering sibling phases (Phase 2-5), regex changes, test-framework additions, and standalone-CLI repurposing. Shared Constraints section adds 6 hard invariants (no behavior change, POSIX-safe sourcing, single source of truth, idempotent sourcing, exit-code preservation, bash-only).

### Dispatchability — PASS
Each of the three tasks is self-contained:
- Task 1.1 (create helper) has zero deps, full acceptance checklist for header/API/guards.
- Task 1.2 (refactor smoke) depends on 1.1, specifies the exact sourcing snippet and the removed line range.
- Task 1.3 (sourceability assertions) depends on 1.2, specifies two explicit `bash -c` sub-shell invocations the implementer must add.

A subagent could execute any one of these tasks with only the task block as context. The single-phase structure means no cross-phase dependency mapping is needed; the `depends_on` chain (null → [1.1] → [1.2]) is intra-phase.

## Strengths

1. **Verbatim-preservation commitment**: Constraint #1 ("the heuristic regex is moved verbatim") plus the grep-based verification (item 6: regex appears exactly zero times in smoke, exactly once in shared) gives a mechanical no-drift guarantee for what is fundamentally a refactor.
2. **Sourceability proof baked into the smoke test**: Task 1.3 doesn't just add unit assertions — it adds *sub-shell sourcing* assertions that exercise the exact invocation pattern Phase 2/3 will use. This catches sourcing bugs before downstream phases inherit them.
3. **Idempotent sourcing guard**: Task 1.1 requires a `declare -F is_ui_touching` guard so multi-source patterns (which Phase 2 dispatch logic may trigger) don't error.
4. **Defensive shell hygiene**: Explicit prohibition against `set -euo pipefail` at top level of the sourceable library — sourceable libs must not mutate caller shell options. This is a real bug class avoided up front.

## Minor Observations (Not Blocking)

1. **Backward-compat alias `_ui_heuristic`**: Task 1.1 acceptance #4 keeps `_ui_heuristic()` as a delegating alias to minimize diff. Reasonable; flag for removal after Phase 2/3 land if no other consumers remain.
2. **Parent plan reconstruction**: Plan honestly notes that `2026-05-18-GH-1314-...md` was not on disk and reconstructs from issue body + siblings. This is documented transparently in the "Prior Work" note — acceptable handling.

## Recommendation

APPROVED. Ready for `/ralph-impl 1317`.

---

*Critique generated by ralph-review in AUTO mode against ralph-hero/skills/shared/quality-standards.md.*
