---
date: 2026-07-01
github_issue: 1526
github_url: https://github.com/cdubiel08/ralph-hero/issues/1526
type: review
status: complete
verdict: APPROVED
plan_doc: thoughts/shared/plans/2026-07-01-GH-1526-next-actions-stateless-aggregate-direction.md
---

# Review: `next_actions` aggregate stateless-triage direction — Implementation Plan

## Plan Reference

`thoughts/shared/plans/2026-07-01-GH-1526-next-actions-stateless-aggregate-direction.md` (reviewed against the research doc and cited source in `mcp-server/src/lib/directions.ts`, `mcp-server/src/tools/directions-tools.ts`, `ralph/skills/catch-up/next-action-ranking.md`).

## Strengths

- **Insertion seam verified-correct and safe.** `rankDirections` ends at `directions.ts:1020` (`return directions;`) right after the `recommended` stamp (`:1013-1018`); nothing downstream re-reads `directions[0]` or switches on kind. When the aggregate fires the list was empty, so the stamp was a no-op and the pushed Direction carries `recommended: true` itself — no double-stamp, no ranking perturbation.
- **`directions.length === 0` is the right gate — better than the agent path's `scored.length === 0`** since PR rows merge in after `scored` (`:888-925`); the final-array check correctly accounts for PR-only results.
- **No mcp-server consumer breaks on a both-null Direction** (grep: every `.issue.`/`.pr.` access is on GraphQL objects, never on `rankDirections` output); `AGENT_BACKLOG_FALLBACK_PENALTY` already imported at `directions.ts:34`.
- **Null-only scoping matches the issue text**; terminal states are non-null values, so `=== null` is exhaustive.
- **catch-up contract fully handled** (three ranking-doc edits match the real table formats; the `Skill("ralph:caretake", ...)` dispatch row mirrors the `human-needed-unblock` precedent at `:99`); leaving catch-up `SKILL.md` untouched is correct by construction.
- **Test plan grounded**: the `:199-209` mirror test, `makeItem`/`makeConfig` fixtures, and the agent null-state fallback test (`:164-176`) all exist.

## Gaps

- **Risk coverage — hero default-mode is an unaddressed human-default consumer.** `ralph/skills/hero/SKILL.md:143` (hero default mode, no issue arg) reads `next_actions({})` at the human default, then resolves `TARGET → get_issue({ number })`. A both-null `triage` direction yields no issue number for that flow. Grep confirms the only human-default call sites are catch-up (handled) and hero `:143` (unhandled). Real but low-frequency (hero default + no arg + stateless-only board).

## Failures

None.

## Recommended Changes

1. In the same PR, add a one-line carve-out at hero's default-mode picker (`ralph/skills/hero/SKILL.md:143` area): a `kind: "triage"` direction dispatches `Skill("ralph:caretake", args="--mode triage")` board-wide instead of `get_issue({ number: TARGET })`. Add a matching Migration Notes bullet to the plan.
2. Minor: unify the `reason` string (plan's version wins: `"<N> item(s) have no workflow state — run /ralph:caretake --mode triage"`).

## Verdict

**Verdict**: APPROVED

**Confidence**: high — every cited file:line verified; the one gap is a real but low-frequency edge that does not undermine the core deliverable.

**Score**:
- Phase clarity: PASS
- File ownership: PASS
- Verification quality: PASS
- Risk coverage: GAP
- Plan-of-plans alignment: n/a
- Estimate sanity: PASS
- Linked research present: PASS

Aggregation: 0 FAIL, 1 GAP → APPROVED.

## Caveats

- Build/tests not executed by the reviewer; all constructs verified to compile by inspection (9 required Direction fields present; `statelessCount?` additive; threshold constant in scope).
- Hero exposure assessed from skill prose, not live execution; either way it is an unhandled shape the implementation now covers per Recommended Change 1.
