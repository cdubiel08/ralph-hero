---
date: 2026-07-19
github_issue: 1565
github_url: https://github.com/cdubiel08/ralph-hero/issues/1565
type: review
status: complete
verdict: APPROVED
decisions_open: 0
plan_doc: thoughts/shared/plans/2026-07-19-GH-1565-group-mcp-tool-surface-optimization.md
---

# Review: Group plan: `create_sub_issues` batch tool + zero-reference tool prune (GH-1565 + GH-1566)

## Plan Reference

- Plan: `thoughts/shared/plans/2026-07-19-GH-1565-group-mcp-tool-surface-optimization.md`
- Parent plan-of-plans: `thoughts/shared/plans/2026-07-19-GH-1563-plan-of-plans.md`
- Research: `thoughts/shared/research/2026-07-19-GH-1563-mcp-tool-surface-pruning-and-tree-creation.md`
- Issues: #1565 (primary, Plan in Review), #1566 — group of 2, parent #1563, estimate S per member, one PR
- Open `#### Decision:` blocks found: **0** (the plan explicitly states "None — no open design decisions")

## Strengths

- **File:line claims are almost uniformly accurate.** Spot-verified the load-bearing ones against source: `batch-tools.ts` builders (:47-196), `MUTATION_CHUNK_SIZE` (:213), `projectMutate` (:504); draft quartet `server.tool` blocks at :44/:145/:201/:267 with the next surviving tool (`create_status_update`) at :478; `list_groups` at relationship-tools.ts:1060-1290 with `RawProjectItem` (:121), `getFieldValue` (:135), and `paginateConnection` import (:35) confirmed exclusive to that block; `view-tools` wiring at index.ts:33/:535 and `fetchProjectViews` at project-tools.ts:653 with no other caller; `debug_stats` at :763-812 with the JSONL machinery confirmed exclusive (`collate_debug` uses the separate `lib/error-signature.ts` — `error-signature.test.ts` imports from there, so deleting `debug-tools.test.ts` is exactly right); the cross-tool-consistency pin at :923-946 and the comment-only `list_groups` reference at cross-tool-pagination-consistency.test.ts:25 both check out.
- **Design Decisions section is genuinely honest** — six real judgment calls (module placement, staged-aliased mechanics, `batch_update` wire-in vs delete, edge-wiring ownership, `fetchProjectViews` cleanup, hook coverage) each with options and rationale, and the `batch_update` decision discharges the parent's explicitly deferred question.
- **Parent alignment is clean**: GH-1565 → GH-1566 phase order, single PR per GH-1538, roster edits landing with the motivating phase, and the informational coupling (Feature B decided with Feature A's shape known) all match the plan-of-plans.
- Partial-failure semantics, pre-mutation cycle validation, and closing the size-gate bypass show real failure-mode thinking, not just happy-path design.

## Gaps

1. **Verification quality — off-by-one tool count in the success criteria.** The named prune set is **7 tools** (draft quartet = 4, plus `list_groups`, `create_views`, `debug_stats` — the plan's own grep command lists all 7 names, and the research doc correctly says 7), but the plan said "6" throughout: Overview, Phase 2 title, Desired End State item 7 ("38 → 33: −6 pruned, +1 added"), and Task 2.4's CHANGELOG entry ("−6 tools"). The arithmetic that *is* correct: "6 manifest entries removed" (the `EXPECTED_TOOLS` manifest has 36 entries and excludes the RALPH_DEBUG-gated `debug_stats`). The arithmetic that is wrong: post-change totals are **32 with RALPH_DEBUG (31 always-on + `collate_debug`)**, not "33 tools (32 + collate_debug)" as Phase 2's Manual Verification stated — a val-agent following that check literally will report a false failure. The miscount is inherited from the parent plan-of-plans and GH-1566's title (issue #1565's body says 7), but the group plan is where an implementer reads the expected counts. *Fix*: Desired End State item 7 → "38 → 32: −7 pruned (6 always-on + 1 debug-gated), +1 added"; Phase 2 Manual Verification → "lists 32 tools (31 + collate_debug under RALPH_DEBUG)"; CHANGELOG acceptance → "−7 tools (named)".
2. **File ownership — the size-gate matcher lives in a file Task 1.3 doesn't own.** `split-size-gate.sh` is registered via `ralph/skills/caretake/SKILL.md` frontmatter (line 23), and the hook-roster table at `ralph/skills/caretake/split-decomposition.md:97` documents the `ralph_hero__create_issue` matcher. Extending coverage to `create_sub_issues` requires editing both — neither was in Task 1.3's file list (which named only the script plus decomposition.md, form/SKILL.md, split.md). The script change is also more than a matcher tweak: it currently reads a scalar `.tool_input.estimate`, so it needs child-array iteration for the batch tool. *Fix*: add `ralph/skills/caretake/SKILL.md` and `ralph/skills/caretake/split-decomposition.md` to Task 1.3's files, and note the array-iteration requirement in its acceptance.
3. **(Sub-gap, folded into 1's dimension) Two verification/file references wrong-but-hedged.** `bash ralph/hooks/scripts/__tests__/run-tests.sh` does not exist — CI runs each `*.test.sh` via `find` (ci.yml:123); the parenthetical hedge saves it, but the primary command isn't runnable as-is. Also "existing hook tests extended" is inaccurate: there is **no** `split-size-gate` test file in `ralph/hooks/scripts/__tests__/` — a new test file must be created. Similarly `mcp-server/CHANGELOG.md` does not exist; the repo convention is the root `CHANGELOG.md` (human-maintained `## [Unreleased]`), which the plan's hedge names as fallback. *Fix*: name the real invocations/files directly.

## Failures

None.

## Recommended Changes

1. Correct the count everywhere it appears: "6 zero-reference tools" → "7 (6 always-on + debug-gated `debug_stats`)"; "38 → 33" → "38 → 32"; Phase 2 Manual Verification → "31 tools (32 with RALPH_DEBUG)"; Task 2.4 CHANGELOG acceptance → "−7 tools". Keep "6 manifest entries removed" as-is (correct). Optionally note the parent/GH-1566-title count is stale rather than propagating it.
2. Task 1.3: add `ralph/skills/caretake/SKILL.md` (frontmatter matcher `ralph_hero__create_issue` → also match `ralph_hero__create_sub_issues`) and `ralph/skills/caretake/split-decomposition.md` (hook table row at :97) to the files list; acceptance should state the script iterates every `children[].estimate`.
3. Phase 1 Automated Verification: replace the `run-tests.sh` line with the actual per-file invocation (new `split-size-gate.test.sh`) or the ci.yml `find` loop; change "existing hook tests extended" to "new `split-size-gate.test.sh` covering scalar and child-array paths".
4. Task 2.4: name root `CHANGELOG.md` as the primary file (drop the `mcp-server/CHANGELOG.md` guess).

> **Disposition**: all four recommended changes were folded into the plan doc immediately after this review (same session, surgical edits) — see the plan's git history.

## Verdict

**Verdict**: APPROVED

**Confidence**: high — every load-bearing structural claim was verified against source and held; the two gaps are precise, mechanical fixes that don't change the design, and were folded into the plan without re-planning.

**Score**:
- Phase clarity: PASS
- File ownership: GAP (caretake SKILL.md frontmatter + split-decomposition.md hook table missing from Task 1.3 — folded)
- Verification quality: GAP (off-by-one expected tool counts; hook-test command and CHANGELOG path wrong-but-hedged — folded)
- Risk coverage: PASS (external-caller impact, release automation, manifest-as-tripwire all covered)
- Plan-of-plans alignment: PASS
- Estimate sanity: PASS (Phase 1 is a dense S but bounded by the existing `batch-tools.ts` precedent; Phase 2 is mechanical deletion)
- Linked research present: PASS
- Decision honesty: PASS (six resolved decisions with rationale; zero open `#### Decision:` blocks; no decision/phase contradictions)

Aggregation: 0 FAIL, 2 GAP → APPROVED. With 0 open `#### Decision:` blocks, no hold applies — the group advances per standard routing.

## Caveats

- The research doc lives in a different worktree (`bridge-cse_01JtrZkEy4BAc5iqfpdtmzHB`); the plan's References use the repo-relative path, which will resolve once merged but does not exist in the implementation worktree today — impl-agent should not treat a missing local research file as a blocker.
- Line numbers were verified at the current worktree HEAD (`d5b48717`); any main-branch drift before implementation could shift the surgical ranges in `project-management-tools.ts`/`relationship-tools.ts` — apply the plan's "read fully before edit" guidance generally.
- `debug-tools.ts`'s header comment (lines 9-10) says `debug_stats` is "preserved for backward compat" — a deliberate prior decision the plan overrides. The zero-live-reference evidence justifies it, but the CHANGELOG entry should acknowledge the removal reverses that stated intent, and the header comment must be updated in Task 2.3 (it would otherwise be a hit for the zero-hits grep criterion).
