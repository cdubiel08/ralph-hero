---
date: 2026-05-28
github_issue: 1457
github_url: https://github.com/cdubiel08/ralph-hero/issues/1457
type: review
status: complete
verdict: APPROVED
plan_doc: thoughts/shared/plans/2026-05-28-GH-1457-docs-readme-index.md
---

# Review: Add `docs/README.md` Index Separating Living Guides from Historical Design Records

## Plan Reference

[thoughts/shared/plans/2026-05-28-GH-1457-docs-readme-index.md](../plans/2026-05-28-GH-1457-docs-readme-index.md)

## Strengths

- Categorization is factually accurate and complete — an independent enumeration of `docs/` confirmed every living doc (4) and every historical directory (4) is correctly classified, with nothing omitted or misfiled.
- The labeled-section-over-relocation decision is well-justified (preserves inbound links from issues/PRs/commits; `docs/archive/` already exists from GH-1453) and recorded in both Overview and Migration Notes — strong risk coverage.
- Phase 2's link-resolution gate directly operationalizes acceptance criterion 3 ("all index links resolve"); scope boundaries (no CI job → #1458; no `thoughts/` entry point) are crisp.
- Prior Work cites real sibling issues under epic #1459 and correctly states the tension is none (no prior index exists; `docs/README.md` confirmed absent).

## Gaps

- **Verification quality (Phase 2)** — the link check is described in prose ("extract every `](...)` relative target and assert each exists") but the `Automated Verification` checkbox lacks a copy-pasteable command, unlike Phase 1's literal `test -f` / `grep -c`. Fix at Phase 2 → Success Criteria → Automated Verification: use a runnable one-liner, e.g.:
  ```
  grep -oE '\]\(([^)]+)\)' docs/README.md | sed -E 's/.*\(([^)]+)\)/\1/' | grep -v '^http' | while read t; do test -e "docs/$t" || echo "MISSING: $t"; done
  ```

## Failures

None.

## Recommended Changes

1. Add the runnable link-check one-liner above to Phase 2 so the Automated Verification is executable as-is. (Non-blocking — the implementer will produce a concrete check regardless; captured here for reuse.)
2. Optional/cosmetic: `docs/superpowers/plans/` is 19 files, not the hedged "~18" in Current State Analysis. Does not affect the deliverable (links are directory-level, not per-file).

## Verdict

**Verdict**: APPROVED

**Confidence**: high — every factual claim was independently verified against the live `docs/` tree; the lone GAP is a verification-ergonomics nit, well under the 2-GAP threshold.

**Score**:
- Phase clarity: PASS
- File ownership: PASS (`docs/README.md` named in both phases)
- Verification quality: GAP (Phase 2 command is prose, not runnable as-is)
- Risk coverage: PASS (relocation-vs-label tradeoff in What We're NOT Doing + Migration Notes)
- Plan-of-plans alignment: n/a (child of epic #1459, planned individually)
- Estimate sanity: PASS (one net-new md file + a link check = correctly S)
- Linked research present: PASS (research waiver reasonable for sub-threshold S; Prior Work cites real sibling/epic issues)

## Caveats

- Link *targets* were verified to exist on disk, but the exact relative-link syntax the implementer writes (e.g. trailing-slash directory links rendering on GitHub) can't be verified pre-implementation — which is exactly what Phase 2's check must catch, reinforcing the value of making that command runnable.
