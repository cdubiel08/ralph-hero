---
date: 2026-05-19
github_issue: 1320
github_url: https://github.com/cdubiel08/ralph-hero/issues/1320
plan_document: thoughts/shared/plans/2026-05-19-GH-1320-mark-scouts-live-docs.md
status: approved
type: review
tags: [plan-review, scouts, director, documentation, model-tier-policy, claude-md, taxonomy]
---

# Plan Critique — GH-1320: Mark Scouts Live in Docs

## Summary

Single-issue atomic XS docs-only plan. Touches exactly three files (`event-classes.md`, `CLAUDE.md`, `docs/model-tier-policy.md`) to flip scouts from "pending Feature F" to "live" across taxonomy and agent-registry surfaces. Phase 4 of 5 in the GH-1314 epic.

Verdict: **APPROVED**. Plan is atomically scoped, factually grounded against the live state of every file it mutates, and includes a strong self-guard (Task 1.4) that refuses to mark scouts live if the upstream Phase 2 / Phase 3 deliverables are not on `main`. Every line-number and verbatim-string claim in the plan was spot-checked against the working tree at review time and matched exactly.

## Five-Dimension Assessment

### 1. Completeness — PASS

All four tasks (1.1, 1.2, 1.3, 1.4) include `files`, `tdd`, `complexity`, `depends_on`, and `acceptance`. Every acceptance bullet is verifiable via a concrete grep / `test -f` / `git diff --stat` command. The plan explicitly enumerates the three rows in `event-classes.md` to mutate (lines 17, 29, 58) with verbatim old/new text. The `CLAUDE.md` mutations are decomposed into one row-add (agents table) and one row-replace (user-facing surface) with the exact new text spelled out, including the cron-note preservation. The `model-tier-policy.md` mutation specifies the new env-var example line and the tier-rationale entry. Nothing important is left to implementer discretion beyond the explicitly flagged "impl-agent's judgment is acceptable" cases (row position in agents table; exact placement of tier rationale line).

### 2. Feasibility — PASS

Every referenced file exists on disk at review time:

- `plugin/ralph-hero/skills/director/event-classes.md` — exists, lines 17/29/58 contain exactly the text the plan claims.
- `CLAUDE.md` — exists, Per-Phase Agents table at lines 62-74, User-facing surface table at lines 85-97, Scouts row at line 92 reads exactly `| Scouts team | no skill — scout-nightly.sh cron + on-PR comment trigger |` as the plan asserts.
- `plugin/ralph-hero/docs/model-tier-policy.md` — exists, examples block at lines 27-33 (plan says 29-33 which is the example-line subset; close enough — the surrounding "Per-session overrides" section spans 21-33). The `RALPH_<AGENT>_MODEL` override pattern is real and matches the plan's claim.

Sibling plan files all exist on disk: `2026-05-19-GH-1317-extract-shared-ui-heuristic.md`, `2026-05-19-GH-1318-scouts-team-skill.md`, `2026-05-19-GH-1319-per-pr-producer-playwright-auto-workflow.md`. The plan is candid about the parent epic plan-of-plans (`2026-05-18-GH-1314-...`) being missing on disk and explicitly says it reconstructs scope from the issue body and siblings — that transparency is the right move.

Phase 2 (`skills/scouts/SKILL.md`, `agents/scouts-agent.md`) and Phase 3 (`.github/workflows/playwright-auto.yml`) deliverables do NOT exist yet — but this is expected and explicitly handled. The plan declares `depends_on: [GH-1318, GH-1319]` and Task 1.4 enforces this via `test -f` sanity checks that emit `IMPL BLOCKED needs=phase-2-or-3-not-yet-merged` if the upstream files are absent. This is exactly the right guard — without it, marking scouts "live" while the actual skill/agent/workflow files don't exist would produce stale-aspirational docs.

### 3. Clarity — PASS

Acceptance criteria are concretely testable. Examples:

- `grep -c 'pending Feature F' plugin/ralph-hero/skills/director/event-classes.md` returns `0`
- `grep -F 'ralph-hero:scouts | live' plugin/ralph-hero/skills/director/event-classes.md` returns ≥ 1
- `git diff --stat` shows exactly three changed files
- `test -f plugin/ralph-hero/skills/scouts/SKILL.md` exits 0

The Phase Success Criteria section is split cleanly into "Automated Verification" (nine grep/test commands) and "Manual Verification" (four read-through checks). Verbatim old/new text in Task 1.1 eliminates ambiguity about what the diff should look like. Both Constraint 3 (single source of truth for tier rationale) and Constraint 4 (preserve cron note) are restated in the Manual Verification list, providing a second checkpoint for reviewer attention.

### 4. Scope — PASS

The "What We're NOT Doing" section is unusually thorough for an XS plan — nine explicit exclusions covering: Phase 2 skill body, Phase 2 agent file, Phase 3 workflow YAML, Phase 5 validation, `ralph-pr` / `ralph-merge`, third docs surfaces (`unified-agent-system.md`), the `RALPH_SCOUTS_MODEL` mechanism itself, `scout-nightly.sh` migration, `memorykeepers` row, and the MCP server test suite. Each exclusion either points at the sibling phase that owns the work or explicitly defers to a follow-up issue (e.g., `unified-agent-system.md` if found stale post-merge).

Scope boundaries are reinforced by Constraint 2 ("No skill or agent behavior changes — documentation-only") and Constraint 5 ("Match existing table conventions ... Diff must be minimal and reviewable as a docs-only change"). The plan explicitly resists scope creep into `unified-agent-system.md` even though that file is a logical adjacent surface — discipline that's appropriate for an atomic XS phase.

### 5. Dispatchability — PASS

Every task is self-contained enough to hand to an impl-agent with zero additional context:

| Task | files | tdd | complexity | depends_on | acceptance |
|------|-------|-----|------------|------------|-----------|
| 1.1 | `plugin/ralph-hero/skills/director/event-classes.md` (modify) | false | low | null | 6 verifiable bullets including verbatim old/new text for 3 rows |
| 1.2 | `CLAUDE.md` (modify) | false | low | null | 8 verifiable bullets including verbatim new row text + grep assertions |
| 1.3 | `plugin/ralph-hero/docs/model-tier-policy.md` (modify) | false | low | null | 5 verifiable bullets including the exact new example line |
| 1.4 | (no edits — runs commands) | false | low | [1.1, 1.2, 1.3] | 8 verifiable bullets including the upstream-existence sanity check |

`tdd: false` is the correct call — these are docs edits and there's no executable code to write tests against. The acceptance bullets serve as the de-facto test suite (greps and `test -f` checks).

The plan does not require reading the full plan to understand any single task: Task 1.1 specifies the file, the lines, and the verbatim old/new strings; Task 1.2 specifies the row format inline; Task 1.3 specifies the exact new example-block line. Task 1.4 enumerates each verification command. An impl-agent could be dispatched to Task 1.2 alone and complete it correctly.

## Constraint-Specific Checks

| Shared Constraint | Status | Note |
|------|--------|------|
| 1. Docs must reflect reality (block on missing upstream) | PASS | Task 1.4 sanity check + `IMPL BLOCKED` verdict path documented |
| 2. No skill/agent behavior changes | PASS | All four tasks limited to three doc files |
| 3. Single source of truth (tier rationale in policy, not CLAUDE.md) | PASS | Task 1.2 row note documents the override env var; Task 1.3 owns the tier rationale; Manual Verification step explicitly re-checks this |
| 4. Preserve the cron note | PASS | Task 1.2 acceptance bullet spells out preserved text verbatim; Manual Verification re-checks |
| 5. Match table conventions (minimal diff) | PASS | `git diff --stat` exactly-three-files assertion + acceptance bullets that name "exactly three changed lines" for `event-classes.md` and "exactly two changed regions" for `CLAUDE.md` |
| 6. Override env var pattern (`RALPH_SCOUTS_MODEL`) | PASS | Both Task 1.2 (CLAUDE.md note) and Task 1.3 (examples block) reference the same env-var name; matches existing `RALPH_IMPL_MODEL`/`RALPH_SPLIT_MODEL` pattern |
| 7. Tier choice matches Phase 2 | PASS | Plan declares `sonnet` in Task 1.2 verbatim row text and cross-references Phase 2 plan in References section |

## Strengths

1. **Verbatim old/new text in Task 1.1.** Removes all ambiguity from the most fragile mutation (taxonomy rows that other Director code reads).
2. **Upstream-existence sanity check (Task 1.4).** The `IMPL BLOCKED needs=phase-2-or-3-not-yet-merged` verdict is the right escape hatch and matches the model-tier policy's BLOCKED escalation contract.
3. **Self-aware about reconstructed scope.** The plan notes that the GH-1314 plan-of-plans file is missing on disk and that it reconstructed scope from the issue body and sibling plans. This is honest and the right call rather than failing silently.
4. **Phase Success Criteria split into Automated / Manual.** Nine concrete grep/`test -f` commands plus four read-through checks. The automated set is sufficient to gate impl-agent → val-agent handoff.
5. **Constraint 3 enforcement is doubly-checked.** The "single source of truth for tier rationale" constraint is enforced once in Task 1.2 (CLAUDE.md row points to the override env var only) and again in the Manual Verification step.

## Minor Notes (not blocking)

1. **Line-range claim in Constraint 6.** The plan says examples are at "lines 29-33" of `model-tier-policy.md`. The actual examples block is at lines 29-33 inside a fenced code block that opens at line 29 and closes at line 33 — confirmed. This is fine, just noting the range is precise to the bash example lines, not the fenced-code-block boundaries. Not a blocker.
2. **Constraint 1 references the `blockedBy` graph.** The issue's `blockedBy` array at review time is empty in the GitHub state I fetched (the dependency is encoded only in the plan, not in the GitHub `blockedBy` field). This is consistent with the plan's intent — impl-agent's local `test -f` check is what enforces the gate at the implementation surface, not the GitHub dependency graph. The plan is right; the comment in Constraint 1 about "the `blockedBy` graph maintained by GitHub" slightly overstates GitHub's role. Trivial wording nit, not actionable.
3. **No CI considerations needed.** Plan correctly notes "No CI or test impact expected; the MCP server, hooks, and skill bodies are not touched." Good.

## Verdict

**APPROVED** — proceed to implementation.

Recommended dispatch: `/ralph-impl 1320`. Impl-agent should execute Tasks 1.1 → 1.2 → 1.3 in any order, then run Task 1.4 last. If Task 1.4's `test -f` checks for `plugin/ralph-hero/skills/scouts/SKILL.md`, `plugin/ralph-hero/agents/scouts-agent.md`, or `.github/workflows/playwright-auto.yml` fail, impl-agent must emit `IMPL BLOCKED needs=phase-2-or-3-not-yet-merged` and stop — that's working as designed.
