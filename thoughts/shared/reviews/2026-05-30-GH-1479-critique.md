---
date: 2026-05-30
github_issue: 1479
github_url: https://github.com/cdubiel08/ralph-hero/issues/1479
type: review
status: complete
verdict: APPROVED
plan_doc: thoughts/shared/plans/2026-05-30-GH-1479-hero-classify-agent-audience.md
---

# Review: GH-1479 — Hero `--mode classify` must read the queue with `audience: "agent"`

## Plan Reference

[`thoughts/shared/plans/2026-05-30-GH-1479-hero-classify-agent-audience.md`](https://github.com/cdubiel08/ralph-hero/blob/main/thoughts/shared/plans/2026-05-30-GH-1479-hero-classify-agent-audience.md)

## Strengths

- **Accurate root-cause diagnosis, verified end-to-end.** Every load-bearing claim checks out against the repo: exactly two `next_actions(` call sites, both in `ralph/skills/hero/SKILL.md` (line 139 default-mode picker; line 151 classify); no call sites in `dispatch.md` / `watch-dispatch.md`; `directions.ts:221` default `audience: "human"`; `audiencePenalty()` returning 0 for non-agent with `ESTIMATE_PENALTY` = `XS:0,S:0,M:20,L:40,XL:60` (null→30); and the Backlog fallback guarded on `config.audience === "agent" && scored.length === 0` (line 853).
- **Correctly scopes the negative assertion.** The plan recognizes that after the fix `next_actions({})` still appears once (the human picker), so a whole-file negative grep would be wrong, and scopes the negative check to the `## --mode classify` block via awk range extraction. The reviewer simulated the awk block-extraction + BRE grep and confirmed it works.
- **Test home and harness are real and idiomatic.** `ralph/hooks/scripts/__tests__/` is the established home; `caretake-watch-blockers.test.sh` has the exact helpers the plan cites (`assert_file_contains`, `pass`/`fail`, `REPO_ROOT` via `git rev-parse --show-toplevel`, `Results:` summary + `exit 1`). CI's `test-hooks` job auto-discovers `*.test.sh`, so no wiring needed.
- **Tight blast radius with disjoint phases.** Phase 1 touches only the skill body; Phase 2 only adds a new test file. The "What We're NOT Doing" section is thorough and correct (no `directions.ts` change, no picker flip, ralph-plugin release path). Tool-level coverage genuinely exists, so the plan rightly avoids duplicating it.

## Gaps

- **Verification quality (minor): Phase-1 grep count is fragile to the rationale wording, and the plan doesn't flag it.** The Phase-1 check `grep -c 'next_actions({})' → 1` only holds if neither inline rationale string re-embeds the literal token `next_actions({})`. The plan's proposed rationale prose happens not to (it references `audiencePenalty` / `scored.length === 0` / `AskUserQuestion`), so the count holds — but the constraint is implicit. Mitigation: when implementing, keep the rationale notes free of the literal `next_actions({})` token, or have Phase 2 assert on the awk-scoped classify block rather than a whole-file count. Implementable as-is; just under-specified.

## Failures

None.

## Recommended Changes

None required to make the plan implementable. Optional hardening (carry into impl, not a re-plan): in Phase 1, keep the inline rationale prose free of the literal `next_actions({})` token so the `grep -c … → 1` check stays exact (the proposed wording already satisfies this).

## Verdict

**Verdict**: APPROVED

**Confidence**: high — every structural and code-level claim was verified against the repo, and the trickiest mechanic (block-scoped negative grep) was simulated and confirmed to work.

**Score**:
- Phase clarity: PASS
- File ownership: PASS
- Verification quality: PASS
- Risk coverage: PASS
- Plan-of-plans alignment: n/a
- Estimate sanity: PASS
- Linked research present: PASS

Estimate-sanity note: S is right-sized — Phase 1 is a one-token edit plus two inline rationale notes in one file; Phase 2 is a single ~40-line bash test modeled on an existing template, with zero CI wiring and no mcp-server changes.

## Caveats

- The optional live behavioral check (`/ralph:hero --mode classify` against an empty-queue + Backlog board) was not executed — it requires live GitHub state and is outside static review. The behavioral claim is verified at the code level (`directions.ts:853`), sufficient for approval.
- Minor line-number drift: the penalty is applied at `directions.ts:503`, not `:504` as the plan's References note states — immaterial to implementation.
