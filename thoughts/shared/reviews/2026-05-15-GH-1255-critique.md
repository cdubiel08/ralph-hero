---
date: 2026-05-15
github_issue: 1255
github_url: https://github.com/cdubiel08/ralph-hero/issues/1255
plan_document: thoughts/shared/plans/2026-05-15-GH-1255-cos-phase3-morning-brief-ntfy.md
status: approved
type: review
tags: [plan-review, cos-mode, launchd, ntfy, morning-brief, unattended, auto-critique]
---

# Plan Critique — GH-1255 cos mode Phase 3 (morning brief + ntfy push)

## Verdict

**APPROVED** — single-issue, single-phase plan with five well-bounded tasks. All five plan-quality dimensions pass. No iteration required.

## Dimension Assessment

### 1. Completeness — PASS

The plan defines 1 phase containing 5 tasks. Every task carries `files`, `tdd`, `complexity`, `depends_on`, and `acceptance` checkbox lists. Files are explicitly tagged `(create)` / `(modify)` / `(read)`. Frontmatter contract for the brief output is fully specified inline (lines 169–177). The launchd plist's required keys (`Label`, `ProgramArguments`, `StartCalendarInterval`, `StandardOutPath`, `StandardErrorPath`, `EnvironmentVariables`, `RunAtLoad`) are enumerated against the snapshot precedent.

### 2. Feasibility — PASS

All referenced paths verified to exist (or correctly described as needing creation):
- `plugin/ralph-hero/scripts/cos/cos.sh` exists with `--role` flag (verified: `cos.sh:8` and parsing at `:92`).
- `plugin/ralph-hero/scripts/cos/cos-unattended.sh` exists as a Phase 2 stub (verified: prints "see Phase 3" and exits 0). The plan correctly identifies it as the file to replace.
- `plugin/ralph-hero/scripts/snapshot/launchd/com.ralph.snapshot.plist.template` exists and matches the shape described (verified: `/bin/bash -lc "cd … && ./run.sh"`, `RunAtLoad: false`, `EnvironmentVariables` block with `PATH`).
- `plugin/ralph-knowledge/src/parser.ts:42–43` PATH_TYPE_MAP claim verified: `{ segment: "/research/", type: "research" }` is exactly at those lines.
- `plugin/ralph-hero/skills/cos/` exists with `SKILL.md` and `system-prompt.md`; the new `prompts/` subdirectory does not yet exist (correctly noted as a creation in Task 3.1).
- `justfile` cos recipe (line 324) `exec`s `cos-unattended.sh "$@"` — replacing the script body (per Task 3.4) is sufficient; no justfile change required (the plan already notes this as a "modify only if needed" caveat).

One minor inconsistency surfaced: the plan references `scripts/dream/launchd/com.dubiel.dream-loop.plist.template` as a convention precedent (line 46, line 345), but `find` shows no such file under `plugin/ralph-hero/scripts/`. The dream-loop launchd template lives under `~/projects/ralph-hero/scripts/dream/launchd/` (the top-level scripts/ dir, not the plugin tree). This is a documentation pointer, not a load-bearing dependency — the plan still correctly anchors on the snapshot template (which does exist in the plugin tree). Not blocking; the planner could clarify the path in a future tweak but it doesn't affect implementability.

### 3. Clarity — PASS

Success criteria use the canonical `- [ ] Automated:` / `- [ ] Manual:` split (lines 286–306). Per-task acceptance lists are testable: `bash -n` syntax checks, `plutil -lint` validation, `grep -q` placeholder presence, exit-code assertions, file-existence checks, frontmatter regex matches. The SUMMARY-line extraction contract is precise: `grep '^SUMMARY:' "$tmp_stdout" | tail -1 | sed 's/^SUMMARY:[[:space:]]*//'`. Truncation rule (>120 chars → 117 + `...`) is unambiguous. Skip / warn behavior under each failure mode (no ntfy, no topic, empty SUMMARY) is enumerated separately so the implementer cannot conflate them.

### 4. Scope — PASS

The "What We're NOT Doing" section (lines 119–130) explicitly lists 9 deferrals, each tied to a future phase issue (#1257, #1258) or a clear rationale (no install helper until 4+ plists exist; no bats tests until Phase 4). The Pre-flight verification section (lines 51–65) names three deliberate divergences from the parent plan (script name, plist naming convention, no install-launchd.sh) and justifies each — this proactively prevents reviewer confusion at implementation time. Shared Constraints inherited from parent are explicit (lines 33–49); Phase 3-specific constraints (lines 44–49) carve out tighter rules (filename is a contract; plist is a template not a live plist; ntfy topic is private).

### 5. Dispatchability — PASS

Each of the 5 tasks is self-contained:

| Task | files | tdd | complexity | depends_on | acceptance |
|------|-------|-----|------------|-----------|-----------|
| 3.1 | yes (1 create) | false | low | null | 8 checkboxes incl. exact frontmatter shape |
| 3.2 | yes (1 create, 2 read) | false | medium | [3.1] | 18 checkboxes covering every branch (ntfy installed/missing, topic set/unset, SUMMARY empty/long) |
| 3.3 | yes (1 create) | false | low | [3.2] | 8 checkboxes incl. plutil-lint and weekday-array shape |
| 3.4 | yes (1 modify+create, 1 read) | false | medium | [3.2] | 6 checkboxes; explicitly notes discovery step (`grep -rn "unattended" …`) so the implementer can locate the stub |
| 3.5 | yes (1 modify, 1 optional) | false | low | [3.2, 3.3, 3.4] | 9 checkboxes covering each documentation section |

Dependency graph is correct: 3.1 → 3.2 → {3.3, 3.4} → 3.5. The plan also correctly notes in "Implementation Approach" that 3.2/3.3/3.4 can dispatch in parallel where deps allow.

For a single-phase plan, the cross-phase integration testing section (lines 312–317) is appropriately scoped — it covers the launchd Mon–Fri schedule edge case (Saturday excluded, Monday fires) which is the only cross-cutting concern.

## Notable Strengths

- **Pre-flight verification block** (lines 51–65) does the codebase grep work upfront — `command -v ntfy` checked, `jq` location checked, parent-plan divergences enumerated. This compresses the implementer's discovery loop significantly.
- **Defensive contract design**: SUMMARY-line truncation, ntfy graceful-degradation, OUT_PATH existence assertion, and the explicit "no Claude Code in the call chain" verification all reflect careful thinking about failure modes.
- **Filename is a contract** callout (line 45) flags a Phase 5 cross-cutting dependency in advance, preventing future name-churn.

## Minor Observations (non-blocking)

1. The dream-loop plist path reference (line 345) points outside the plugin tree (`scripts/dream/launchd/...`). Cosmetic — anchor citation is the snapshot template which is correctly located.
2. Task 3.4 says "Phase 2's stub may be inline in the justfile recipe or a separate script — discover via `grep -rn`". This is already known: it's at `plugin/ralph-hero/scripts/cos/cos-unattended.sh` and the justfile `exec`s it. The plan's discovery instruction is still safe (defensive) but the file is in fact already a script — the implementer will find it on the first grep.
3. No mention of a `--help` flag for `morning-brief.sh` itself (only the dispatcher's `--help`). The Phase Success Criteria automated verification (line 290) acknowledges this with "if morning-brief.sh has no --help, the dispatcher's usage exits 0 — confirm one of the two paths is documented." Adequate.

None of these block approval.

## Recommendation

Approve. Move issue to **In Progress** and dispatch `/ralph-impl 1255`.
