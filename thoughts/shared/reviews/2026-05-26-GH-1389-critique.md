---
date: 2026-05-26
github_issue: 1389
github_url: https://github.com/cdubiel08/ralph-hero/issues/1389
type: review
status: complete
verdict: APPROVED
plan_doc: thoughts/shared/plans/2026-05-26-GH-1389-knowledge-expert-domain-heuristic.md
---

# Review: GH-1389 — Document the knowledge_expert domain-extraction heuristic

## Plan Reference

[`thoughts/shared/plans/2026-05-26-GH-1389-knowledge-expert-domain-heuristic.md`](https://github.com/cdubiel08/ralph-hero/blob/main/thoughts/shared/plans/2026-05-26-GH-1389-knowledge-expert-domain-heuristic.md)

## Strengths

- Single trivial phase with a complete Overview + Changes Required (numbered three-priority heuristic) + Success Criteria triad — phase structure is unambiguous.
- File ownership is precise and verified: names `ralph/skills/research/research-shapes.md`, cites the real `knowledge_expert(domain="<domain>", ...)` call at line 36, the real "skip silently" fallback at line 77, and a real insertion point ("after the 'Use results to:' bullets, before `## Cross-repo addendum`"). All anchors confirmed present in the live file.
- Heuristic content matches the issue's stated intent (label match → noun-phrase extraction → skip); priority 3 aligns with the pre-existing line-77 fallback, so the addition is internally consistent.
- Scope correctly bounded for an XS doc add — `What We're NOT Doing` rules out code, SKILL.md body edits, and other sections; no over-scoping.

## Gaps

- **Verification quality**: the automated check `grep -ciE 'label match|noun.?phrase|skip'` is not falsifiable — the bare word `skip` already appears 5× in the unmodified file, so the command passes before any edit. Only `grep -nE 'Picking the .domain. parameter'` (currently 0) is a genuine gate. Non-blocking on an XS doc change, but the priority-presence check is theater as written.

## Failures

None.

## Recommended Changes

- Tighten the priority-presence verification to be falsifiable. Concrete swap: `grep -cE 'Issue label match|Noun-phrase extraction' ralph/skills/research/research-shapes.md` returns `2` (both currently 0). The implementer will use this falsifiable form in place of the loose `|skip` count.
- Render literal backticked `` `domain` `` in the actual markdown subsection (the plan doc shows escaped backticks, which is a plan-doc artifact only).

## Verdict

**Verdict**: APPROVED

**Confidence**: high — insertion point and all three line anchors verified against the live file; heuristic matches issue intent; the sole gap is a non-blocking weak-verification-command issue on an XS doc-only change. Impl will use the tightened falsifiable check.

**Score**:
- Phase clarity: PASS
- File ownership: PASS
- Verification quality: GAP
- Risk coverage: PASS
- Plan-of-plans alignment: n/a
- Estimate sanity: PASS
- Linked research present: n/a

## Caveats

- Test suites not executed by the reviewer (only glob paths confirmed to resolve). The plan's claim that they don't assert on `research-shapes.md` is plausible but unverified.
- Heuristic intent read from the plan's restatement + the existing line-77 fallback, not from #1389's body directly; the old `plugin/ralph-hero/skills/ralph-research/SKILL.md:183-201` source was deleted in GH-1438 and can't be cross-checked.
