---
date: 2026-03-01
github_issue: 488
github_url: https://github.com/cdubiel08/ralph-hero/issues/488
plan_document: thoughts/shared/plans/2026-03-01-GH-0488-spawn-n-builders-stream-detection.md
status: approved
type: critique
---

# Plan Critique: GH-488 Spawn N Builders from Stream Detection

## Summary

This plan modifies a single file (`plugin/ralph-hero/skills/ralph-team/SKILL.md`) to add stream-aware multi-builder spawning to the ralph-team orchestrator. The changes are organized into four logical edits: a roster table, a stream detection step before implementation tasks, updated spawn prompt guidance, and a dynamic post-mortem worker table. The plan depends on GH-487 (suggestedRoster in detect_stream_positions) and GH-465 (stacked branch infrastructure).

## Completeness

**Strong.** All four change sections include exact markdown content to insert, precise line references, and clear placement instructions. The plan covers the full lifecycle: roster documentation, stream detection timing, per-builder task assignment with `[stream-N]` tags, overflow handling for 4+ streams with 3-builder cap, single-stream fallback, and post-mortem reporting. Dependencies are explicitly listed. The success criteria include both automated grep checks and manual review items.

No gaps identified. The plan correctly identifies that this is a SKILL.md-only change with no TypeScript or infrastructure modifications.

## Feasibility

**Verified.** All technical claims check out:

- `plugin/ralph-hero/skills/ralph-team/SKILL.md` exists and contains the referenced content at the stated line numbers:
  - Line 48: "Spawn one worker per role needed based on the suggested roster from pipeline detection." -- confirmed
  - Line 50: spawn prompt paragraph -- confirmed
  - Lines 52-56: "Build the Task List" section -- confirmed
  - Lines 92-97: post-mortem worker summary table -- confirmed (plan says 93-97, actual table starts at line 92 with the heading; the data rows are at 95-97, so the reference is close enough for a human implementer)
- `plugin/ralph-hero/hooks/scripts/worker-stop-gate.sh` line 29 uses `builder*)` glob pattern, which already matches `builder-2` and `builder-3` without modification -- plan correctly notes no changes needed
- Research document exists at `thoughts/shared/research/2026-03-01-GH-0488-spawn-n-builders-stream-detection.md`

The plan's approach of editing a markdown skill file is low-risk -- no compilation, no runtime dependencies, no test suite changes needed.

## Clarity

**Good.** The plan provides verbatim markdown blocks for each change, making implementation unambiguous. The stream detection algorithm is presented as a numbered step-by-step procedure with a concrete `detect_stream_positions` call example. The naming convention (`builder`, `builder-2`, `builder-3`) and stream-to-builder mapping (`builder` = stream-1, `builder-2` = stream-2) are explicit.

One minor note: the plan references `detect_pipeline_position` in the roster table's "Initial spawn" paragraph alongside `pipeline_dashboard`, but the primary tool throughout is `detect_stream_positions`. This is not a contradiction -- they are different tools used at different phases -- but an implementer should be careful not to confuse them.

## Scope

**Well-defined.** The "What We're NOT Doing" section explicitly excludes MCP server changes, stacked branch mechanics, dynamic mid-session rescaling, more than 3 builders, and stream detection algorithm changes. Each exclusion maps to either a dependency issue or a deliberate design boundary. The single-file scope keeps the change atomic and reviewable.

## Verdict

**APPROVED.** The plan is complete, feasible, clearly scoped, and all file references have been verified. The single-file approach to a SKILL.md edit is appropriately sized for a Small estimate. No iteration needed.
