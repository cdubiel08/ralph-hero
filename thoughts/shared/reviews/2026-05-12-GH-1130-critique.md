---
date: 2026-05-12
github_issue: 1130
github_url: https://github.com/cdubiel08/ralph-hero/issues/1130
plan_document: thoughts/shared/plans/2026-05-12-GH-1130-mcp-boolean-coercion.md
status: approved
type: review
tags: [plan-review, mcp-server, zod, type-coercion, bug-fix]
---

# Plan Critique: GH-1130 MCP Boolean Param Coercion

## Verdict: APPROVED

Single-phase atomic XS plan. All five quality dimensions PASS. The plan is
dispatchable to a subagent without additional context.

## Dimension Assessment

### Completeness — PASS

Phase 1 defines five concrete tasks (1.1–1.5), each with explicit file paths,
TDD flag, complexity, dependency edges, and a checkbox acceptance list. The
"15 `z.boolean()` declarations across 10 files" inventory is provided as a
table in `Current State Analysis`, giving the implementer a closed worklist.

### Feasibility — PASS

Verified the referenced surface area against the live tree:

- `plugin/ralph-hero/mcp-server/src/tools/` contains all 10 files referenced.
- `plugin/ralph-hero/mcp-server/src/lib/` exists (target dir for the new
  `zod-helpers.ts`).
- `plugin/ralph-hero/mcp-server/src/__tests__/empty-params.test.ts` exists as
  the cited template for the new regression test.
- `grep -rn "z\.boolean\|\.boolean()" plugin/ralph-hero/mcp-server/src/tools/`
  returns exactly 15 hits matching the plan's table.
- `validateToolInput` patch is at `src/index.ts:471-473` (plan cites 472-474;
  off-by-one, immaterial — the right block).

The `z.preprocess` mitigation around `Boolean("false") === true` is a real
Zod pitfall and the chosen workaround is the idiomatic fix.

### Clarity — PASS

Every task has a checkbox acceptance list with verifiable predicates
(`grep` returns 0, specific `.parse()` results, `npm run build` clean,
`npm test` clean). Phase Success Criteria splits Automated vs. Manual
verification explicitly. Success criteria are testable, not aspirational.

### Scope — PASS

"What We're NOT Doing" is explicit and well-bounded: ralph-knowledge boolean
coercion deferred, `validateToolInput` patch path explicitly rejected with
rationale, no upstream filing, no `undefined`/optional behavior changes, no
existing-test churn. Scope sits comfortably inside XS.

### Dispatchability — PASS

Each task carries the required dispatch fields:

| Task | files | tdd | complexity | depends_on | acceptance |
|------|-------|-----|------------|------------|------------|
| 1.1  | yes (create) | true | low | null | yes |
| 1.2  | yes (create + read) | true | low | [1.1] | yes |
| 1.3  | yes (modify) | false | low | [1.1] | yes |
| 1.4  | yes (modify, 9 files) | false | low | [1.1] | yes |
| 1.5  | none (verify only) | false | low | [1.2, 1.3, 1.4] | yes |

Dependency DAG is sound (1.1 must land first; 1.2/1.3/1.4 parallelizable;
1.5 gates on all three). Task 1.4 is the only task with breadth (9 files)
but the per-file work is mechanical and constrained by the existing grep
output, so it remains within reach of a single subagent invocation.

## Notes

- Minor: Plan body says "14 `z.boolean()` declarations" in one paragraph
  (line 49) and "15 occurrences" in the table footnote (line 69). The table
  and live grep agree on 15. This is a cosmetic drift the implementer will
  notice but doesn't change the worklist.
- Minor: Plan cites `src/index.ts:472-474` for the `validateToolInput`
  patch; live source has the binding at line 472 and assignment at 473.
  Not load-bearing.

Neither warrants an iteration cycle.

## Recommendation

Approve and route to In Progress.
