---
date: 2026-05-03
github_issue: 974
github_url: https://github.com/cdubiel08/ralph-hero/issues/974
plan_document: thoughts/shared/plans/2026-05-03-GH-0974-reflect-backtick-yaml-scalar.md
status: approved
type: review
tags: [plan-review, dream-loop, reflect, yaml, parser, llm, bug-fix]
---

# Plan Critique: GH-974 — reflect.py Backtick-in-YAML Tolerance

Verdict: **APPROVED**

## 1. Completeness — PASS

The plan defines one phase with three coordinated tasks (sanitization, prompt update, regression test) and ties each to specific file paths and line ranges (`scripts/dream/reflect.py:31`, `:69-94`, `:340-365`, `:368-404`; `scripts/dream/tests/test_reflect.py`). The "Current State Analysis," "Desired End State," "What We're NOT Doing," and "Implementation Approach" sections are all populated. Acceptance criteria appear at both the task level and phase level.

## 2. Feasibility — PASS

Verified against the actual codebase:

- `scripts/dream/reflect.py` exists; `re` is imported at line 31, `yaml` at line 42 (matches plan claims).
- `_PROMPT_FOOTER` is at lines 69-94 (verified — Python string concatenation, no f-string, parenthesized).
- `_extract_frontmatter_block` is at lines 340-365 (verified).
- `_parse_llm_response` is at lines 368-404, with `_extract_frontmatter_block` called at line 390 and `yaml.safe_load(front)` at line 397 (verified — insertion point is exactly between these calls).
- `scripts/dream/tests/test_reflect.py` contains `TestSynthesizeReflection` with the `fake_post` / `http_post` seam pattern at line 352 (verified — matches plan's referenced pattern).
- The proposed regex `re.sub(r"`([^`\n]+)`", r"\1", front)` strips matched backtick pairs without crossing newlines, so the substitution only affects single-line scalar values where Gemma puts identifiers — won't mangle multi-line markdown bodies (which aren't passed to this regex anyway, since `front` is the extracted frontmatter only).
- The research document `thoughts/shared/research/2026-05-03-GH-0974-reflect-backtick-yaml-scalar-rejection.md` exists and the research comment on the issue confirms position-dependent failure mode and recommends the same combined Option A + B fix.

## 3. Clarity — PASS

Each task uses the dispatchability table format (files, tdd, complexity, depends_on, acceptance) and acceptance criteria are specific and testable:

- "insert a sanitization step between the `_extract_frontmatter_block(raw)` call (line 390) and the `yaml.safe_load(front)` call (line 397)" — pinpoint precision.
- Test acceptance specifies expected dict keys and lengths (`r["title"]` non-empty, `len(r["insights"]) == 2`, `r["source_ids"] == ["raw-00", "raw-01"]`).
- Phase Success Criteria split Automated / Manual cleanly with the exact `uv run` commands the implementer needs.

## 4. Scope — PASS

"What We're NOT Doing" enumerates five specific exclusions (YAML library swap, JSON migration, real-Gemma test, modifying the extractor helper, sanitizing the markdown body). The body-vs-frontmatter distinction is especially valuable — it preempts an implementer over-applying the regex to markdown content that legitimately contains backticks.

## 5. Dispatchability — PASS

All three tasks include the required fields (files with create/modify, tdd, complexity, depends_on, acceptance). Task 1.3 declares `depends_on: [1.1]` correctly because the test asserts behavior introduced by 1.1. Tasks 1.1 and 1.2 are declared independent (`depends_on: null`), which is accurate — the prompt change and the sanitization change touch different functions and don't share state. An implementer could land them in either order.

The only minor quibble is that Task 1.2's acceptance criterion "String concatenation is syntactically valid (the test suite still imports the module successfully)" is implicit (any working edit satisfies this) — but flagging it explicitly matches the plan's defense-in-depth ethos and isn't worth iterating over.

## Summary

The plan is tightly scoped, technically precise, and implementable as an atomic single-PR fix. It correctly applies the research recommendation (combined Options A + B), inserts the sanitization at the verified correct seam in `_parse_llm_response`, and adds a regression test that mirrors the production failure mode using the existing `http_post` test seam. No iteration needed.

Approved for implementation.
