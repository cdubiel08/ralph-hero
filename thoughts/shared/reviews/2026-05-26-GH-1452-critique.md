---
date: 2026-05-26
github_issue: 1452
github_url: https://github.com/cdubiel08/ralph-hero/issues/1452
type: review
status: complete
verdict: APPROVED
plan_doc: thoughts/shared/plans/2026-05-26-GH-1452-readme-claude-roster-drift.md
---

# Review: GH-1452 — Fix agent-roster drift in README.md and CLAUDE.md

## Plan Reference

[`thoughts/shared/plans/2026-05-26-GH-1452-readme-claude-roster-drift.md`](https://github.com/cdubiel08/ralph-hero/blob/main/thoughts/shared/plans/2026-05-26-GH-1452-readme-claude-roster-drift.md)

## Strengths

- Every factual claim verified true against `main`: `ls ralph/agents/` returns exactly the 16 files the plan lists; the 9 phantom names are present at `CLAUDE.md:76,78`; `README.md:27` has "Node.js 18+". No plan↔reality drift.
- The Phase 1 for-loop + phantom-grep verifications are genuinely runnable and falsifiable (flip from live-hits to empty after the fix) — gating, not decorative.
- Clean file ownership: Phase 1 owns `CLAUDE.md`, Phase 2 owns `README.md` — disjoint, both `depends_on: null`, parallel-safe.
- Tight scope: explicitly excludes siblings #1453-#1458, the #1458 CI check, and the #1383 dangling-ref; correctly attributes the drift origin to the GH-1438 rewrite.

## Gaps

- **Verification quality (minor):** Phase 2's phantom-grep (Success Criteria) checks only 5 of the 9 phantom names, vs Phase 1's full 9. Defensible (README:118 is a count-comment, likely never carried the 4 omitted names) but asymmetric and unexplained.
- **Verification quality (minor):** The MCP-tool-table step ("verify each row against `mcp-server/src/tools/`") is actionable but has no falsifiable command like the roster greps — it rests on a manual pass, complicated by the table using short names while registrations use the `ralph_hero__` prefix.

## Failures

None.

## Recommended Changes

Applied at implementation time (not blocking; verdict is APPROVED):
- Phase 2: use the full 9-name phantom alternation in the grep (symmetry with Phase 1), OR note README only carried the count-comment.
- MCP-table: add a falsifiable cross-check, e.g. `grep -oE 'ralph_hero__[a-z_]+' mcp-server/src/tools/*.ts | sed 's/ralph_hero__//' | sort -u` diffed against the README table's rows; label the table a curated subset if intentional.

## Verdict

**Verdict**: APPROVED

**Confidence**: high — every load-bearing factual claim independently verified true against `main`; headline verification commands run and are falsifiable; scope is clean. The sole GAP is soft verification-command symmetry on a doc-only change, addressed at impl time.

**Score**:
- Phase clarity: PASS
- File ownership: PASS
- Verification quality: GAP
- Risk coverage: PASS
- Plan-of-plans alignment: PASS
- Estimate sanity: PASS
- Linked research present: n/a

## Caveats

- Did not enumerate actual `ralph_hero__*` registrations across all `mcp-server/src/tools/*.ts` to confirm the README table's current correctness — deferred to impl (the falsifiable cross-check above).
- The implementing session reads the stale on-disk CLAUDE.md until merge; impl happens in an isolated worktree off `main`, corrected roster lands at merge.
